# 왈가왈BOT 현재 상태

마지막 업데이트: 2026-09-08

## 기준 문서

- 제품·안전·화면 범위: `PRD.md`
- IA·유저플로우·스타일 분석: `design-analysis.md`
- 구현 규칙: `AGENTS.md`, `.agents/rules/project_rules.md`
- Figma 근거: `자료종합`, `확정 스타일가이드`, `1차 디자인 시안 > 컨펌`

## 구현 완료

- 홈 화면: `src/pages/Home/Home.jsx`, `src/pages/Home/Home.css`
- 홈 구성: 오늘 사건, 최근 본 사건, 광고 배너, 밸런스 게임, 막상막하, 왈가왈후~, AI 맞춤 추천, 하단바
- 밸런스 게임의 선택 전·선택 후 시각 상태
- `App.jsx`에서 홈 화면 연결
- `main.jsx`의 React 렌더링 import 연결

## 구현 중 / 예정

- 배심원 광장: `src/pages/Plaza/`
- 사건 접수: `src/pages/Submit/`
- 사건 상세 및 AI 1심: `src/pages/Case/`
- 다음 행동, 챗봇·전문가 도움, 이어진 이야기: 담당 화면 컨펌 후 구현
- MY: `src/pages/My/`
- 공통 상단바·하단바 컴포넌트 분리와 화면 간 연결

## 확정된 UX / 디자인 기준

- 디자인 기준 아트보드는 iPhone 17의 402 × 874px이다. 긴 콘텐츠는 세로 스크롤한다.
- 홈은 Figma `1차 디자인 시안 > 컨펌 > 김하은/홈수정`을 시각 기준으로 삼는다.
- 자료종합의 스타일가이드(Font·Color)를 신규 화면의 공통 토큰 기준으로 사용한다.
- 페이지는 섹션을 조립하고, 재사용되는 카드·탭·칩·버튼·내비게이션은 React 컴포넌트로 분리한다. 화면 전용 조각은 각 페이지의 components에 둔다.
- 실제 서버가 없는 현재 단계에서는 카드·투표 수치가 예시 데이터이며, 밸런스 게임 선택은 화면 로컬 상태다.
- AI 판단은 실제 모델 결과가 아니라 UI 프로토타입이며, 근거·양쪽 관점·비결정적 표현을 유지해야 한다.

## 알려진 문제 / 확인 필요

- 자료 IA의 하단 메뉴에는 `알림`이 있으나, 현재 홈 컨펌은 `왈가왈후~`를 사용한다. 공통 하단바나 라우팅을 확정하기 전에 팀 확인이 필요하다.
- 다른 화면의 컨펌 시안이 확정되기 전에는 IA만으로 시각 스타일을 새로 확정하지 않는다.
- 실제 AI 분석, 로그인, 저장, 알림 API는 연결되어 있지 않다.
- Figma의 일부 원본 아이콘·일러스트와 실제 폰트 파일은 프로젝트 에셋으로 정리될 필요가 있다.

## 다음 작업

1. 각 담당자가 자료종합·확정 스타일가이드와 컨펌 화면을 기준으로 자신의 페이지를 섹션·컴포넌트 단위로 구현한다.
2. 공통 상단바·하단바의 메뉴 표기와 담당자·사용 방식을 합의한다.
3. 각 페이지 완성 후 `App.jsx` 또는 라우팅 담당자가 화면 이동을 통합한다.

## 마지막 검증 결과

- `npm run lint`: 통과
- `npm run build`: 통과
- 확인 기준: iPhone 17, 402 × 874px 아트보드
- 아직 확인하지 못한 범위: 다른 페이지의 화면별 컨펌 시안 및 연결 흐름