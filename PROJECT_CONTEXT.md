# 왈가왈BOT 현재 상태

## 후일담 연결 사건 판결 태그 통일 (2026-09-17)

- 후일담 게시 확인 파일의 ‘투표 종료’를 ‘판결 완료’로 바꿨다. 내가 올린 사건 카드와 동일한 배지 컴포넌트·스타일을 사용한다.
- 게시 확인 화면에서 바뀐 문구를 확인했고 `npm run lint`, `npm run build`, `git diff --check` 통과.

## 홈 막상막하 카드 반복 재생 (2026-09-17)

- 위 게이지 카드는 누르면 업데이트 예정 토스트만 표시한다. 아래 대기 카드나 ‘다른 사건’ 버튼을 누르면 두 사건이 교체된다.
- 교체된 사건의 게이지, 퍼센트, 판멍이 말풍선이 매번 처음부터 나타나도록 등장 상태를 초기화한다. 연타 중복 전환과 동작 줄이기 설정은 기존 방식대로 처리한다.
- 홈에서 위 카드의 토스트와 아래 카드의 사건 교체를 확인했다. `npm run lint`, `npm run build`, `git diff --check` 통과.

## MY 배심 참여 목록 페이지네이션 (2026-09-17)

- 배심 참여 사건을 한 페이지에 5건씩 표시하고 광장·댓글과 같은 `Pagination` 컴포넌트를 사용한다. 지훈 전체 12건은 5·5·2건, 투표 완료 8건은 5·3건으로 나뉜다.
- 필터를 바꾸면 첫 페이지로 이동한다. 사건을 열었다가 상단 뒤로가기로 돌아오면 필터·페이지·스크롤 위치를 함께 복원한다.
- 지훈 계정에서 1~3페이지 이동, 마지막 페이지 2건, 사건 상세 복귀 시 3페이지 유지, 투표 중 필터 변경을 확인했다. `npm run lint`, `npm run build`, `git diff --check` 통과.

## MY 배심 참여 상세 뒤로가기 수정 (2026-09-17)

- 배심 참여 목록에서 연 사건 상세·결과의 상단 뒤로가기는 기존 목록 기록으로 돌아간다. 목록을 새 기록으로 추가해 다시 상세로 되돌아가던 문제를 없앴다.
- 목록 복귀 시 저장한 필터와 스크롤 위치를 복원하고, 진입 슬라이드와 카드 순차 등장 애니메이션을 재생하지 않아 화면이 튀지 않는다.
- 지훈 계정에서 투표 중·투표 완료 사건의 상세/결과 복귀, 필터·스크롤 복원, 연속 뒤로가기를 확인했다. `npm run lint`, `npm run build`, `git diff --check` 통과.

## 지훈 MY 배심 참여 목록 (2026-09-17)

- MY의 ‘배심 참여 12건’을 누르면 단일 사건 결과 대신 `/my/jury`의 참여 사건 목록으로 이동한다. 지훈 시연 기록은 투표 중 4건, 투표 완료 8건으로 구성했다.
- 광장 `전체 사건` 카드를 `CaseFeedCard`로 분리해 MY 목록에도 동일한 카드, 상태 태그, 제목, 요약, 조회·댓글 수를 사용한다. 전체/투표 중/투표 완료 필터를 제공한다.
- 지훈의 기존 12건은 계정 투표 기록에도 반영해 진행 중인 사건을 다시 열어도 ‘이미 투표한 사건이에요’와 당시 선택이 표시된다. 새 투표는 기존 12건과 중복 집계되지 않는다.
- 카드에서 상세·결과로 간 뒤 상단 뒤로가기를 누르면 배심 참여 목록의 기존 필터와 스크롤 위치로 돌아온다.
- 지훈 계정에서 12건 목록, 4/8 필터, 진행 사건의 재투표 방지, 완료 사건 결과와 목록 복귀를 확인했다. 광장 카드 목록도 확인했고 `npm run lint`, `npm run build`, `git diff --check` 통과.

## AI 맞춤 추천 투표 중 태그 통일 (2026-09-17)

- 광장 사건 카드의 ‘투표 중’ 태그를 공통 `VotingStatusBadge`로 분리해 홈 AI 맞춤 추천의 진행 사건에도 동일하게 적용했다.
- 연파란 캡슐, 주황 점, 2.4초 호흡 애니메이션을 두 화면이 함께 사용한다. 동작 줄이기 설정에서는 점을 고정한다.
- 완료 사건의 AI·배심원 의견 태그는 기존 표시를 유지한다. 지훈 계정 홈에서 진행 사건 추천 연결을 확인했고 `npm run lint`, `npm run build`, `git diff --check` 통과.

## 하단 네비 선택 색상 보정 (2026-09-17)

- 선택 메뉴 뒤에 추가했던 연파란 사각 인디케이터를 제거했다. 기존 네비의 배경과 아이콘 배치는 유지한다.
- 메뉴를 바꾸면 선택된 아이콘 형태 안에서 파란색이 아래부터 차오르고, 글자색도 부드럽게 바뀐다. 동작 줄이기 설정에서는 즉시 전환한다.
- 홈에서 광장으로 전환해 사각 배경 없이 선택 상태가 표시되는 것을 확인했다. `npm run lint`, `npm run build`, `git diff --check` 통과.

## 하단 사건 접수 버튼과 첫 화면 진입 모션 (2026-09-17)

- 하단 중앙 사건 접수 버튼을 누르면 파란 원이 240ms 동안 살짝 아래로 눌리며 작아졌다가 돌아온 뒤 접수 화면으로 이동한다. 중복 클릭은 한 번만 처리한다.
- 사건 접수 첫 화면은 헤더를 고정한 채 진행 표시, 안내 문구, 입력 항목이 60ms 간격으로 아래에서 14px 올라오며 나타난다.
- 동작 줄이기 설정에서는 버튼 지연과 등장 모션을 생략한다.
- 로그인 상태 시연에서 하단 버튼 클릭 → `/cases/new` 이동과 첫 화면 표시를 확인했다. `npm run lint`, `npm run build` 통과.

## 마이페이지 섹션 순차 등장 (2026-09-15)

- 로그인 상태의 마이페이지 진입 시 프로필 → 플랜 → 나의 활동 → 설정 → 전문가 정보 → 로그아웃 순서로 섹션이 90ms 간격을 두고 등장한다.
- 각 섹션은 520ms 동안 아래에서 18px 올라오며 페이드인되고, 헤더와 계정 전환·로그아웃 오버레이의 기존 모션은 건드리지 않았다.
- `prefers-reduced-motion: reduce`에서는 섹션 등장 모션을 제거한다.

## 왈가왈후 진입 카드 아이콘 축소 (2026-09-15)

- 왈가왈후 홈의 `내 이야기 남기기`, `다른 이야기 읽어보기` 카드 아이콘 영역을 `78 × 66`에서 `68 × 58px`로 줄였다.
- 투명 여백 때문에 별도 확대하던 책 아이콘 배율도 `1.52`에서 `1.38`로 낮춰 두 카드의 시각 무게를 맞췄다.

## 홈 상단 검색 아이콘 교체 (2026-09-15)

- 홈 상단 네비의 검색 아이콘을 광장 검색바와 같은 넓은 원형 경로로 교체했다.
- 상단 네비의 기존 크기 `24 × 24`, 선 굵기 `2px`, 색상 `#252525`는 유지했다.

## 광장 카테고리 카드 순차 등장 (2026-09-15)

- 광장 `전체 사건`의 카테고리 칩을 바꾸면 사건 카드가 한꺼번에 교체되지 않고 위에서부터 90ms 간격으로 차례대로 나타난다.
- 각 카드는 480ms 동안 아래에서 16px 올라오며 페이드인되고, 검색어 입력 때마다 모션이 반복되지 않도록 카테고리 변경에만 목록을 재마운트한다.
- `prefers-reduced-motion: reduce`에서는 카드 등장 모션을 제거한다.

## 광장 트로피 광택 애니메이션 (2026-09-15)

- 광장 랭킹 시상대의 세 트로피가 모두 등장한 뒤 은색 → 금색 → 동색 순서로 작은 빛이 얼굴 가장자리를 스치는 광택 애니메이션을 추가했다.
- 마지막 순위 등장 모션이 끝나는 약 0.91초 이후인 1.2초부터 순차 재생하고, 각 트로피가 짧게 반짝인 뒤 쉬는 3.2초 주기로 계속 반복한다.
- `prefers-reduced-motion: reduce`에서는 광택 효과를 표시하지 않는다.

## 광장 검색바 순환 스트로크 (2026-09-15)

- 광장 `전체 사건` 검색바의 2px 연파란 테두리 위로 밝은 파란 하이라이트 구간이 2.6초마다 한 바퀴 순환하도록 적용했다.
- 회전이 잘 보이도록 하이라이트 구간은 넓게 유지하되 중심색은 부드러운 `blue-200` 수준으로 낮췄다.
- 검색바 내부는 유리 질감이나 배경 블러 없이 불투명한 흰색 단색으로 표시한다.
- 내부를 가로지르던 유리 광택 모션은 파란 레이저처럼 보여 제거하고, 애니메이션은 바깥쪽 2px 스트로크에만 적용했다.
- 포커스 상태는 회전 테두리를 유지하면서 연파란 외곽 그림자로 구분한다.
- `prefers-reduced-motion: reduce`에서는 순환을 멈추고 연파란 테두리 상태만 유지한다.

## 마이 계정 전환 닉네임 표시 (2026-09-15)

- 마이페이지의 계정 전환 시트에서 현재 계정과 전환 가능한 계정 이름 오른쪽에 각각 `익명의 왈가닥`, `익명의 왈랑이` 닉네임을 표시한다.
- 마이페이지 프로필과 같은 11px `neutral-650` 보조 텍스트 스타일을 사용하고 기존 이메일 줄은 유지한다.

## 회원가입 서아 자동 채우기 닉네임 (2026-09-15)

- 회원가입의 `서아 계정으로 채우기`를 누르면 닉네임 입력값이 `서아`가 아닌 `윤서아`로 채워진다.

## 회원가입 폼 묶음 수직 위치 보정 (2026-09-15)

- 1화면 고정 과정에서 위쪽으로 붙었던 `서아 계정으로 채우기`부터 동의 영역까지의 콘텐츠 묶음을 기본 화면에서 16px 아래로 이동했다.
- 하단 회원가입 CTA 위치는 유지하며, 높이 760px 이하 화면에서는 넘침 방지를 위해 콘텐츠만 8px 아래로 보정한다.

## 회원가입 1화면 고정 폼 간격 (2026-09-15)

- 회원가입 화면은 공통 헤더 아래 남은 높이 `100%`를 채우고 `auth-layout__scroll`의 세로 스크롤과 하단 여백을 제거해 한 화면에 고정한다.
- 필드 그룹 간격을 30px에서 실무 폼 리듬인 20px로, 동의 영역 상단 간격도 20px로 정리했다.
- 화면 높이 760px 이하에서는 필드 간격 12px·상단 32px·하단 푸터 여백을 자동 축소해 CTA까지 화면 안에 유지한다.

## 로그인 완료 목적지 홈 통일 (2026-09-15)

- 로그인 페이지가 `from` 쿼리의 마이페이지·사건 페이지로 돌아가던 동작을 제거하고, 일반 로그인과 서아 데모 로그인 모두 완료 후 항상 홈으로 이동하도록 통일했다.
- `from` 값은 로그인 페이지에서 회원가입으로 이동할 때의 안전한 내부 경로 전달에만 유지한다.

## 회원가입 자동 채우기 값 색상 완화 (2026-09-15)

- `서아 계정으로 채우기`로 입력된 닉네임·이메일·비밀번호·생년월일 값만 `neutral-700`로 낮춰 기존의 진한 본문색보다 부드럽게 표시한다.
- 사용자가 어느 입력값이든 직접 수정하거나 지우면 자동 채우기 표시 상태를 해제하고 일반 입력 색으로 복귀한다.

## 로그인 최신 공통 로고 적용 (2026-09-15)

- 로그인 페이지에서 한글·영문·법봉 SVG를 따로 조립하던 구형 워드마크를 제거하고, 네비와 PC 쇼케이스가 사용하는 최신 `wgwb-logo.svg`로 교체했다.
- 상단 마스코트는 유지하고 새 로고는 기존 로그인 워드마크 영역과 같은 134px 폭으로 표시한다.

## 광장 사건 키워드 데모 검색 (2026-09-15)

- 광장 `사연, 사건 키워드 또는 AI 추천 검색` 입력창의 비활성 상태를 제거하고 입력 즉시 결과가 갱신되는 검색 인터랙션을 연결했다.
- 어떤 검색어를 입력해도 연인·친구·가족·직장·학업 사건이 섞인 데모 결과가 나타나며, 검색어 문자값에 따라 결과 시작 순서가 달라진다.
- 검색 중에는 결과 안내 문구를 표시하고 전체 카테고리로 전환한다. 정렬 또는 카테고리 필터를 선택하면 검색어를 비우고 기존 목록 탐색으로 복귀한다.
## 챗봇 로그인 전 "다른 질문" 메뉴 분리 (2026-09-15)

- 로그인 전 상태에서 `내 사건에 대해 물어볼게요` → `caseIntroEmpty`(사건 없음 안내) → `다른 질문 할게요`로 들어오면, 기존에는 `restart` 스텝(문구 "더 궁금한 점이 있으면 골라주세요." + `내 사건에 대해 물어볼게요`를 포함한 5개 메뉴)이 그대로 다시 나와 방금 겪은 흐름이 반복되는 느낌이 있었다.
- `chatbotScript.ts`에 새 스텝 `guestMenu`(`GUEST_MENU_STEP_ID`)를 추가했다. 문구는 "좋아요. 어떤 내용이 궁금한가요? / 아래에서 궁금한 내용을 골라주세요."이고, `내 사건에 대해 물어볼게요`를 뺀 4개(이용 방법·AI 판정·전문가 상담·이용 중 문제)만 보여준다. 각 버튼의 `next`는 기존 `howToUse`/`aiResultInfo`/`needExpertInfo`/`troubleInfo`를 그대로 재사용해 이후 응답은 기존 플로우 그대로다.
- 라우팅은 `ChatbotPage.tsx`의 `resolveNextStepId`에서만 처리했다. `다른 질문 할게요`(id `more`)는 여러 스텝이 공유하는 버튼이라, "직전 스텝이 `caseIntroEmpty`이고 `sessionStatus !== 'authenticated'`일 때만" `guestMenu`로 보내고 그 외에는 기존처럼 `restart`로 간다. 계정(personaId)은 보지 않고 로그인 여부만 본다 — 서아·지훈 로그인 전 모두 동일하게 `guestMenu`로 가고, 로그인 후(예: 서아 인증 상태에서 우연히 caseIntroEmpty에 닿는 경우)는 이번 변경 이전과 똑같이 `restart`(5개 메뉴)로 간다. `caseIntroEmpty` 스텝 데이터 자체와 다른 스텝들의 `다른 질문 할게요`(howToUse, troubleInfo 등)는 손대지 않았다.
- `ChatOptionButtons`, 선택 인터랙션(오렌지 표시 → 그룹 제거 → 사용자 버블 추가), 버튼 스타일은 변경하지 않았다.
- `npm run lint`, `npm run typecheck`, `npm run build` 통과. 기존 500kB 초과 청크 경고는 유지된다. 실제 브라우저 클릭 시연은 이번 세션에서 실행하지 않고 `resolveNextStepId` → `requestChatbotReply` → `STEP_MAP` 경로를 코드로 추적해 서아/지훈 로그인 전 두 경우 모두 `guestMenu`로 귀결됨을 확인했다. → 확인 필요(실제 브라우저 검증)

## 챗봇 자유 입력 안내 플로우 변경 (2026-09-15)

- 사용자가 선택 버튼 없이 임의의 텍스트를 입력했을 때(현재 스텝에 `freeTextNext`가 없을 때) 나오던 "음, 지금은 정해진 답변만 드릴 수 있어요. 아래에서 골라볼래요?" + 그 스텝의 기존 선택지 재노출을, "혹시 다른 도움이 필요하신가요? 지금 하던 내용을 이어가거나, 다른 도움을 받을 수 있어요." + `이어서 진행하기`/`다른 도움 받기` 2버튼으로 바꿨다.
- `services/chatbotService.ts`의 `buildClarifyStep` 하나만 고쳤다. 이 함수가 자유 입력 안내가 나오는 유일한 지점이라(옵션 클릭이나 `freeTextNext`가 있는 스텝의 자유 입력은 그대로 기존 플로우를 탄다), 다른 대화 흐름은 건드리지 않았다.
- `이어서 진행하기`(`continue-flow`)의 `next`는 자유 입력 직전에 있던 스텝의 id(`resumeStepId`, 없으면 `RESTART_STEP_ID`) 그대로다. `STEP_MAP`에서 같은 키를 다시 찾아오는 것뿐이라 원래 보여주던 문구·선택지가 그대로 복원된다. `reply.kind === 'clarify'`일 때는 기존처럼 `activeStepId`를 바꾸지 않으므로, 이 안내 화면에서 또 자유 입력을 보내도 원래 스텝 기준으로 같은 분기를 다시 탄다.
- `다른 도움 받기`(`other-help`)는 새 스텝 `otherHelp`(`chatbotScript.ts`, `OTHER_HELP_STEP_ID`)로 이동한다. "어떤 도움이 필요한지 선택해주세요." 안내와 함께 `상담 준비하기`/`전문가 매칭 받기`/`전문가 랭킹 보기` 3개를 보여주는데, 이 세 id(`prep-questions`/`match-expert`/`expert-ranking`)는 이미 `consultCheck`에서 쓰던 것과 같아 `ALWAYS_DISABLED_OPTION_IDS`에 그대로 걸려 버튼만 보이고 클릭은 안 된다 — 기획대로 이 프로젝트에서 이후 화면·기능은 만들지 않는다.
- `ChatOptionButtons`·선택 인터랙션(오렌지색 표시 → 그룹 사라짐 → 사용자 버블 추가 → 다음 응답)과 사용자/챗봇 메시지 버블 디자인은 전혀 건드리지 않았다. 새 스텝도 기존 `BotStep`/`p()` 형태를 그대로 썼다. `step.optionsLayout`은 기존에도 더 이상 읽지 않는 필드라 새 스텝에 넣지 않았다.
- `npm run lint`, `npm run typecheck`, `npm run build` 통과. 기존 500kB 초과 청크 경고는 유지된다. 실제 브라우저 클릭 시연은 이번 세션에서 실행하지 않았다. → 확인 필요

## 챗봇 뒤로가기 기본 경로 보완 (2026-09-15)

- 09-14 병합 기록에 남아 있던 "챗봇 직접 진입 뒤로가기 기본 경로 누락"을 해결했다. `ChatbotPage.tsx`는 이미 `useWizardBack(BACK_FALLBACK.chatbot)`을 쓰고 있었지만 `routes/paths.ts`의 `BACK_FALLBACK`에 `chatbot` 키 자체가 없어 값이 `undefined`였다.
- `BACK_FALLBACK.chatbot = PATHS.home`을 추가했다. 앱 내부에서 챗봇으로 들어온 경우(플로팅 버튼·홈 AI 카드 등)는 원래대로 `navigate(-1)`이 실제 이전 화면으로 돌아가고, `/chatbot`으로 직접 진입해 돌아갈 내부 기록이 없는 경우에만 이 기본값(홈)으로 이동한다.
- `npm run lint`, `npm run typecheck`, `npm run build` 통과. 기존 500kB 초과 청크 경고는 유지된다. 다른 화면의 `useWizardBack` 사용(`cases`, `afterstory`)은 변경하지 않았다.

## 챗봇 선택지 UI 개편 (2026-09-15)

- 사용자 첨부 디자인 기준으로 챗봇(`/chatbot`) 대화 중 선택지 UI를 일괄 교체했다. 대상은 `ChatOptionButtons`(공용 선택 버튼 컴포넌트) 하나뿐이었다 — `STEP_MAP`의 모든 스텝(`caseIntro`의 2버튼 `actions`, `caseFocused`·`expertCheck`·`consultCheck` 등 여러 스텝의 세로 `chips`)이 같은 컴포넌트를 공유하고 있어 전수 검색으로 확인했다. 대화 시작 전 첫 화면(`ChatEmptyState`)의 추천 질문 칩은 애초에 메시지에 붙어 있던 적이 없는 별개 Figma 화면(`Chatbot/Initial`)이라 이번 범위에서 제외했다.
- 선택지를 챗봇 말풍선(`chatbot-bot-content`) 안에서 빼내 `chatbot-msg--bot`의 형제 요소로 옮겼다. 부모가 이미 `align-items: center`라 별도 래퍼 없이 대화창 중앙에 놓인다. 기존 `actions`(2버튼 가로) / `chips`(세로 목록) 구분과 그 CSS(`chatbot-action*`, `chatbot-option*`)는 지우고, 모든 선택지가 같은 너비로 한 줄씩 쌓이는 `chatbot-select-group` / `chatbot-select-option` 하나로 통일했다. `step.optionsLayout` 데이터 필드 자체는 스크립트 데이터라 남겨뒀지만 더 이상 읽지 않는다.
- 버튼은 `min-height`(고정 `height` 아님)로 만들어 문구가 길면 잘리지 않고 높이가 늘어난다. 기본 흰 배경, 고른 선택지만 `--orange-700`(#ff9524, 확정 스타일가이드 Secondary orange) 배경 + 흰 글자로 바뀐다.
- 동작 흐름: 클릭 즉시 `pendingSelection` 상태로 같은 그룹의 다른 버튼과 재클릭을 막고 고른 버튼에 오렌지색(`selectedOptionId`)을 표시 → 320ms(`SELECT_FEEDBACK_MS`) 뒤 해당 봇 턴에 `optionsHidden: true`를 표시해 선택지 그룹 전체를 화면에서 제거하면서, 동시에 고른 문구를 기존 사용자 말풍선 컴포넌트로 대화 기록에 추가 → 이어서 기존 `runRequest`가 다음 봇 응답을 스크립트대로 가져온다. `ChatTurn`(`bot` variant)에 `optionsHidden?: boolean` 필드를 추가했다. 페이지 이탈 시 지연 타이머는 `useEffect` cleanup으로 정리한다.
- 과거 턴은 `optionsHidden`이 참이면 `ChatOptionButtons` 자체를 렌더링하지 않아(이전에는 비활성 상태로 계속 남아 있었다) 다시 선택할 수 있는 것처럼 보이지 않는다. `isLatest && !isPending && !pendingSelection` 조건으로 항상 최신·미응답 선택지만 클릭 가능하다.
- 대화 순서·분기(`STEP_MAP`)·`chatbotService.ts`의 mock 요청 로직·`ALWAYS_DISABLED_OPTION_IDS` 처리(예: "다른 사건 선택할래요")는 변경하지 않았다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build` 통과(기존 500kB 청크 경고는 이번 변경과 무관하게 유지). 전체 검색으로 옛 `chatbot-action*`/`chatbot-option-list`/`chatbot-option` 클래스가 이 두 파일(`ChatOptionButtons.tsx`, `Chatbot.css`) 밖에는 없었음을 확인했다. 실제 브라우저 클릭 시연·402px 외 폭에서의 시각 검증은 이번 세션에서 실행하지 않았다. → 확인 필요

### 챗봇 선택지 UI — Figma 디테일 보정 (2026-09-15)

- 사용자 지정 Figma `ChatMessageList`(node `2168:6389`, 개발 페이지)를 `get_design_context` + `get_metadata`로 직접 읽어 위 개편에서 추측했던 값을 실측치로 맞췄다. `chatbot-select-option`·`chatbot-select-group`·`chatbot-msg--bot`(`Chatbot.css`)만 수정했고 컴포넌트 구조·상태 로직(`ChatbotPage.tsx`, `ChatOptionButtons.tsx`)은 건드리지 않았다.
- 바뀐 값: 버튼 사이 간격 10→8px, 좌우 패딩 20→16px, 그림자 `rgba(0,0,0,.08)`→`.05`(Figma 그대로), 글자 15px/1.4→14px/1.3(Figma `text/txt/14_M`), 정렬 가운데→왼쪽(`text-align:left`, `justify-content:flex-start`), 글자색 `var(--neutral-900)`→`#000`(Figma가 문자 그대로 `text-black`을 쓰고 있어 그대로 반영). 기본 상태에 옅은 파랑 테두리(`1px solid #d9e4ff`)를 추가하고, 선택된 버튼에는 흰색 1px 테두리(`border-color:#fff`)를 더했다 — 둘 다 기존 코드엔 없던 값이다. `min-height`는 47px로 맞췄다(Figma에 같은 문구의 40px짜리 그룹도 섞여 있었지만, `get_metadata` 좌표로 대조해보니 실제 대화 흐름 위치가 아니라 같은 자리에 겹쳐진 사양 참고용 중복 노드였다 — 실제 흐름에 있는 두 인스턴스는 모두 47px).
- `.chatbot-msg--bot`의 말풍선-선택지 간격을 16→40px로 올렸다. Figma에서 말풍선과 선택지 그룹 사이 간격이 다른 턴 사이 간격(`chatbot-thread`의 40px)과 정확히 같았다.
- 선택지 그룹 폭은 그대로 `100%`(부모 콘텐츠 폭)를 유지했다. Figma는 328px/282px 같은 고정값을 쓰지만 402px 프레임 전용 값이라 더 좁은 화면(예 360px, 콘텐츠 폭 312px)에서는 그대로 쓰면 넘친다 — 직전 개편에서 정한 반응형 원칙(PROJECT_SPEC.md 3장)을 우선했다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build` 통과(기존 500kB 청크 경고 유지). 실제 브라우저 시각 대조는 이번 세션에서 실행하지 않았다. → 확인 필요
- 후속 사용자 요청으로 선택지 그룹 폭을 `100%`에서 `300px`(고정)로 바꿨다. 다만 콘텐츠 폭이 300px보다 좁은 화면에서 넘치지 않도록 `max-width: 100%`를 함께 둬서, 300px가 확보되면 300px로 고정되고 그보다 좁으면 폭에 맞춰 줄어든다. 부모(`chatbot-msg--bot`)가 `align-items: center`라 줄어든 폭에서도 계속 가운데 정렬된다.

## 왈가왈후 해결사건 연결 (2026-09-14)

- 중복 인용문을 `다른 후일담 보러가기` CTA로 교체했다.
- CTA는 `/plaza?view=closed&section=cases`로 이동하고, 광장은 해결사건 목록만 처음부터 연다.

## PC 소개 정렬·설명 크기 보정 (2026-09-15)

- PC 왼쪽 소개 영역을 우측 정렬 flex 열로 바꿔 로고·설명·`기기 안에서 스크롤해 보세요` 안내의 오른쪽 끝선을 맞췄다.
- 로고 폭 `266px`은 유지하고 설명은 그 시각적 위계에 맞춰 Figma 원본 34px의 절반인 17px로 줄였다. 색상 강조와 600/700 굵기 대비는 유지한다.

## PC 소개 상세 문구 Figma 스타일 적용 (2026-09-15)

- Figma 노드 `883:5592`를 기준으로 PC 쇼케이스 왼쪽 설명을 `내 고민,` / `AI와 배심원이 함께 판단해드려요` 두 줄로 변경했다.
- Paperlogy 6 SemiBold 34px, 행간 1.35, 자간 0.68px, 우측 정렬을 적용하고 `AI`는 파랑, `배심원이`는 오렌지 Paperlogy 7 Bold로 강조했다.
- 프로젝트에 이미 포함된 `Paperlogy-6SemiBold.woff2`를 공통 폰트 등록에 재사용했다.

## PC 곽지훈 계정 카드 오렌지 호버 (2026-09-15)

- 서아가 현재 계정일 때 비활성 곽지훈 카드에 마우스를 올리면 파란 호버 대신 연한 오렌지 테두리와 옅은 오렌지 배경을 표시한다.

## PC 소개 로고 2배 확대 (2026-09-15)

- PC 쇼케이스 왼쪽 소개 영역의 브랜드 로고 표시 폭을 `133px`에서 `266px`로 정확히 2배 확대했다.
- 사용자 제공 Figma 노드 `883:5576`은 파일에서 찾을 수 없다는 응답이 반복되어 상세 설명 스타일 반영은 유효한 새 노드 링크 확인 후 진행한다.

## PC 시연 계정 곽지훈 오렌지 활성 상태 (2026-09-15)

- PC 쇼케이스의 시연 계정 선택에서 서아(A)는 기존 파란 활성 상태를 유지한다.
- 곽지훈(B)을 선택하면 계정 카드 테두리·이름·그림자가 서브 브랜드 오렌지로 전환된다.

## 네비·PC 배경 확정 로고 교체 (2026-09-15)

- 사용자 제공 `walgawalBotNEW 1.svg`를 공통 브랜드 에셋 `src/assets/brand/wgwb-logo.svg`에 반영했다.
- 같은 공통 에셋을 참조하는 모바일 상단 네비와 PC 쇼케이스 왼쪽 소개 영역이 모두 새 로고로 교체된다.
- 새 로고의 원본 비율은 `285 × 48`이며, 주변 레이아웃이 밀리지 않도록 기존 표시 폭(네비 100px, PC 소개 133px)은 유지한다.

## 파비콘·OG·사이트 메타 정보 확정 세트 적용 (2026-09-15)

- 사용자 제공 `favicon-og-set.zip`의 파비콘·Apple Touch·PWA·maskable·MS Tile 아이콘과 웹 매니페스트를 `public/favicon`에 반영했다.
- `og-image.png`(1200×630), `og-image@2x.png`(2400×1260), `twitter-card.png`(1200×675)를 `public` 루트에 추가했다.
- 사이트 제목·설명·키워드·작성자·robots·canonical·theme color·Open Graph·X 카드·MS Tile 메타 정보를 `index.html`에 병합했다. Vite 정적 파일 경로에 맞춰 OG/X 이미지는 `/home/` 아래가 아닌 배포 루트의 `/og-image.png`, `/twitter-card.png`를 사용한다.
- 기존 모바일 `viewport-fit=cover` 설정은 유지하고, 압축에 포함된 Next.js 전용 예시는 적용하지 않았다.

## 홈 오늘의 사건 실시간 참여 인원 (2026-09-15)

- 홈 `오늘의 사건`의 배심원 참여 인원이 1,245명에서 시작해 화면을 보는 동안 1~2.6초의 불규칙한 간격으로 2~8명씩 증가하고, 1,420명에 도달하면 멈춘다.
- 숫자는 쉼표를 고정한 채 실제로 바뀌는 각 자릿수가 오도미터처럼 위로 굴러 다음 숫자를 보여준다. `9 → 0`의 올림에서는 함께 바뀌는 앞자리도 독립적으로 회전하며, 자릿수 폭을 고정해 주변 문구가 흔들리지 않게 했다.
- 숫자의 크기·굵기·자릿수 슬롯 폭을 키워 회전 변화를 더 잘 보이게 했다. 백그라운드 탭에서는 타이머를 멈추며, 동작 줄이기 설정에서는 회전 없이 초기 숫자를 고정한다.

## PC 쇼케이스 배경 이미지 2안 적용 (2026-09-15)

- 선택한 배경 시안 2를 `src/assets/showcase/showcase-background.png`에 저장하고 PC 쇼케이스 배경 설정에 연결했다.
- 중앙 기기와 좌우 정보 패널의 가독성을 유지하도록 밝고 저대비인 종이 레이어·법봉 모티프 배경을 `cover` 방식으로 사용한다. 모바일 앱 화면에는 적용되지 않는다.

## PC 쇼케이스 소개 로고·고정 정렬 (2026-09-15)

- PC 기기 목업 왼쪽의 텍스트형 `왈가왈BOT` 제목도 확정된 `wgwb-logo.svg`로 교체하고 원본 비율 `133 × 24px`로 표시한다.
- 소개 문구를 `갈등을 안전하게 정리하고,` / `다음 행동을 함께 찾는 곳` 두 줄로 고정했다.
- 첫 내부 스크롤 후 안내 배지를 DOM에서 제거해 소개 블록 전체가 움직이던 동작을 수정했다. 배지는 투명하게 숨기되 레이아웃 공간을 유지해 로고와 소개 문구의 위치가 움직이지 않는다.

## 홈 상단 네비 확정 로고 적용 (2026-09-15)

- 확정된 `wgwb-logo.svg`를 프로젝트 공통 브랜드 에셋으로 추가하고, 홈 상단 네비 왼쪽의 텍스트 조합 로고를 모바일 앱 헤더에 맞는 약 `100 × 18px` SVG 로고로 교체했다.
- 배심원 광장처럼 가운데 페이지 제목을 표시하는 제목형 `TopBar`는 기존 표시를 유지한다.

## 지난 사건 2심 투표 결과 진입 애니메이션 (2026-09-15)

- `/cases/case-company-874/result`의 `2심 배심원 투표 결과`에서 실제 네 개 막대 목록이 앱의 실제 스크롤 영역 하단 78% 지점에 도달하면 퍼센트 숫자와 막대가 0부터 각 최종값까지 함께 증가한다.
- 콘텐츠 전체 높이를 가진 `.case-result__body`를 관찰 영역으로 잘못 사용하면 페이지 진입 즉시 교차한 것으로 판정되는 문제를 실제 브라우저에서 확인해 제거했다. 마우스 휠·터치·스크롤바·키보드 등 사용자의 스크롤 의도가 확인된 뒤에만 실행하므로 진입 및 브라우저 스크롤 복원만으로 미리 재생되지 않는다.
- 애니메이션은 페이지 진입당 한 번만 실행하며, 운영체제에서 동작 줄이기를 설정한 사용자는 최종 결과를 즉시 본다.

## 광장 전체 사건 카드 지난 사건 연결 (2026-09-15)

- 광장 전체 사건 목록의 모든 카드를 클릭 가능한 링크로 통일하고, 필터·카테고리·페이지와 관계없이 시연용 지난 사건 상세 `/cases/case-company-874`로 이동하게 했다. 카드별 제목은 접근성 레이블에 유지한다.

## 광장 사건 필터 제목 띄어쓰기 (2026-09-15)

- 광장 필터의 다섯 제목을 `최신 사건`, `인기 사건`, `진행 사건`, `해결 사건`, `추천 사건`으로 띄어쓰기 통일했다. 선택 버튼과 드롭다운 목록이 같은 데이터를 사용하므로 양쪽에 함께 적용된다.

## 왈봇 플랜 선택 카드 상태·인터랙션 (2026-09-15)

- 월간·연간 플랜 카드의 비선택 상태는 중립 회색 테두리와 흰 배경으로 바꾸고, 선택된 카드에만 주황 테두리·연한 주황 배경·강조 그림자를 적용했다.
- 체크 아이콘 자리를 항상 확보해 선택 전환 때 제목이 움직이지 않게 했고, 선택 시 아이콘이 부드럽게 나타난다. 포인터 hover·눌림·키보드 포커스와 움직임 줄이기 설정도 반영했다.

## 왈봇 플랜 무료체험 하단 CTA 고정 (2026-09-15)

- 무료체험 선택 화면을 앱 뷰포트 전체 높이로 고정하고, 모바일에서는 `100vh`와 `100dvh`에서 상단 안전 영역을 제외한 실제 표시 높이를 사용한다.
- 사건접수 화면과 동일하게 본문만 스크롤되고 `무료체험 시작하기`·`다음에 할게요` 푸터는 화면 하단에 남도록 했다. 기존 투명 푸터 예외를 제거해 공통 흰 배경·상단 그림자·안전 영역 여백을 사용하며, 토스트 높이도 공통 사건접수 푸터 기준을 따른다.

## 하단 내비게이션 화면 토스트 위치 (2026-09-14)

- `MainLayout`의 모든 공통 토스트를 하단 내비게이션 기준 16px에서 32px 위로 올려, 위로 솟은 중앙 사건 접수 CTA와도 여백이 보이도록 조정했다. 하단 내비게이션이 없는 고정 푸터 화면의 별도 토스트 위치는 유지한다.

## PC 시연 계정 프로필 사진 (2026-09-14)

- 기기 오른쪽 시연 계정 선택 버튼에 MY 계정 전환에서 사용하는 윤서아·곽지훈 프로필 사진을 이름 왼쪽에 추가했다. 기존 계정 전환·로그인 상태 동작과 선택 스타일은 유지한다.

## 공통 헤더 하단선 통일 (2026-09-14)

- 홈·광장·사건 상세·사건 접수·왈가왈후·MY·플랜·챗봇 헤더를 `64px` 높이, 화면 전체 폭, `1px #F0EEF1` 하단선으로 통일했다. 광장·플랜에만 있던 66px 예외와 홈/챗봇의 다른 선 색을 제거했다.
- 402px Edge 확인: 주요 메뉴와 접수·플랜·MY 하위·사건 상세 헤더 모두 `0~402px`, `64px`, `1px rgb(240, 238, 241)`로 일치. 린트 통과.

## 왈가왈후 진입 카드 아이콘 정렬 (2026-09-14)

- `다른 이야기 읽어보기`의 책 PNG에는 왼쪽 투명 여백이 크게 포함되어 있어, 아이콘이 카드 오른쪽으로 밀리고 연필보다 작아 보였다. 책 이미지는 오른쪽 기준으로 잘라 실제 그림을 중앙에 배치하고, 시각적 높이를 왼쪽 연필 아이콘과 맞췄다.
- 두 진입 카드의 레이아웃 기준 상자는 모두 `78 × 66px`로 유지했다. 카드·문구·다른 후일담 영역은 수정하지 않았다.
- 검증: 402px Edge 자동 캡처에서 두 아이콘의 중심·높이와 본문 간격을 확인했다. `npm run lint`, `npm run typecheck`, `npm run build` 통과. 기존 500kB 번들 경고는 유지된다.
## 로그아웃 확인 팝업 여백 조정 (2026-09-14)

- 사용자 후속 요청으로 텍스트 확인 팝업의 공통 `profile-switch-confirm--message` 스타일을 `AccountSwitchSheet.css`에 통합했다. MY 페이지에 흩어져 있던 로그아웃 팝업 전용 규칙은 제거하고 서아·지훈이 같은 컴포넌트/스타일을 사용한다.
- 폭 312px을 유지하며 상단 32px, 좌우·하단 24px, 제목과 버튼 영역 사이 28px, 버튼 간격 8px을 적용했다. 확인 버튼은 48px, 취소는 44px 높이로 조정했다. 실제 전체 높이는 약 206px이다. 기존 프로필 전환 팝업의 크기는 유지한다.
- Chrome에서 PC 목업과 320×640/390×874/844×390 모바일 영역을 확인했다. 320px에서는 폭 272px로 축소되고 모든 크기에서 가로·세로 잘림이 없었다. 확인 후 팝업 닫힘·홈 이동도 확인했으며 임시 검증 파일을 삭제했다.
- 타입 검사·린트·프로덕션 빌드·diff 검사를 통과했다. 기존 번들 크기 경고는 유지되며 실제 Vercel 배포는 하지 않았다.

## 가입 환영 문구 및 MY 로그아웃 (2026-09-14)

- 가입 완료 제목은 사용자 지정 Figma `2378:31776`의 Paperlogy 7 Bold · 22px · 120% · `#374BFF`, 설명은 `2378:31778`의 Pretendard Regular · 18px · 120% · `#78757A`로 반영했다. 기존 로컬 폰트와 토큰을 사용하며 버튼 문구는 `시작하기`로 변경했다. 홈 이동·가입 보상 연결을 유지한다. 작업 시작 당시 사용자가 변경해 둔 영상 배속 1.4도 그대로 보존했다.
- 서아/지훈이 공유하는 `MyPage`에서 전문가 정보 기록 아래에 가운데 정렬·밑줄 `로그아웃 하기` 버튼을 추가했다. MY 계정 전환의 기존 중앙 오버레이·버튼 스타일을 재사용한 `LogoutConfirmDialog`에 `로그아웃 하시겠습니까?`와 확인/취소를 표시한다.
- 확인은 기존 `signOut()` 후 `/home`으로 replace 이동한다. 취소·Esc·배경 클릭은 세션을 변경하지 않는다. 취소 버튼 초기 초점, Tab 순환, 배경 inert·스크롤 잠금 및 닫힐 때 초점 복원을 적용했다. 기존 계정 전환 팝업의 로그아웃은 유지한다.
- 브라우저 검증: 서아·지훈 각각 취소 시 MY 로그인 유지, 확인 시 비로그인 홈 이동, 새로고침 후 로그아웃 유지 확인. Esc 닫기·Tab 순환·초점 복원·배경 차단, 모바일 확인 후 홈 이동을 확인했다. 가입 환영 문구의 computed font/weight/line-height와 시작하기의 홈 이동도 검증했다.
- 화면 확인: PC 목업, 모바일 360/390/402/430px 제목·설명 줄 수 및 가로 넘침 없음. 로그아웃 팝업은 360/390/402/430/844px에서 폭 312px·가로 넘침 없음, 844×390 가로 화면에서도 팝업 전체가 표시됐다. 임시 iframe 검증 파일은 삭제했다. 실기기 터치는 미검증이다.
- 로컬 변경이며 실제 Vercel 배포는 미실행이다. 로그인 로고·파비콘 및 다른 화면의 기존 미커밋 작업은 보존했다.
- 최종 검사: `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과. 기존 500kB 번들 경고는 남는다.

## 로그인 로고 통일 (2026-09-14)

- 사용자 지정 Figma `5msPuamjPpGJOUFl0OXBOX / 2264:13801`의 판멍이 아이콘과 검정·파랑 워드마크로 `/login` 브랜드 영역을 교체했다. 비밀번호 숨김·표시는 하나의 `LoginPage`가 공유하므로 두 상태 및 빈 입력 상태에 같은 로고가 표시된다.
- 원본 PNG 1개·SVG 3개를 `src/assets/auth/brand/`에 저장했다. 아이콘 62 × 62px·반경 15px, 워드마크 134 × 24.839px와 원본 간격을 반영했다. 로그인 폼과 인증 동작은 유지했다.
- 후속 사용자 지시대로 파비콘과 `index.html`은 변경하지 않았다. 홈·광장·쇼케이스 로고 및 Figma 원본 파일도 이번 코드 수정 범위에 포함하지 않는다.
- 검증: 타입 검사·린트·프로덕션 빌드 통과. Chrome에서 자동 입력과 비밀번호 표시 전환, 이미지 4개 정상 로드, PC 목업과 390/402px 시각 확인. 임시 iframe으로 360/390/402/430/844/1366/1440px 콘텐츠 가로 넘침·깨진 이미지 없음 확인. 실기기 미검증이며 기존 500kB 번들 경고가 남는다. 임시 검증 페이지는 삭제했다.
- 로컬 반영 완료, 실제 Vercel 배포는 미실행. 로그인 변경만 담은 별도 패키지를 `output/login-brand-update.zip`으로 준비한다. 기존 가입 완료 영상 1.3배속과 다른 화면의 미커밋 변경은 보존한다.

## 가입 완료 페이지 미드저니 영상 교체 (2026-09-14)

- 1.3배속 후속 검증: 타입 검사·린트·프로덕션 빌드를 다시 통과했다. Chrome에서 새로고침 후 `playbackRate`와 `defaultPlaybackRate`가 모두 1.3이며 원본 시각 7초에서 정상 종료(`ended: true`, 영상 오류 없음)됨을 확인했다. 실제 배포는 미실행 상태다.

- 사용자 요청으로 `/signup/complete`의 정지 캐릭터를 미드저니 영상으로 교체했다. 지정 파일 `social_u5845923537_A_single_continuous_5-second_3D_animation_using_t_cacde3ca-c06a-4fb8-9020-43aa457a32e4_1.mp4`에서 **2초 이상 9초 미만의 7초 구간**만 사용한다.
- `src/assets/auth/signUpCompleteMascot.mp4`는 H.264, 720 × 738, 24fps, 168프레임, 약 1.31MB다. 프레임을 다시 인코딩해 정확히 잘랐고, 무음·faststart로 웹 재생을 준비했다. 원본의 흰 배경을 유지한 MP4이며 알파 투명 영상은 아니다.
- 후속 요청의 재생 사양은 **페이지에서 1.3배속**이다. MP4 파일은 기존 7.00초 그대로 유지하고 실제 재생 시간은 약 **5.38초**(`7 / 1.3`)로 한다. 속도 설정 위치는 `src/pages/Auth/SignUpCompletePage.tsx`의 `SIGNUP_VIDEO_PLAYBACK_RATE` 상수다.
- 무음 자동 재생·인라인 재생을 사용하고 반복하지 않는다. 재생이 끝나면 마지막 프레임을 유지한다. `object-fit: contain`으로 원본 프레임을 자르지 않으며, 문구·CTA·가입 완료 이후 이동 동작은 유지한다.
- `poster`로 로딩/자동 재생 제한 시 기존 캐릭터를 보여주고, 영상 오류 또는 `prefers-reduced-motion`에서는 기존 그림과 원 배경을 표시한다. 영상의 재생 상태는 자식 컴포넌트가 소유해 움직임 줄이기 설정 전환 때 초기화한다. Safari의 자동 재생을 막지 않도록 영상 자체에 `visibility: hidden`을 적용하지 않는다.
- 최초 영상 교체의 1배속 버전 검증 기록: 영상 전체 168프레임 디코딩, 7.00초 길이 확인. Chrome에서 자동 재생과 7초 종료 후 마지막 프레임 유지, 390px 모바일 콘텐츠 영역 및 PC 화면의 영상·문구·버튼 배치를 확인했다. 타입 검사·린트·프로덕션 빌드 통과. 실제 iOS 기기와 OS 움직임 줄이기 전환은 미검증이다.
- 실제 Vercel 배포는 미실행. 사용자가 팀장 소유의 Hobby 프로젝트이며 현재 계정에 배포 권한이 없다고 확인했다. 영상과 이번 화면만의 패치를 팀장에게 전달해 적용·배포하는 방식으로 인계한다. 기존 다른 화면의 미커밋 변경은 패치에 포함하지 않는다.
## 홈 민트초코 밸런스 게임 (2026-09-14)

### 색상 통일 후속 수정
- `싫다` 결과에서 흰 사각형이 보이던 문제를 수정했다. 기존 RGB 이미지 `mint-bowl-red.png`는 보존하고, 실제 알파 채널이 있는 `mint-bowl-red-transparent.png`를 새로 생성해 연결했다. Sharp 검사 결과 `isOpaque: false`를 확인했다.
- 최신 사용자 요청에 따라 왼쪽 `좋다`는 파란 그릇, 오른쪽 `싫다`는 빨간(코랄) 빈 그릇으로 통일했다. 아래 최초 기록의 보라/파랑 배치는 이전 버전이다. 선택지 글자·결과 강조·선택 테두리는 민트 전용 파란색 덮어쓰기를 제거하여 깻잎과 같은 `--blue-600` / `--negative`를 그대로 사용한다.
- `mint-bowl-blue.png`를 왼쪽에 재사용하고, 새 파일 `src/assets/home/figma/mint-bowl-red.png`, `src/assets/home/figma/mint-bowl-filled-blue.png`를 추가했다. 기존 보라 그릇·보라 결과 이미지는 삭제하지 않았다. 내장 이미지 생성 도구로 그릇만 색을 바꿨으며 흰 배경 RGB PNG로 저장했다. 그릇 모양·스쿱·질문·배치·드래그 동작은 유지한다.
- 파란 결과 프롬프트: `Use case: precise-object-edit. Image 1 is the edit target: mint chocolate ice cream in a lavender scalloped pedestal dessert bowl. Image 2 is the color reference: the same empty bowl in pale cornflower blue. Change ONLY the entire bowl color in image 1 (rim, fluted cup, stem, foot) from purple to the light blue of image 2. Keep the green ice cream and brown chocolate completely unchanged. Preserve exact object size, framing, shape, lighting, shadows on object, and white background of image 1. No spoon, no decorations. Pure white background, no checkerboard.`
- 빨간 빈 그릇 프롬프트: `Use case: precise-object-edit. Image 1 is the edit target: EMPTY blue scalloped pedestal ice cream dessert bowl. Image 2 is a COLOR REFERENCE ONLY, a coral red plate. Recolor ONLY the bowl of image 1 to the soft coral-red/salmon color of image 2. Keep the exact scalloped pedestal bowl shape, stem, base, viewing angle, lighting and proportions of image 1, NOT the flat plate shape. Bowl remains completely EMPTY. Maintain original framing: object fills about 88 percent of width, 96 percent of height, aspect ratio 268:188. White background #FFFFFF, no checkerboard, no floor shadow. No ice cream, no spoon, no other objects, no lettering.`

### 최초 구현 기록

- 기존 깻잎 게임 옆에 민트초코 문항을 추가했다. 제목 옆 `다음 ›`으로 1/2 깻잎 ↔ 2/2 민트초코를 전환한다. 새로고침은 현재 문항 다시하기다. 다른 홈 섹션·광장은 수정하지 않았다.
- 질문 `민트초코,당신의 선택은?`, 왼쪽 보라색 그릇 `좋다`, 오른쪽 파란색 그릇 `싫다`. 스쿱을 좌우로 드래그해 손을 놓거나 선택 버튼을 누른다. 드래그 중에는 확대 미리보기만 하고, 짧은 이동·취소는 확정하지 않는다.
- `좋다`: 선택 그릇 확대 후 민트초코가 채워진 보라색 그릇으로 전환. `싫다`: 파란색 빈 그릇만 확대하며 스쿱과 채워진 결과 이미지는 남기지 않는다. 두 문항은 `BalanceRound`의 Pointer Events·타이머·선택 상태를 공유하고 민트 전용 배치만 `BalanceGame.css`로 분리했다.
- 원본 세 소재를 `src/assets/home/figma/mint-scoop.png`, `mint-bowl-purple.png`, `mint-bowl-blue.png`로 복사했다. 원본 파일은 보존했다. 결과 이미지는 같은 폴더 `mint-bowl-filled.png`에 저장했다. **기본 내장 이미지 생성 도구**로 제작했으며 흰색 게임 배경에 쓰는 RGB PNG다(투명 PNG로 표기하지 않는다).
- 이미지 생성 요청: 제공된 보라색 꽃잎형 굽 그릇의 형태·색·각도를 유지하고 민트초코 3스쿱으로 채우기, 스푼·장식·글자 없음. 최종 배경 보정 프롬프트: `Replace the checkered background with SOLID PURE WHITE (#FFFFFF). This is a white-background product photograph. No transparency pattern, NO checkerboard, NO gray squares. Keep the mint ice cream and lavender pedestal bowl unchanged. Entire background must be featureless pure white. No floor shadow. Single isolated object on pure white.`
- 문항을 바꾸거나 퍼소나를 전환하면 로컬 선택을 초기화한다. 서버 투표나 집계 수치는 추가하지 않았다. 모션 줄이기 설정에서는 중간 연출 없이 결과를 즉시 표시한다.
- 검증: `npm run build`·`npm run lint` 통과. Edge 자동화로 402px에서 선택 전/좋다/싫다 화면 캡처와 양쪽 선택, 다시하기, 깻잎 결과 회귀 확인. 360·390·402·430px 및 1440px PC 목업에서 오른쪽 드래그, 키보드 Enter 선택, 결과 영역 잘림 없음, 모션 줄이기 확인. 문항 전환 시 타이머 정리·짧은 드래그·취소도 확인했다. 실기기 터치는 미검증. 번들 500kB 초과 경고는 남아 있다.
## 광장 랭킹 안내문 텍스트 스타일 변경 (2026-09-13)

- 사용자 지정 `2101:21823`의 최신 `text/txt/11_R` 스타일을 확인했다. `RankingDescriptionText`를 Noto 대신 기존 본문 토큰 `--font-body`(Pretendard)로 변경하고 11px·400·줄간격 140%·자간 0·`#78757A`를 적용했다. 사용하지 않는 Plaza Noto 폰트 등록도 제거했다.
- 타입 검사 포함 빌드·린트·diff 검사 통과. 이 변경이 이전 Noto 사용 기록보다 우선한다.

## 광장 챗봇 아이콘 복원 (2026-09-13)

- 후속 사용자 요청으로 광장 숨김 조건을 제거했다. 다른 주요 페이지와 동일한 56px 회전 판멍이 아이콘을 하단 내비게이션 위에 표시하고 `/chatbot`으로 연결한다. 아래 시안 반영 기록의 챗봇 숨김 결정보다 이 요청이 우선한다.
- 타입 검사 포함 빌드·린트·diff 검사 통과. 격리 Chrome 390/1440px에서 광장 아이콘 표시와 클릭 후 챗봇 이동을 확인했다.

## 광장 최신 Figma 시안 반영 (2026-09-13)

- 사용자 지정 `5msPuamjPpGJOUFl0OXBOX / 2101:21799`의 디자인 컨텍스트·주석·402 × 1949 원본 캡처를 확인해 광장을 수정했다. 본문 내 구버전 광장 상태 설명보다 이 기록이 우선한다.
- 랭킹을 트로피 시상대로 교체했다. 배경·판멍이·트로피·시상대·버튼 화살표·검색 아이콘은 Figma 원본을 `src/assets/plaza/`에 저장했다. 순위/이름/포인트는 이미지에 굽지 않고 실제 텍스트로 표시한다. 845pt·증거 수집가·랭킹 전체 보기 문구, 599px 히어로·285px 패널·간격을 반영했다.
- 최신 첫 4개 사건의 카피·147 조회수·연애 표기·조회수 단위와 배지 색을 맞췄다. 배지 색을 일치 여부에서 분리했으며, 기존 20건/카테고리 필터/4개 단위 페이지네이션/첫 사건 상세 연결은 유지했다.
- 정렬 주석에 따라 네이티브 선택 컨트롤로 최신순/종결된 순을 제공한다. `?sort=closed`로 새로고침·상세에서 뒤로가기 시 정렬을 유지하고, 정렬 변경 시 1페이지로 돌아간다. 종결 확인된 `case-company-874`만 우선 정렬하고 나머지 사건의 마감일은 만들지 않았다. 이 사건은 기존 최신순에서도 첫 카드이므로 두 정렬의 첫 페이지 내용은 같다.
- 공통 변경은 기존 기본값을 유지하는 `TopBar.logo`, `Pagination.neutralArrows` 옵션 및 광장 플로팅 챗봇 숨김뿐이다. 하단바 64px CTA·아이콘·간격은 광장에 한정한 CSS로 적용했다. 공통 토큰·홈 화면·라우트는 변경하지 않았다.
- 광장 전용 Paperlogy 600은 제작자 공식 배포본(https://freesentation.blog/paperlogyfont → Freesentation/paperlogy), Noto 400·Inter 500은 로컬 원본에서 웹폰트로 준비했다. Noto는 랭킹 안내문, Inter는 숫자만 서브셋했다. 원본 저작권과 OFL 전문은 `src/assets/plaza/fonts/`에 보관했다.
- 미연결: 검색, 전체 랭킹 목적지, 최다 투표자 전용 목록. 컨페티는 디자이너의 참고자료 대기 메모여서 추가하지 않았다. 실제 AI·서버 데이터는 연결하지 않았다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build` 통과. 격리 Chrome의 320/360/390/402/430/844/1366/1440px에서 가로 넘침·이미지 오류 없음. 카테고리별 4건, 전체 20건/5페이지 중복·누락 없음, 정렬/1페이지 복귀/새로고침, 사건 상세/뒤로가기, 키보드 랭킹 선택, 홈 로고·챗봇 유지 및 런타임 오류 없음 확인.
- 402px에서 시스템 상태바 62px를 제외한 좌표를 비교: 히어로 시작 66, 패널 293, 검색창 777, 첫 카드 889, 페이지네이션 1671px로 시안과 0.02px 이내 일치. 174px 카드와 좁은 화면/PC 상·하단 캡처를 시각 확인했다. 실기기 터치·키보드는 미검증이다.
- 빌드는 성공했지만 약 504kB JS 청크에 기존 500kB 기준 경고가 남는다. 새 원본 PNG는 총 약 3.9MB이므로 향후 에셋 최적화 대상이다. 검증용 의존성 설치는 lockfile을 유지했으며 npm 캐시 권한 문제는 `/tmp` 임시 캐시로 우회했다.

## 프로젝트 문서 전체 분석 (2026-09-13)

- 사용자 요청으로 저장소의 문서 5개를 전체 열람했다: `PROJECT_SPEC.md`, `PROJECT_CONTEXT.md`, `AGENTS.md`, `CLAUDE.md`, `src/assets/chatbot/README.md`. 의존성·빌드 산출물을 제외하고 숨김 경로도 검색했으며 다른 문서 파일은 발견되지 않았다.
- 서비스 목적·안전 원칙, A/B 퍼소나, 시연 흐름, 디자인 토큰, PC 402 × 874 목업/1024px 전환 기준, 상태 격리, mock 범위, 검증 기준과 §9 미확정 항목을 확인했다. 이번 범위는 로컬 문서 분석이며 Figma 원본을 새로 열람한 것은 아니다.
- 문서의 과거 상태표와 후속 구현 기록이 혼재한다. `AppRoutes.tsx` 대조 결과 챗봇, 후일담 홈·작성·미리보기·완료, MY·플랜, 사건 접수·상세·결과 경로가 존재한다. 과거의 챗봇/후일담 미착수 및 폴더 부재 설명은 현재 코드와 맞지 않는다. `/demo`와 후일담 상세는 경로 상수만 있고 등록된 Route는 없다.
- **로그인 시작 상태 불일치:** SPEC 마지막 변경 및 09-10 CONTEXT 기록은 최초 서아 데모 로그인을 명시하지만, 현재 `SessionProvider.tsx`의 `createInitialState`와 `switchPersona`는 `startStatusOf`를 사용해 서아를 비로그인, 지훈을 로그인으로 시작한다. 실제 코드 동작과 문서상 결정이 다르며 이번 분석에서 어느 쪽으로도 변경하지 않았다.
- **디자인 기준 불일치:** `CLAUDE.md`는 여전히 컨펌 페이지를 최우선으로 적지만 SPEC §0-3·§1-1은 개발 페이지 이관을 명시한다. 개별 화면의 후속 지정 기록도 있으므로 다음 구현 시 해당 화면의 기준을 확인해야 한다.
- **폰트/저장 상태 설명 지연:** `fonts.css`와 실제 에셋에는 RIDIBatang·Noto Sans KR·Cafe24를 포함한 폰트 등록이 있으며, `SessionProvider`는 A/B 활동 기록을 각각 저장한다. 폰트 미등록 및 퍼소나별 저장소 미사용이라는 과거 설명과 다르다.
- §9의 퍼소나 세부·공통 관점 선택·광장 후속 기능·토큰 불일치는 임의로 확정하지 않았다. 구현 파일의 존재를 디자인 승인이나 동작 검증 완료로 간주하지 않는다.
- 검증 범위: 문서 전체 열람, 파일 목록·라우트·세션·설정·폰트 코드 정적 대조. 앱 코드는 수정하지 않았고 타입 검사·린트·빌드·브라우저/실기기 테스트는 이번 분석에서 실행하지 않았다. 기존 통과 기록은 당시의 검증 이력이다.

## 계정별 왈봇 플랜 화면 (2026-09-11)

- 최신 기준은 개인 Figma 파일 `xzXF3SIGO4rcu8wIM9002Z`의 `789:8406`(플랜 선택), `791:9720`(결제 및 플랜 관리)이다. 이전 첨부 이미지로 만든 안내 화면을 이 두 시안으로 교체했다.
- `/my/plan`에서 로그인한 지훈은 관리 화면, 서아와 비로그인 사용자는 플랜 선택 화면을 표시한다. 서아 MY의 `왈봇 플랜 살펴보기` 버튼과 지훈 MY의 구독 카드 전체를 연결했다. 지훈 카드는 중첩 버튼 없는 Link로 키보드 Enter 이동도 지원한다.
- 서아: 원본 배경·캐릭터·선택 아이콘, 무료체험 제목, 기본 연간 39,000원(정가 49,000원·추천)과 월간 4,900원, 혜택 3개, 무료체험 CTA·다음에 할게요를 구현했다. 기존 화면의 결제수단 선택과 확인 카드는 제거했다. 기간은 네이티브 라디오로 선택하며 무료체험 CTA는 준비 중 안내만 표시한다.
- 지훈: 월간 플랜·다음 결제일 2026.09.25, 카카오페이, 결제 내역 2건(2026.08.25/2026.07.25), 연간 변경·구독 해지를 표시한다. 결제 정보는 시안의 데모 값이며 변경·해지는 준비 중 안내, 전체보기는 현재 2건을 모두 표시 중이라는 안내를 제공한다. 실제 체험 신청·결제·구독 변경은 연결하지 않았다.
- 공통 `CaseSubmitHeader`, `SectionTitle`, 기존 본문/52px CTA 스타일과 디자인 토큰을 재사용했다. 신규 Figma 원본은 `src/assets/my/plan/`에 저장했다. 페이지 안에서만 스크롤하며 하단 내비게이션·플로팅 챗봇은 숨긴다. 시스템 상태바·홈 인디케이터는 기존 기기 프레임이 담당한다.
- 뒤로가기는 앱 내 이전 화면, 직접 접근 시 MY로 복귀한다. 다음에 할게요도 MY로 이동한다. 계정·로그인 상태 변경 시 화면 상태를 초기화하며 MY의 위 24px/아래 16px 패딩과 계정별 활동 기록은 유지한다.
- 검증: 타입 검사 포함 build·lint 통과. 서아 버튼/지훈 카드 제목 클릭과 Enter 이동, 뒤로가기·다음에 할게요, 연간/월간 클릭·방향키 선택, 안내 메시지, 새로고침·계정 전환을 확인했다. 320·360·390·402·430px에서 가로 넘침과 이미지 오류가 없고, 320×640에서 내부 스크롤로 하단 혜택/관리에 접근한다. 실기기 및 실제 결제 검증은 미실시.

## 서아 마이페이지 최신 시안 반영 (2026-09-11)

- 사용자 지정 Figma `1913:12453` 기준으로 서아 MY를 수정했다. 미가입 플랜 카드의 배경·문구·판멍이·주황 버튼, 메뉴 높이·간격·아이콘, 흰색 전문가 정보 카드를 적용했다. 판멍이는 기존 `img1What.png`와 Figma 원본의 SHA-256이 같아 재사용하고, 변경된 아이콘 6종만 원본 SVG로 추가했다.
- 후속 사용자 요청으로 구독 플랜 카드의 위 패딩은 24px, 아래 패딩은 16px로 적용했다. 좁은 모바일 폭에서도 같은 값을 유지한다.
- 서아 활동은 최초 `0건 / 0건 / 0pt`로 시작한다. 기존 접수 완료 이벤트에서 접수 건수를, 축의금 첫 투표에서 배심 참여와 10pt를 반영한다. 포인트는 시안의 `1건 / 1건 / 10pt` 예시를 재현하는 데모 값이다. 같은 초안 재접수·동일 사건 재투표는 중복 집계하지 않는다.
- 기록은 SessionProvider에서 계정별 localStorage 키로 저장·복원한다. 지훈의 기존 `4건 / 24건 / 0pt`와 이용 중 플랜, 계정 전환 팝업 동작은 유지했다. MY 메인 뒤로가기 제거는 이전 사용자 결정대로 유지했다.
- 플랜 버튼은 후속 작업으로 `/my/plan`에 연결했다(위 계정별 플랜 화면 항목 참고). 전문가 정보 기록은 기존처럼 비활성이다. 실제 서버 접수·투표·포인트 지급 및 새 사건의 내 사건 목록 추가는 미연결이며 기존 별도 시연 목록을 유지한다.
- 검증: `npm run typecheck`, `npm run lint`, `npm run build` 통과. 브라우저에서 320·360·390·402·430·844·1366·1440px 가로 넘침·이미지 로드 확인, 402/320px 시각 대조, 플랜 안내·메뉴 접기·계정 전환·새로고침 확인. 실제 서아 접수 후 `1/0/0`, 첫 투표 후 `1/1/10`, 재접수·재투표 후 중복 없음 및 A→B→A 기록 분리/유지를 확인했다. 실제 휴대폰 터치·키보드는 미검증.

## 챗봇 플로팅 아이콘 3D 회전 (2026-09-11)
- Figma 개발 페이지 `2191:3560`(Frame 3465555, 56 × 56, 우측 16px·하단 내비 바로 위)의 챗봇 아이콘 자리에 판멍이 머리가 360도 회전하는 표현을 넣었다. 아이콘 슬롯과 챗봇 화면은 다른 담당자의 작업이라 `FloatingChatButton`의 내부 이미지 한 줄만 교체했다.
- 원본 `AIchat360.glb`는 136.9MB(메시 11개·정점 594만, 그중 mesh 1이 589만으로 99.2%)다. 56px 아이콘에 싣기에는 JS 번들(455kB)의 300배라 그대로 쓰지 않았다. three.js 실시간 렌더도 공용 `package.json`에 런타임 의존성이 생겨(§7-11) 채택하지 않았다.
- 대신 GLB를 미리 180프레임으로 구워 스프라이트 시트로 만들었다. `src/assets/chatbot/mascot-spin-sheet.webp` — 15열 × 12행, 프레임 128px, 1920 × 1536, 314kB. GLB 애니메이션 `Horizontal_360_12s`와 같은 12초 1바퀴(15fps)다.
- `SpinningMascot`이 CSS 애니메이션 두 개로 재생한다. 가로가 1초에 한 행(15프레임), 세로가 12초에 12행을 넘겨 12 × 1초 = 12초로 두 축이 어긋나지 않는다. 런타임 WebGL과 JS 타이머를 쓰지 않는다.
- `span`이 아니라 `i`로 그린다. `.floating-chat-button span`이 스크린리더 전용 숨김 규칙이라 span으로 두면 아이콘이 1px로 잘린다.
- `prefers-reduced-motion: reduce`에서는 회전을 멈추고 정면 한 컷만 보여준다.
- 새 라이브러리는 설치하지 않았다. JS 번들은 455.53 → 455.66kB로 사실상 그대로이고 시트 314kB만 늘었다.
- 검증: `npm run lint`·`npm run typecheck`·`npm run build` 통과. 브라우저에서 배경 위치가 열·행 모두 실제로 넘어가는 것과 375 × 812 모바일 렌더를 확인했다. 실기기는 미검증.
- 파란 원 배경·흰 테두리·바깥 그림자를 뺐다. 다른 담당자가 디자인 전에 임시로 만들어 둔 자리였고, Figma `2191:3560`을 픽셀로 확인하니 배경은 페이지 크림색 `#FEFAEE`뿐이고 머리만 51 × 29px로 놓여 있다. 연한 픽셀이 머리 영역(y 9~38)에만 있고 아래로는 하나도 없어 드롭섀도가 아니라 안티앨리어싱이다.
- 그래서 그림자는 되살리지 않았다. 원형 `box-shadow`는 회전하는 머리 실루엣과 어긋나기도 한다. 나중에 입체감이 필요하면 `filter: drop-shadow()`가 스프라이트의 알파를 따라가므로 프레임마다 모양이 맞는 그림자를 만들 수 있다.
- 마우스를 올리거나 누르면 위로 2px 뜨던 반응을 `scale(1.08)`로 바꿨다. 위치는 그대로 두고 얼굴만 56 → 60.5px로 커져서 챗봇 버튼 영역임을 알린다. `transform-origin`이 기본값(가운데)이라 아이콘 중심 좌표는 움직이지 않는다. 스프라이트 원본이 프레임당 128px라 60.5px로 키워도 해상도 여유가 있다.
- hover는 `@media (hover: hover)`로 감쌌다. 터치 기기에서는 탭한 뒤 hover가 남아 커진 채로 굳는다. 터치는 `:active`, 키보드는 `:focus-visible`이 같은 크기 변화를 맡는다.
- 버튼을 58 → 56px로, 머리를 48 → 56px로 키워 시안과 맞췄다. 이후 "작다"는 팀 피드백으로 여러 차례 키워 **80px**(시안 대비 +43%)로 확정했다. 시안(56)과 다른 값이라 확정되면 시안 쪽도 맞춰야 한다. → 확인 필요. 스프라이트 원본이 프레임당 128px라 80px(hover 시 86px)에서도 해상도 여유가 있어 시트는 다시 굽지 않았다. 90px을 넘기면 시트를 다시 구워야 한다. `border-radius: 50%`는 배경이 없어도 focus 링을 둥글게 유지하려고 남겼다. 위치(우측 16px, 하단 내비 + 16px)와 탭 영역은 건드리지 않았다. 시안은 내비에 딱 붙은 +0이라 16px 차이가 남아 있다. → 필요하면 조정
- **이 기록은 한 번 사라졌다가 복구했다.** main 병합(1c985ba)에서 PROJECT_CONTEXT.md 충돌이 이쪽 내용을 버리는 쪽으로 정리됐다. 코드와 에셋은 영향이 없었다. 같은 파일을 여러 명이 동시에 고치므로 병합 때 양쪽 섹션이 모두 남았는지 확인이 필요하다.

## 챗봇 화면 통합 (2026-09-11)

- origin/geonyoung의 df78c38 ChatbotPage를 main에 통합했다. 홈 AI 맞춤 추천 카드에서 /chatbot으로 진입하며, 초기 안내·선택지 대화·직접 입력·재시도·사건 접수/광장 이동을 mock 대화 스크립트로 제공한다.
- 챗봇은 실제 AI 또는 서버와 연결되지 않는다. 현재 시각과 최근 사건 여부는 데모 상태에 따라 표시된다.

마지막 업데이트: 2026-09-10

## 댓글 5개 단위 페이지 분리 (2026-09-10)

- 오늘의 사건 결과 댓글을 실제 배열 기준 페이지당 최대 5개로 분리. 새 댓글은 맨 앞에 추가하고 1페이지로 돌아가며 밀려난 댓글은 다음 페이지에서 표시한다. 5→6개 시 기존 마지막 `한번더생각`은 2페이지로 이동.
- 공통 Pagination 디자인은 유지하고 전체 페이지 수만 `ceil(댓글 수 / 5)`로 계산. 기존 표시용 댓글 수 124는 실제 댓글 수로 변경(초기 5개). 댓글 공감/반대 상태를 ID별 부모 상태로 옮겨 페이지 왕복/밀림에도 유지.
- build·lint 통과. 격리 Chromium 320·390·1440px에서 5/6/10/11개 경계, 페이지별 5/5/1개, 중복·누락 없음, 마지막 댓글 이동, 공감 유지, 2페이지에서 새 댓글 등록 후 1페이지 복귀, 공백 등록 방지 확인.
- 서버 저장은 기존대로 미연결. 페이지 이동 중에는 유지되지만 새로고침/결과 화면 이탈 시 새 댓글과 반응은 초기화된다.

## 사건 접수 하단 버튼 위치 통일 (2026-09-10)

- 사용자 요청으로 서아·지훈 접수 모든 단계의 하단 주황 CTA 위치 통일. 완료 화면만 helperText가 없어 footer가 약 27px 작아져 버튼이 내려가던 문제를 수정.
- 공통 footer를 52px 버튼 행 + 최소 15.4px 안내 행/12px 간격의 grid로 고정. 문구가 없어도 안내 공간을 확보하며 긴 문구는 잘리지 않게 확장 가능. 화면은 높이 100%/min-height 0/overflow hidden, 본문만 스크롤하고 footer는 flex:none 유지. 색상·라벨·클릭 동작은 변경하지 않음.
- build·lint 통과. 격리 Chromium 320·390·430·1440px에서 서아 4개/지훈 5개 화면의 버튼 높이 52px·하단 오프셋 일치·본문 스크롤 전후 버튼 이동 0px, 전체 접수/계정 전환 동작 재확인. 완료 화면 캡처 확인. 실제 휴대폰 키보드는 미검증.

## 최신 변경: 서아·지훈 사건 접수 분기 (2026-09-10)

- 최초 방문(저장 세션 없음)은 서아 데모 로그인. 이후 전환 계정과 명시적 로그아웃은 저장값 복원. 과거의 비로그인 시작 기록보다 이 변경이 우선한다.
- 서아 Figma `1446:10091` 및 하위 4개 프레임 `10093/10149/10178/10207`의 디자인·주석을 확인하고 기존 Submit 공통 화면에 적용. 같은 사건 접수 메뉴/URL에서 A는 3단계+완료, B는 기존 추가 질문 포함 4단계+완료로 동작.
- 서아는 동아리 축제 마감 사연, 전용 요약·AI 참고 의견, 광장 공개만 선택 가능. 지훈의 잔금 사연·추가 질문·나만 보기 전용은 유지. 공개 선택은 처음 미선택이며 완료 카드에 선택 범위 반영.
- 서아 입력창은 최초 포커스에서 예시를 채우고 이후 직접 편집 가능. 실제 글자 수를 표시하므로 시안의 고정 `135`와 다름. 서아 요약 수정 버튼은 시안의 숨김 상태 유지. 관계 버튼은 시안 메모대로 미선택에서 시작.
- 기존 원본 화살표·라디오·캐릭터를 재사용. `SeoaSubmit.css`로 서아 전용 64px 헤더·3분할 진행률·간격·258px 입력창·완료 화면 여백만 적용. 상태바/홈 인디케이터는 기존 목업이 담당. 지훈 디자인은 유지.
- MY 계정 전환 확정 후 세션이 바뀌면 접수도 해당 계정으로 전환. 흐름 Provider를 계정/인증 상태로 key 분리해 전환 시 작성 내용·첨부명·답변·선택·완료 상태를 초기화. 화면 이탈/새로고침 시에도 접수 초안은 초기화되며 저장 기능은 추가하지 않음.
- 요약/의견은 고정 예시이며 실제 AI·서버 접수·광장 게시·새 내 사건 목록 추가는 미연결. 기존 MY 사례 목록을 임의로 교체하지 않음.
- 검증: build(타입 검사 포함)·lint 통과. 격리 Chromium 320·390·430·1440px에서 양쪽 전체 접수 완료, MY 실제 계정 전환 양방향, 새로고침 계정 유지, 초안 혼입 없음, 직접 후속 URL 진입 복귀, 하단 CTA/가로 넘침/런타임 오류 없음 확인. 390px 서아 4개 화면 캡처로 시안 대조. 명시적 로그아웃 저장값 유지 확인. 실기기 키보드/터치는 미검증.

기준 문서는 `PROJECT_SPEC.md`다. 이 문서는 **현재 상태만** 기록한다.
규칙·토큰·구현 기준을 이 문서에 다시 적지 않는다.

## 검사 명령

```bash
npm run typecheck   # tsc -b --force
npm run lint        # eslint .
npm run build       # tsc -b && vite build
npm run dev
```

`tsconfig.json`이 프로젝트 참조(solution) 방식이라 `tsc --noEmit`은 아무것도 검사하지 않는다.
반드시 빌드 모드(`tsc -b`)를 쓴다.

## 결정 사항

| 날짜 | 결정 | 근거 |
| --- | --- | --- |
| 2026-09-08 | md 문서를 `PROJECT_SPEC.md` 하나로 통합 | 강사 제공 기준 초안 적용, 기존 6개 문서의 중복 제거 |
| 2026-09-08 | **TypeScript 전면 전환** | 강사 기준 "React + TypeScript + Vite", 팀 결정 |
| 2026-09-08 | React Router Declarative Mode 도입 | 라우터가 없던 상태 → 강사 기준의 기본안 채택 |
| 2026-09-08 | 스타일은 기존 일반 CSS 방식 유지 | 강사 기준 "스타일은 기존 프로젝트 방식을 유지한다" |
| 2026-09-08 | 402 × 874는 PC 목업 내부 크기로만 사용 | 강사 기준 "모바일에서는 402×874로 고정하지 않는다" |
| 2026-09-08 | **퍼소나 A=윤서아(신규 가입), B=곽지훈(기존 사용자), C 없음** | 팀 결정. 강사 초안은 3개를 전제하나 "실제 기획을 따른다" 단서 적용 |
| 2026-09-08 | 시스템 상태바·홈 인디케이터는 목업이 담당 | Figma `top_nav` 상단 62px = 상태바, `down_nav` 하단에 인디케이터 자리 확인 |
| 2026-09-08 | 페이지 폴더는 기존 대문자 표기 유지 | `Home/`, `Plaza/`, `Case/`, `Submit/`, `My/`가 이미 있음 |
| 2026-09-08 | **회원가입 화면(`/signup`)을 시안 없이 추가** | 팀 결정. 발표 시연에 필요. 확정 스타일가이드 토큰만 사용 |
| 2026-09-08 | 온보딩·별도 로그인 화면은 만들지 않음 | 시안도 없고 시연 흐름에도 없음 |
| 2026-09-08 | 곽지훈(B)은 가입 없이 계정 전환으로 진입 | 발표에서 윤서아 다음 순서라 로그인 시점이 필요 없음 |
| 2026-09-09 | **Figma `개발` 페이지에 올라온 화면만 구현 대상** | 팀 결정. 확정된 화면을 하나씩 옮겨 담기로 함 |
| 2026-09-09 | 아이콘은 `수정금지`의 **기타 아이콘 · nav · 화살표 · 꼬리화살표**만 사용 | 나머지는 참고용 모음이라 확정이 아님 (`PROJECT_SPEC.md` §9-17) |
| 2026-09-09 | 벡터 에셋은 Figma Plugin API `exportAsync`로 내보내 저장 | 프록시가 `figma.com` 직접 다운로드를 막아 에셋 URL을 받을 수 없음 |
| 2026-09-09 | 비트맵 에셋은 Figma에서 직접 내보내 폴더에 넣기 | 이그레스 정책이 `figma.com` 자산 URL을 막는다 |
| 2026-09-09 | 광장 에셋은 컴포넌트에서 직접 import | 자동 감지(`optionalImages.ts`) 없이 파일을 바로 넣는 편이 단순하다. 에셋 6종 반영 완료 |
| 2026-09-09 | **자료종합 페이지 전체를 기준 자료로 학습** | IA·유저플로우·설문·퍼소나·Font/Color가 모두 이 페이지에 있다. 아래 "자료종합 학습 기록" 참고 |
| 2026-09-09 | 미확정 항목을 본문에서 확정처럼 쓰지 않기 | §6의 관점 선택 3택 서술이 §9-12(미정)와 충돌했다. 본문은 §9를 가리키게만 두고 값은 §9에서 확정한다 |

## 구현 상태

| 화면 | 디자인 | 구현 | 비고 |
| --- | --- | --- | --- |
| 홈 | **확정** (`김하은/홈수정`) | **구현됨** | 에셋·폰트 교체 남음 |
| 404 | — | **구현됨** | `pages/Error/NotFoundPage.tsx` |
| 배심원 광장 | **확정** (`개발 > 광장`) | **구현됨** | 에셋 반영 완료. 정렬·검색·페이지네이션 등 **기능 구현만 남음** |
| 사건 접수 | 작업 중 | **구현됨** | `/cases/new` 5단계 접수 흐름과 하단 중앙 CTA 연결 |
| 사건 상세·투표·결과 | **축의금 로그인 전·후 투표 시안 구현** | **구현됨** | 공개 상세, 로그인 후 4택 투표, 결과·댓글 UI와 `/login` 임시 연결 포함 |
| 왈가왈후~ (후일담) | 작업 중 | 미착수 | 폴더 없음 |
| MY | **개발 시안 구현 기준** (`1883:3031`, `1863:13250`, `1863:13312`) | **구현됨** | MY → 내 사건 목록 → 결과 확인 흐름 연결 |
| 챗봇 | 미확정 | 미착수 | 화면 형태 미정 (`PROJECT_SPEC.md` §9-6) |
| 회원가입 `/signup` | **시안 없음** | **구현됨** | 팀 결정으로 추가. 더미값 readOnly + `더미텍스트 입력` 버튼 |
| 계정 전환 (시연 도구) | — | **구현됨** | `PersonaSwitcher` — PC 기기 바깥 패널 |
| 온보딩·별도 로그인 | — | 만들지 않음 | 시연 흐름에 없음 |

## 이번 작업에서 추가·변경한 파일

### 새로 만든 파일

```
tsconfig.json · tsconfig.app.json · tsconfig.node.json
vite.config.ts                          # vite.config.js 대체
src/vite-env.d.ts
src/main.tsx                            # main.jsx 대체, BrowserRouter 연결
src/App.tsx                             # App.jsx 대체
src/styles/tokens.css                   # 확정 스타일가이드 컬러·폰트·기기 크기
src/config/app.ts                       # 서비스·기기·배경·브레이크포인트·데모 설정
src/types/index.ts
src/routes/paths.ts · src/routes/AppRoutes.tsx
src/hooks/useIsDesktop.ts
src/components/device/DeviceFrame.tsx · DeviceFrame.css
src/components/device/StatusBar.tsx · HomeIndicator.tsx
src/components/device/AppViewport.tsx · AppViewport.css
src/components/common/BottomNavigation.tsx
src/layouts/ShowcaseLayout.tsx · ShowcaseLayout.css
src/layouts/MainLayout.tsx · MainLayout.css
src/pages/Home/HomePage.tsx             # Home.jsx 대체
src/pages/Error/NotFoundPage.tsx · NotFoundPage.css
src/data/common/homeContent.ts          # 홈 더미데이터 분리
src/data/personas/index.ts              # A 윤서아 · B 곽지훈 + 시연 계정
src/state/sessionContext.ts             # 세션 컨텍스트 타입
src/state/SessionProvider.tsx           # 퍼소나·로그인 상태, localStorage 복원
src/hooks/useSession.ts
src/layouts/AuthLayout.tsx · AuthLayout.css
src/pages/Auth/SignupPage.tsx · SignupPage.css   # 시안 없이 추가한 데모 화면
src/components/demo/PersonaSwitcher.tsx · PersonaSwitcher.css
```

### 고친 파일

- `package.json` — `typescript`, `typescript-eslint`, `react-router-dom` 추가.
  `typecheck` 스크립트 추가, `build`가 타입 검사를 먼저 실행
- `eslint.config.js` — TypeScript 파서 연결, 대상을 `**/*.{ts,tsx}`로 변경
- `index.html` — `/src/main.tsx`, `viewport-fit=cover`, `lang="ko"`, 타이틀
- `src/index.css` — 전역 초기화 확장 (`100dvh`, 폰트 변수)
- `src/pages/Home/Home.css` — `:root` 토큰과 전역 리셋을 분리해 내보냄,
  `.home-screen`의 402px·100vh 고정 제거, `.home-header` 66px로 조정,
  `.status-bar`·`.island`·`.bottom-nav`·`@media (min-width: 600px)` 제거,
  `.all-cases`를 링크로 쓸 수 있게 `display: block` + `text-align: center` 추가
- `src/pages/Home/HomePage.tsx` — `로그인 하고 사건 투표하기`를 `/signup` 링크로 연결
- `src/App.tsx` — `SessionProvider` 연결
- `src/routes/` — `/signup` 경로와 `AuthLayout` 추가
- `src/layouts/ShowcaseLayout.tsx` — 기기 바깥 패널에 `PersonaSwitcher` 배치

**레이아웃 수정 (2026-09-08 오후)**

- `src/pages/Home/Home.css` — `.home-screen`의 `overflow: hidden`을 `overflow-x: clip`으로.
  `hidden`은 스크롤 컨테이너를 만들어 `.home-header`의 `position: sticky`를 무력화했다
- `src/components/device/DeviceFrame.tsx` · `.css` — `.device-frame-fit` 래퍼 추가.
  `transform: scale()`이 레이아웃 자리를 줄이지 않아 기기 아래 빈 공간이 생기던 문제 수정.
  프레임 폭을 `화면 + 24`에서 `화면 + 30`으로 정정 (검은 베젤 3px × 2 누락이라
  화면이 베젤을 밀고 나가 오른쪽 테두리만 얇았다)
- `src/config/app.ts` — `DEVICE_FRAME` 추가. 프레임·베젤 두께를 상수로 분리
- `src/layouts/ShowcaseLayout.tsx` · `.css` — 절대 위치 패널을 걷어내고
  좌 패널 · 기기 · 우 패널의 3단 flex로. 양쪽 폭이 같아 기기가 항상 가로 중앙에 온다.
  축소 비율을 `(창 높이 − 여백) / 기기 높이`로 정정.
  `화면에 맞추기`에서는 페이지 스크롤을 막아 가로 중앙이 밀리지 않게 했다
- `src/layouts/MainLayout.css` · `AuthLayout.css` — 기기 안 데스크톱 스크롤바 숨김
  (스크롤 동작은 유지)
- `src/state/SessionProvider.tsx` — 복원을 effect에서 첫 렌더 초기값으로 이동.
  저장은 effect로 분리 (린트 `react-hooks/set-state-in-effect`)
- `src/layouts/ShowcaseLayout.tsx` — 축소 비율을 state에서 빼고 렌더 중 계산 (같은 린트 규칙)

**배심원 광장 (2026-09-09)**

새로 만든 파일

```
src/pages/Plaza/PlazaPage.tsx
src/pages/Plaza/Plaza.css
src/pages/Plaza/components/RankingHeroSection.tsx
src/pages/Plaza/components/CaseFeedSection.tsx
src/data/common/plazaContent.ts        # 랭킹·카테고리·사건 목록 더미데이터
src/components/common/TopBar.tsx · TopBar.css
src/assets/icons/                      # 17개 (nav 5, 기타 아이콘, 화살표, 꼬리화살표)
src/assets/plaza/rank-1.svg            # 왈가닥이
src/assets/plaza/rank-2.svg            # 왈랑이
```

고친 파일

- `src/pages/Home/HomePage.tsx` — 화면 안에 있던 앱 헤더를 공통 `TopBar`로 올림
  (§7-2 "두 번째 사용이 확인되면 공통 폴더로" 규칙 적용)
- `src/components/common/BottomNavigation.tsx` — 문자 아이콘을 실제 SVG로 교체.
  아이콘 경로를 `--nav-icon` CSS 변수로 넘긴다. 인라인 `mask-image`를 쓰면
  중앙 CTA의 원형 배경까지 함께 잘려 나간다. `배심원 광장`을 `enabled: true`로 전환
- `src/layouts/MainLayout.css` — `.bottom-nav__icon`과 CTA `::after`가
  `var(--nav-icon)`을 마스크로 쓰도록 정정 (`mask-image: inherit`은 동작하지 않았다)
- `src/routes/AppRoutes.tsx` — `/plaza` 라우트 추가
- `src/routes/paths.ts` — `plaza` 경로 상수
- `src/types/index.ts` — `JurorRank`, `PlazaSortKey` 추가

### 삭제 정리

**TypeScript 전환으로 대체된 파일 — 2026-09-08 삭제 완료 (폴더에서 확인함)**

`vite.config.js` · `src/main.jsx` · `src/App.jsx` · `src/App.css` · `src/pages/Home/Home.jsx`

**통합 문서 4개 — 2026-09-09 삭제 완료**

`PRD.md` · `design-analysis.md` · `project_rules.md` · `SKILL.md`

2026-09-08 기록에는 이미 삭제했다고 적혀 있었지만, 09-09에 확인해 보니
안내표만 남은 껍데기 파일 네 개가 그대로 있었다. 같은 날 실제로 지웠다.

이제 문서는 네 개다 — `PROJECT_SPEC.md`(기준) · `PROJECT_CONTEXT.md`(상태) ·
`CLAUDE.md` / `AGENTS.md`(진입점).

## 홈 화면 잔여 항목

- 상단 헤더 sticky, 기기 테두리 좌우 두께, 기기 안 스크롤바는 2026-09-08에 수정 완료.
- 아이콘·일러스트가 아직 문자 표시(`●ᴗ●`, `⌂`, `♟`, `⌕`)다.
  `src/assets/home/figma/`의 실제 SVG·PNG로 교체해야 한다.
- Paperlogy·Pretendard 폰트 파일이 `src/assets/fonts/`에 있으나
  `@font-face` 등록이 아직 없다.
- 하단바 아이콘은 2026-09-09에 실제 SVG로 교체 완료.
  배심원 광장과 MY를 활성화했다. 사건 접수·왈가왈후~는 아직 라우트가 없어 비활성이다.
  각 화면이 생기면 `BottomNavigation.tsx`의 `enabled`만 켜면 된다.
- 앱 헤더는 2026-09-09에 공통 `TopBar`로 올렸다.

## 배심원 광장 잔여 항목

**에셋은 끝났다 (2026-09-09).** `src/assets/plaza/`에 6종이 들어와 있고
`RankingHeroSection.tsx`가 직접 import 한다.

```
hero-bg.webp · hero-mascot.png · hero-trophy.png
rank-1.png · rank-2.png · rank-3.png
```

이전 기록에 있던 `hero.png` / `hero-bg.png` 3종 규격과
`src/assets/plaza/optionalImages.ts`(폴더를 훑어 자동 반영) 방식은 **더 이상 쓰지 않는다.**
그 파일은 만들지 않았고, 1·2위 마스코트도 SVG가 아니라 PNG로 교체됐다.

남은 것은 **기능 구현뿐이다.**

- 정렬·검색·페이지네이션은 동작 후 화면이 시안에 없어 비활성으로 두었다.
  카테고리 칩만 실제로 목록을 거른다. (§9-14)
- 랭킹 탭은 선택 상태만 로컬로 표현한다. 탭별 목록은 시안에 없다. (§9-13)
- `랭킹 보러가기`는 이동할 화면이 없어 링크를 걸지 않았다. (§9-15)

## 자료종합 학습 기록 (2026-09-09)

Figma `자료종합` 페이지(노드 `1264:12056`)의 최상위 16개 항목을 모두 읽었다.
상세 정리는 별도 노트에 있고, 여기에는 **기준 문서에 영향을 준 것만** 남긴다.

읽은 것

| 자료 | 노드 | 얻은 것 |
| --- | --- | --- |
| 유저플로우 1, 2 | `1264:12058` | 서아·지훈의 화면 단위 플로우, 1·2순위 퍼소나 보드 |
| 전제구조ia | `1264:12421` | 홈·광장·사건 상세·접수·후일담·MY·챗봇·FAQ 전체 IA |
| 결과요약 · 설문결과 | `1264:16599` · `1264:16611` | 설문 Q1~Q21 전체와 해석 주의사항 |
| 서아 · 지훈 | `1264:14326` · `1264:14357` | 퍼소나 나이·직업·유형·사건 예시·필요한 도움 |
| Font · Color · Corner_Radius | `1264:16617` · `16688` · `16878` | Headline 1~23 램프, 컬러 스케일, 임시 반경 |
| 왈 패밀리 · 컴포넌트_신상 | `1264:16913` · `1264:16932` | 캐릭터 3종 역할, `radio`/`RadioList`/`PanMungyee` 컴포넌트 |

새로 확인된 것

- **사건 상세의 화면 순서**가 IA에 정의되어 있다.
  `사건 본문 → AI가 정리한 핵심 쟁점 → 당신의 판단은?(투표) → AI와 배심원의 판단은? → 댓글`
- **투표 전에는 AI·배심원 판단을 보여주지 않는다**는 설계 이유가 명시돼 있다.
  "의견을 따라가는 편향이 생길 수 있으므로 투표 후에 보여준다."
- 결과 화면에는 배심원 최다 선택 비율, AI 최종 판결, **판단 확신도**(예: 68% · 73%),
  AI와 배심원의 일치/불일치 문구가 함께 들어간다.
- 챗봇은 추천 질문 칩 5종과 각 응답·이동 버튼까지 정의돼 있다.
- 사건 진행 상태는 5단계다.
  `사건 접수 완료 → AI 1심 완료 → 커뮤니티 공개 완료 → 배심원 투표 완료 → 결과 확인 가능`
- 계정 전환 경로는 `내정보 → 프로필 → 계정 전환`이다.

## 발표 시연 흐름

**윤서아 (퍼소나 A — 신규 사용자)**

```
비로그인 진입 → 홈 → 오늘 사건 `투표하러 가기`
→ 사건 상세 투표 화면 → `로그인하고 나도 투표하기`
→ /signup → `더미텍스트 입력` → `가입하고 투표하기` → 원래 화면 복귀
```

사건 상세 화면이 아직 없어서, 지금은 홈의 `로그인 하고 사건 투표하기` 버튼이
`/signup`으로 가는 임시 진입점이다.

**곽지훈 (퍼소나 B — 기존 사용자)**

가입 흐름 없음. PC 기기 바깥 패널의 계정 전환 버튼으로 윤서아 → 곽지훈으로 바꾼다.

## 서버 없이 표현한 데이터·상태

- 홈의 사건 카드·투표 수치·참여 인원은 모두 예시 데이터다 (`src/data/common/homeContent.ts`).
- 광장의 랭킹·포인트·사건 목록·페이지 수도 예시 데이터다 (`src/data/common/plazaContent.ts`).
  실제 집계가 아니며 카테고리 필터만 화면 로컬 상태로 동작한다.
- 밸런스 게임 선택은 화면 로컬 상태이며 저장되지 않는다.
- AI 판단은 실제 모델 결과가 아닌 UI 프로토타입이다.
- 로그인·알림·저장 API는 연결되어 있지 않다.
- 퍼소나별 저장소(`walgawalbot:demo:v1:*`)는 아직 사용하지 않는다.

## 다음 작업

### 2026-09-10 댓글 페이지네이션 공통 컴포넌트

- Figma `1846:14647` 기준으로 `components/common/Pagination.tsx`와 전용 CSS를 추가하고 오늘의 사건 결과 댓글 하단에 적용. 다른 화면의 페이지네이션은 변경하지 않음.
- 원본 이전·다음 SVG를 로컬 에셋으로 보관. 다음 아이콘은 시안의 좌우 반전을 반영. 숫자 26px·화살표 24px, 숫자 간격 10px·그룹 간격 18px, 주황 선택 상태·테두리·그림자를 고정하며 작은 화면에서도 축소하지 않음. 화면에 떠 있는 fixed 배치가 아닌 디자인 규격의 공통화.
- `currentPage`, `totalPages`, `onPageChange`, `ariaLabel`로 재사용. 최대 5개씩 번호 표시, 처음/마지막 이동 제한 및 키보드 포커스 지원. 댓글은 기존 데모처럼 선택 표시만 바뀌며 실제 페이지별 데이터 연동은 미구현.
- Figma 숫자 폰트 Gmarket Sans 원본은 프로젝트에 없어 기존 Pretendard로 대체. 원본 확보 시 공통 컴포넌트의 폰트 선언으로 연결 가능.
- 검증: build(타입 검사 포함)·lint 통과. 격리 Chromium 320·360·390·430·1440px에서 규격/간격/넘침, 번호 선택·이전/다음·끝 경계·키보드 Enter 동작 및 런타임 오류 없음 확인. 390px 캡처로 원본 화살표 방향·색상 확인.

### 2026-09-10 전체 결과 기본 펼침

- 사용자 요청으로 오늘의 사건 결과의 `전체 결과 보기` 초기 상태를 펼침으로 변경. 기존 접기/펼치기 애니메이션과 접근성 상태는 유지.
- 초기 진입·새로고침 시 펼침, 클릭 시 접기/다시 펼치기 확인. build·lint 통과.

### 2026-09-10 MY 메뉴 스타일 통일

- 사용자 요청으로 나의 활동·설정·전문가 정보 기록의 상태별 스타일 통일. 원본 단색 SVG를 CSS mask로 재사용하여 비활성 아이콘을 #939094, 원 배경을 neutral-250으로 일치. 활성 내 사건은 주황 아이콘/흰 배경/테두리 유지.
- 설정과 전문가 카드의 글자·아이콘·화살표 비활성 톤, 좌우 16px 정렬, 글자 굵기·행 간격 일치. 전문가 정보 기록은 기존 onClick/목적지가 없는 버튼이므로 disabled 의미와 시각 상태도 통일. 새 기능/목적지는 추가하지 않음.
- 접힌 섹션 하단 이중 선 제거, 키보드 포커스 표시 보완. 프로필·계정 전환·포인트·플랜은 그대로 유지.
- build·lint 통과. 격리 Chromium 320·390·1440px에서 비활성 아이콘 7개 동일 색/배경/원본 마스크, 활성 아이콘 색, 가로 넘침 없음, 접기/펼치기와 내 사건 이동 검증. 설정/전문가 카드 캡처 확인.

### 2026-09-10 프로필 전환 확인 팝업 축소

- 사용자의 크기 조정 요청에 따라 중앙 확인 팝업만 축소. 폭 354→312px, 내부 패딩 20px, 프로필 상하 여백 50→20/24px, 사진 100→72px. 제목/이름 18px, 질문 16px, 이메일 12px로 조정. 확인 버튼 높이 52→44px·글자 16→14px, 로그아웃 13px. 닫기 아이콘은 그대로 두고 클릭 영역 32px 확보.
- 양쪽 계정에서 실측 312×358px, 320px 화면에서는 폭 272px로 축소. 모션·배경 딤·확정/취소 동작 및 하단 계정 선택 시트는 유지.
- PC 1440×900·모바일 390×844/320×740에서 크기/가로 넘침·사진/버튼 크기·계정 전환 양방향·빠른 취소/포커스 복원 및 reduced-motion 검사, 캡처 확인. build·lint 통과.

### 2026-09-10 MY 포인트 표시

- Figma `1913:12480`·`1913:13356`에 맞춰 판결 신뢰도 98%를 포인트로 교체. 서아 10pt, 지훈 0pt를 DEMO_ACCOUNTS의 points로 관리. 숫자는 주황 20px Bold, 단위는 회색 14px Regular로 중앙 정렬. 나머지 통계는 유지.
- 실제 포인트 적립/차감은 미연결이며 시안의 데모 값만 표시. 양방향 계정 전환·새로고침 후 값 유지/색상/텍스트 및 캡처 확인. build·lint 통과.

### 2026-09-10 계정 전환 팝업 모션

- 하단 계정 전환 시트는 360ms ease-out 곡선으로 아래에서 진입, 배경 딤은 240ms로 서서히 표시. 중앙 확인 팝업은 220ms의 작은 이동/확대와 페이드로 연결. 정지 상태 디자인은 유지.
- X·배경·Esc 취소 및 확인/로그아웃은 240ms 퇴장 후 처리. 입장 도중 닫아도 현재 transform/opacity에서 이어지도록 Web Animations API 사용. 중복 종료 방지, 퇴장 중 입력 차단 및 배경 잠금 유지, 언마운트 시 애니메이션 취소. 별도 라이브러리 없음.
- reduced-motion 설정에서는 이동/페이드 없이 즉시 표시·종료. 양쪽 계정 전환/토스트·빠른 Esc/중복 Esc·X 닫기·포커스 복원/배경 잠금, PC 1440×900·모바일 390×844/320×740 및 reduced-motion 모의 검증. build·lint 통과.

### 2026-09-10 계정 전환 현재 사용중 표시

- Figma `1913:13062`의 `현재 사용중` 표시를 양쪽 계정 팝업에 적용: 회색 #F0EEF1 배경, 50px 반경, 상하 6px/좌우 12px 패딩, 12px Medium/줄높이 1. 작은 화면에서도 동일한 알약 모양 유지.
- 전환·확인·로그아웃 버튼과 기능은 변경하지 않음. 두 계정 × 320·390·402·1440px 스타일/가로 넘침·실제 계정 전환 테스트 및 캡처 확인, build·lint 통과.

### 2026-09-10 계정별 내 사건 목록·필터

- 사용자가 링크 배정 반대를 확인: 서아(A)는 조별 과제/Figma `1913:12839`, 지훈(B)은 잔금 미지급/Figma `1922:14963`로 구현.
- `src/data/personas/myCases.ts`에 프로필별 목록 데이터를 분리하고 기존 세션의 personaId로 선택. 계정 변경 시 목록 필터도 전체로 초기화.
- 서아는 전체 1/투표 완료 1, 지훈은 전체 1/비공개 1을 활성화. 선택한 버튼만 파란색, 선택하지 않은 활성 버튼은 흰색. 0건 또는 해당 없는 필터는 회색 비활성. 전체와 해당 상태 필터 모두 같은 계정 사건을 표시.
- 시안의 직장/나만 보기 배지, 잔금 사건 제목, AI 1심(나만 보기), 참여 없음, 사건번호 및 목록 간격·안내 텍스트 색상 반영. 목록도 헤더 아래 독립 스크롤로 낮은 화면에서 하단 접근 가능.
- 서아 사건번호를 CASE-FRIEND-01로 갱신하고 결과 링크·상세 표시도 맞춤. 기존 CASE-SEOA-01 직접 링크는 호환 유지. 지훈 CASE-COMPANY-01 결과 상세 시안은 제공되지 않아 결과 버튼 비활성 유지(서아 결과로 연결하지 않음).
- 검증: build·lint 통과. 격리 Chromium에서 두 계정 × PC 1440×900/1366×768·모바일 320/360/390/402/430·가로 844×390, 필터 선택/색상/건수, 동일 사건 유지, 하단 안내 접근, 가로 넘침 없음, 서아 결과 연결 확인. 실제 MY 계정 전환 양방향→내 사건 진입·새로고침 유지 및 이전 결과 URL 호환 확인. 화면 캡처로 두 시안 대조. 실제 휴대폰 미검증.

### 2026-09-10 내 사건 결과 상세 스크롤·최신 디자인

- Figma `1913:12900` 기준으로 내 사건 결과 상세를 갱신. 높이 제한 없는 페이지가 AppViewport에서 잘리던 문제를 해결: 결과 페이지를 화면 높이의 flex 컨테이너로 만들고 헤더 아래 본문에만 `flex: 1; min-height: 0; overflow-y: auto` 적용. 헤더는 스크롤 밖에서 고정, 모바일 하단 안전 영역 확보.
- 최신 판멍이 원본 이미지를 `panmung-curiosity.png`로 추가. 판별이 이미지는 Figma 다운로드와 기존 파일의 SHA-256이 같아 기존 `verdict-difference.png` 재사용. 캐릭터 크기/위치, 46% 강조 26px, 집계 항목 14px 및 막대별 주황 단계, 안내 카드 테두리/반경, 하단 문구 간격 반영.
- 기존 헤더 64px·상태바 경계 비침 방지 유지. Figma 헤더 위 2px 오프셋은 사용자의 앞선 간격 제거 요청에 따라 재도입하지 않음. Gmarket Sans 숫자 폰트는 보유 소스가 없어 기존 Pretendard 굵은 글꼴 유지.
- 검증: 격리 Chromium에서 PC 1440×900·1366×768 및 모바일 320·360·374·390·402·430px, 가로 844×390 확인. 실제 휠 입력·키보드 Ctrl+End·CDP 터치 스와이프로 내부 스크롤, 마지막 문구 노출, 헤더 고정, 가로/바깥 스크롤 없음, 이미지 로드, 목록 복귀 후 재진입 상단 시작 확인. 전체/상단/하단 캡처로 시안 대조. build·lint 통과. 실기기 테스트는 미실시.
- 배심원 의견 전체 보기·후일담 작성하기는 목적지 화면 미구현으로 기존 비활성 유지. 내 사건 목록의 별도 스크롤 구조는 이번 결과 상세 범위에서 변경하지 않음.

### 2026-09-10 MY·오늘의 사건 상단 정렬

- 후속 수정: 좌표상 간격이 0이어도 PC 목업의 소수점 축소 배율에서 sticky 헤더 위로 본문이 1px 비치는 현상을 재현. 앞선 경계 좌표 검사만으로는 이 렌더링 문제를 찾지 못했음.
- `AppViewport.css`에서 상태바/모바일 안전 영역을 불투명한 흰 배경으로 덮고 헤더 쪽으로 2px 겹쳐 경계 비침 차단. 공통 상단 인셋 변수로 실제 여백과 덮개 높이를 함께 계산. 헤더·본문 좌표/높이, 상태바 아이콘, 네비, 팝업 레이어 순서는 유지.
- 후속 검증: 격리 Chromium에서 MY 메인·내 사건 목록·내 사건 결과·오늘의 사건 상세·결과 5개 경로 × PC/모바일 7개 화면·DPR 조건 × 5개 스크롤 요청(175개) 경계 픽셀 검사 통과. 본문에 고대비 테스트 배경을 넣어도 경계 비침 0건. MY 하위 페이지는 기존 스크롤 미구현 상태여서 정적 경계만 검사. 실제 화면 캡처 확인, 계정 팝업 상단 딤/닫기·모바일 47px 안전 영역 모의 검사 통과. build·lint 통과. 실제 휴대폰은 미검증.
- MY 메인·내 사건 목록·내 사건 결과의 헤더를 오늘의 사건 상세/결과와 같은 64px 높이로 통일(이전 MY 66px). MY 페이지 범위의 변수만 조정하고 공통 상태바 62px 및 홈 등 다른 페이지는 유지.
- PC 5개 경로에서 상태바 하단과 헤더 상단의 경계 차이 0px 확인. 기존에도 경계 여백은 없었으며 이번 변경은 헤더 내부 제목 높이 차이만 정리한 것. build·lint 통과.
- 별도 발견 당시: MY 하위 결과에 내부 스크롤 컨테이너가 없어 긴 내용을 휠로 스크롤할 수 없었음. 위의 후속 내 사건 결과 상세 작업에서 수정 완료.

### 2026-09-10 익명 프로필·상세 인터랙션 보완

- 카운트다운은 직전 복구 상태 그대로 유지. 요약 숫자 연결선만 각 행의 중앙 사이로 연결해 첫 숫자 위·마지막 숫자 아래 돌출 제거.
- `전체 결과 보기`는 목록을 유지한 채 CSS grid 높이와 투명도를 300ms/200ms로 전환. 빠르게 다시 눌러도 자연스럽게 역전하며, 닫힌 내용은 aria-hidden/inert 처리. reduced-motion에서는 즉시 전환.
- Figma `1913:12458` 기준으로 MY 실명 옆에 작은 익명 닉네임을 추가. 실제 인물 프로필 사진·계정 전환의 실명은 유지.
- 공개 닉네임: A 윤서아=`익명의 왈가닥`, B 곽지훈=`익명의 왈랑이`. `DEMO_ACCOUNTS`→세션→MY/댓글에서 같은 데이터 사용(데모 가입 닉네임에도 반영).
- 댓글 캐릭터는 보유 원본 소스 재사용: 서아는 분홍 사각 캐릭터(`comment-avatar-2.png`), 지훈은 초록 원형 캐릭터(`comment-avatar-3.png`). 새 댓글 생성 시 작성자 닉네임·이미지를 함께 기록.
- 검증: build·lint 통과. 격리 Chromium에서 실제 MY 계정 전환 후 양쪽 닉네임·댓글/이미지 확인, 320·360·390·402·430px MY 가로 넘침·계정 전환 버튼 겹침 없음 확인. 아코디언 열기/닫기 중간 높이·빠른 역전·reduced-motion 및 요약 연결선 확인. 댓글은 기존 시연용 로컬 상태로 새로고침 시 초기화.

### 2026-09-10 축의금 상세·결과 최신 시안 반영

- 사용자 지정 Figma `1802:5912`·`1846:14556` 기준으로 홈의 축의금 CTA 이후 화면을 수정. 홈 자체와 공통 네비 디자인은 유지.
- 상세: 작성자 이미지, 네 가지 투표 캐릭터 원본 SVG, 본문 좌우 여백, 선택지 간격·CTA 간격 반영. 로그인 전 안내와 임시 로그인 후 복귀 유지.
- 결과: 새 판결 일러스트에 실제 텍스트로 한 줄 판결 표시, 우측 마감 배지, 기본 접힘 집계 카드, 연한 파랑 AI 판결 카드, 댓글 헤더·프로필·후일담 스타일 적용.
- Figma 메모대로 공감/반대는 처음 회색이며 선택한 반응만 색을 표시. 기존 댓글 입력·등록·반응 시연 동작 유지.
- 상세·결과 진입 시 실제 내부 스크롤 컨테이너를 맨 위로 이동.
- 카운트다운은 각 프레임의 예시 시간에서 시작해 0까지 감소하는 표시용 데모. 두 시안의 시작 시간이 서로 달라 화면별로 초기화하며 실제 마감 판정/서버 동기화는 미연결.
- 기존 댓글 저장·정렬·페이지별 데이터·대댓글·후일담 이동의 미구현 범위는 변경하지 않음. 댓글/반응은 새로고침 시 초기화.
- RIDIBatang 원본 폰트가 없어 후일담은 serif 대체 폰트 사용. AI 제목은 상대방 지지, 근거는 글쓴이 지지로 읽히는 시안 카피 불일치를 임의로 바꾸지 않고 유지(디자인 확인 필요).
- 검증: 타입 검사 포함 build 및 lint 통과. 격리 Chromium에서 홈→상세→로그인→선택→결과→댓글 등록, 미선택 안내, 결과 접기/펼치기, 반응 전환 및 320·360·390·430px/PC 가로 넘침·에셋 로드를 확인. 실제 휴대폰 키보드는 미검증.

### 2026-09-10 계정 전환 팝업

- MY 메인 화면의 상단 뒤로가기 화살표를 모든 계정에서 제거하고 제목 중앙 정렬을 유지. 내 사건 목록·결과의 뒤로가기는 유지.

- 사용자 지정 Figma `1913:13042` 기준으로 MY 계정 전환을 하단 팝업으로 구현.
- `app-overlay-root`를 재사용하여 목업 내부 전체를 어둡게 덮고, 배경 입력·스크롤을 차단.
- 현재 계정과 전환 가능 계정을 사진·이메일로 표시. 선택 아이콘은 Figma `1446:10062` 원본 SVG를 사용.
- `프로필 전환하기` → 대상별 중앙 확인 팝업(Figma `1913:13090` 곽지훈 / `1913:13110` 윤서아) → `확인` 순서로 동작. 마지막 확인에서만 기존 세션의 `switchPersona`를 호출.
- 전환 완료 후 `○○○ 프로필로 전환되었습니다` 안내를 3.5초간 표시. 중앙 팝업 취소 시 계정 유지, 로그아웃은 기존 세션 종료 후 홈 이동.
- 닫기·배경 클릭·Esc는 변경 없이 취소. 키보드 포커스 제한 및 닫은 뒤 버튼 포커스 복원.
- MY 사진도 세션에 맞춰 변경. 기존 세션 저장 방식으로 계정 전환 유지.
- 임시 저장 사건·프로필별 설정 저장 기능은 아직 미구현이며 팝업 안내 문구는 Figma 카피를 반영한 것.
- 검증: lint 및 타입 검사 포함 build 통과. 격리된 브라우저에서 선택/취소/확정/역방향 전환/새로고침/사진/포커스 복원/320·360·390·430px 가로 넘침/PC 오버레이 경계를 확인. 런타임 오류 없음.
- 중앙 확인 팝업 추가 검증: 양방향 대상 표시·확인 전 계정 유지·완료 안내·취소·로그아웃 및 모바일/PC 중앙 배치 확인. 타입 검사 포함 build와 lint 통과.

1. 모바일 폭(360·390·430)과 키보드가 열린 상태를 추가 확인
2. 광장 기능 구현 — 정렬·검색·페이지네이션·랭킹 탭은 §9-13~15 확인 후 진행
3. 실제 로그인 화면 시안이 확정되면 `/login`의 임시 연결 UI 교체
4. 축의금 결과 화면의 실제 API가 정해지면 투표·댓글·공감 상태를 서버 데이터로 교체
5. `개발` 페이지에 화면이 올라오는 순서대로 구현하고,
   완성되면 `BottomNavigation`의 `enabled`와 `AppRoutes`에 연결

## 마지막 검증 결과

2026-09-10 MY·내 사건 목록·내 사건 결과 연결 당시 실행 결과 (main 통합 검증은 문서 마지막 기록 참고).

| 검사 | 결과 | 비고 |
| --- | --- | --- |
| `npm install` | **통과** | 163 packages, 0 vulnerabilities |
| `npm run typecheck` | **통과** | `tsc -b --force`, 타입 오류 0건 |
| `npm run lint` | **통과** | ESLint 오류 0건 |
| `npm run build` | **통과** | 135 modules, 210ms |
| MY 라우팅 | **구현됨** | `/my` → `/my/cases` → `/my/cases/CASE-SEOA-01` 연결 |

빌드 산출물

| 파일 | 크기 | gzip |
| --- | --- | --- |
| `index.js` | 327.98 kB | 99.91 kB |
| `index.css` | 50.03 kB | 9.85 kB |
| `vote-result.png` | 1,766.33 kB | — |
| `perilla-table.png` | **2,127.51 kB** | — |

`perilla-table.png` 하나가 번들 전체보다 9배 크다.
`src/assets/home/`의 다른 이미지도 1~3.8 MB대라, 실제 에셋을 붙이기 전에
리사이즈나 WebP 변환을 검토해야 한다.

## 알려진 문제 / 확인 필요

`PROJECT_SPEC.md` §9에 정리되어 있다.
해결된 것은 §9-2(가입 화면), §9-4(하단바 표기), §9-5(홈 인디케이터),
§9-16(광장 비트맵 에셋), §9-17(아이콘 확정 범위)이다.

자료종합 학습에서 새로 올라온 것은 §9-18(기준 페이지 이관 범위),
§9-19(컬러 값 불일치 4건), §9-20(반경 토큰화 여부)이고,
§9-1(퍼소나 세부)과 §9-12(관점 선택)는 자료종합에서 근거를 찾아 내용을 보강했다.

가장 급한 것은 **§9-3 사건 상세 시안**이다.
시연 흐름의 가입 진입점이 사건 상세에 있어서, 그 화면이 나와야 흐름이 제자리를 찾는다.
그다음이 **§9-12 관점 선택**이다. 자료종합은 4택으로 일관되지만 문구가 세 가지로 갈려 있어
사건 상세를 만들기 전에 정해야 한다.

## 밸런스 게임 후속 구현 (2026-09-09)

- 현재 기준 Figma 노드: `1692:12930` (기존 `1402:7202`는 없어짐).
- 깻잎 + 사용자 승인 데모 문항 3개(새우 껍질·패딩 지퍼·차량 블루투스). 주제 참고: https://www.nocutnews.co.kr/news/5788798
- 새로고침 아이콘으로 1→2→3→4→1 순환. 문항별 선택은 로컬 상태로 유지하고 새로고침·퍼소나 전환 시 초기화.
- 좌우 버튼 및 Pointer Events 드래그 선택. PC 목업 배율 보정, 세로 스크롤·취소·짧은 탭은 선택하지 않음. 선택 결과를 스크린리더에 알림.
- 깻잎·접시 에셋을 Figma에서 별도로 렌더/내보냄. 처음 받은 원본 URL은 전 픽셀이 투명해 교체함.
- 타입 검사·린트·프로덕션 빌드 통과. Headless Edge에서 클릭·드래그·4문항 순환·선택 유지 및 폭 360/390/402/430/844/1366/1440의 가로 넘침 검사 통과.
- 실제 모바일 터치·키보드 실기기 검증은 미실행. PC↔모바일 레이아웃 변경 시 기존 상위 레이아웃이 재마운트되어 로컬 게임 상태가 초기화되는 현상은 공통 레이아웃 후속 항목.
- 최종 추가 검증: 투명 깻잎 PNG 적용 후 타입·린트·빌드 재통과. 키보드 Enter 선택, 축소된 PC 목업 드래그, pointercancel에서 기존 선택 유지 확인.

## 깻잎 선택 애니메이션 정정
- 사용자 첨부 시안을 우선 적용. 다른 3문항은 노출하지 않고 깻잎만 시연한다. 상단 아이콘은 다시하기이며 1/4 표시는 참조 시안 유지.
- 클릭/드래그 → 선택 접시 위 고정(400ms) → 점진 확대 → 1700ms 시점 깻잎 더미 이미지로 전환. 결과는 중앙 접시와 선택 문구로 표시하며 젓가락 깻잎을 위에 유지한다.
- 제공된 PNG를 perilla-pile.png로 원본 복사. 양쪽 모두 제공된 파란 접시 이미지를 사용하고 선택 문구는 해당 색상을 유지한다.
- 애니메이션 중 중복 선택 방지, 다시하기/이탈 시 타이머 정리, reduced-motion 전환 효과 제거.
- 타입·린트·빌드 통과. Edge에서 왼쪽 클릭·오른쪽 드래그·결과 전환·애니메이션 중 다시하기·360/402/430/1440 폭 넘침 검증 통과. 실기기 터치는 미검증.

- 깻잎 접시 정렬 수정: 젓가락 포함 이미지 박스 대신 잎 중심(가로 35%)을 접시 중심에 맞춤. 확대 원점도 잎 하단으로 변경. 깻잎 최대 확대 1.55→1.08, 선택 접시 1.2배·버튼 1.15배로 첨부 시안 비율 적용. 402px 브라우저에서 양쪽 확인, 오른쪽 잎/접시 중심 오차 0.01px 미만.

- 드래그 범위를 고정 ±70px에서 실제 양쪽 접시 중심까지의 거리로 변경. 초기 잎 중심도 보드 중앙으로 정렬. 선택 즉시 접시 중심에 고정한 뒤 확대 시작하며 결과의 잎 역시 중앙 정렬. Edge에서 양쪽 드래그 끝점 및 선택 후 50/500/1100ms 중심 오차 1px 미만 확인. 타입·린트·빌드 통과.

- 최종 상태 위치 조정: 확대 중 중앙 정렬은 유지하고 결과에서 잎 중심을 보드 너비 74%의 오른쪽 위로 이동. 750ms ease-out 전환, 좁은 화면에서 이미지 오른쪽 경계 제한. 402px Edge 렌더에서 위치/넘침 확인. reduced-motion 유지.

## 드래그 놓기 시점 정정
- 사용자 설명에 따라 드래그 중 접시에 접근하면 해당 접시를 900ms 동안 1.2배로 확대하는 미리보기로 변경. 잡고 있는 동안 결과를 확정하지 않는다.
- 접시에서 벗어나면 미리보기 해제. 유효 접시 위에서 pointerup일 때만 확정하고, 같은 접시 위치에서 중앙으로 확대/이동하면서 깻잎 더미로 교차 전환. 기존 놓은 뒤 1700ms 대기는 제거. 클릭도 동일한 확정 전환 사용.
- 양쪽 각각 1.9초 드래그 유지 시 미확정·1.2배 미리보기, 중앙 복귀 시 해제, 놓은 뒤 결과 전환 확인. 타입·린트·빌드 통과.

- 전환 자연스러움 수정: 놓은 뒤 빈 접시 확대(900ms)를 먼저 완료하고, 결과 이미지는 같은 위치/최종 크기에서 550ms opacity 교차 전환. 확대 중 결과 이미지가 나타나며 이동하던 효과 제거. Edge에서 확대 중 opacity 0 → 완료 후 1 및 초기화 확인, 타입·린트·빌드 통과.

- 빈 접시 2종 배경 제거: 흰 배경이 포함된 렌더 PNG를 Figma 원본 노드 1554:8700/1554:8699의 contentsOnly 투명 PNG로 교체. plate-left.png·plate-right.png 경로 유지, 실제 alpha 최소 0 확인. 접시 모양 재생성 없이 원본 보존.

## 말풍선 이동·선택 테두리 (2026-09-10)
- 말풍선의 단계별 scale/좌우 기준 변경 대신 고정 크기·중심 좌표 이동으로 통일. 접시와 동일한 900ms easing 적용, 꼬리 위치도 중앙 유지.
- 드래그 진입/선택 상태에 currentColor 테두리: 왼쪽 파랑, 오른쪽 빨강. 결과 상태까지 유지.
- Edge에서 양쪽 드래그 진입·확정 테두리 및 애니메이션 중 말풍선 폭 일정 확인. 빌드 통과.

- 말풍선 꼬리 테두리 보완: 기존 SVG 형상을 유지한 파랑/빨강 stroke 변형을 추가하고 드래그 진입·선택 상태에서 적용. 꼬리 밑변과 본체 윗선의 중복선을 감춰 테두리 연결. 브라우저 렌더 및 타입·린트·빌드 확인.

- 꼬리 접합 틈 수정: 별도 꼬리 이미지와 흰 덮개를 제거하고 본체+꼬리를 단일 SVG 폐곡선으로 렌더. 선택/드래그 파랑·빨강 stroke 유지. 2배 픽셀 브라우저 렌더와 타입·린트·빌드 확인.

## 막상막하 최신 개발 시안 반영 (2026-09-10)

- Figma 개발 > 홈/로그인 전 > Section / 막상막하 · Close call 노드 1872:16223 기준으로 기존 카드형 화면을 게이지형 화면으로 교체했다.
- 위 카드: 하늘색 354 × 262 게이지, 제목, 양쪽 비율, 단 차이 배지, 왈가닥이 캐릭터. 최신 시안의 반려견 사건은 구속 합당 48% / 구속 과도 52%다.
- 아래 바꿔보기는 실제로 위·아래 사건을 교환한다. 중고거래 사건으로 바뀌면 위 게이지·바늘·차이(16%)·퍼센트와 아래 그래프·라벨이 함께 갱신된다.
- Figma 원본 왈가닥이 SVG를 src/assets/home/figma/close-call-mascot.svg에 저장했다.
- npm run typecheck, npm run lint, npm run build 통과. Headless Edge에서 초기 48/52·4% 차이와 교환 후 58/42·16% 차이·아래 카드 수치 갱신 확인.

## hyomin 브랜치 병합 (2026-09-10)

- `origin/hyomin` 최신 커밋 `48c1631`의 축의금 사건 상세·투표·결과 페이지와 에셋, `/login`·`/cases/:caseId`·`/cases/:caseId/result` 라우트를 병합했다.
- 홈 페이지와 밸런스 게임은 현재 `main` 구현을 보존해 충돌을 해결했다.
- `npm run typecheck`, `npm run lint`, `npm run build`를 통과했다.

## main → hyomin 통합 충돌 정리 (2026-09-10)

- 사용자 진행 중 병합: hyomin `950f720`에 origin/main `60e942e` 통합. 충돌은 `src/routes/AppRoutes.tsx`와 이 문서 두 곳.
- AppRoutes의 MY 3개 import와 사건 접수 6개 import를 모두 유지. 기존 MY·내 사건·오늘의 사건 경로 및 main의 DetailLayout/5단계 접수 경로를 함께 보존. 홈·밸런스 게임·막상막하·폰트 등 main에서 자동 병합된 작업은 변경하지 않음.
- 문서는 hyomin의 최신 MY·댓글·검증 기록을 유지하고 main의 홈 작업 이력을 보존. 중복된 구버전 검증 기록 대신 최신 기록을 선택하고 광장 후속 기능 항목을 유지.
- `npm run build`(타입 검사 포함, 214 modules), `npm run lint`, `git diff --check` 통과. 소스와 프로젝트 문서의 충돌 표시 없음.
- 격리 Chromium 390·1440px에서 홈·광장·MY·내 사건 목록/결과·오늘의 사건 상세/결과·사건 접수 총 8개 경로 직접 접근, MY/사건 접수 네비 이동 및 런타임 오류 없음 확인. 접수 5단계 전체 제출/실기기 검증은 이번 범위에서 미실시.
- 파일 내용만 수정했으며 스테이징·병합 커밋·푸시는 실행하지 않음. 사용자가 두 충돌 해결 파일을 git add한 뒤 병합 커밋으로 마무리해야 함.

## 막상막하 게이지 `수정?` 시안 재작업 (2026-09-10)

- Figma 개발 페이지 `수정?` 프레임(노드 `1949:3366`)의 실측 치수로 게이지를 다시 그렸다. 카드 354 × 304, 라운드 16, 배경 `--blue-050`. 제목은 위에서 30px, 게이지 상자는 83px 자리에 309 × 191이다.
- 눈금은 conic-gradient를 버리고 인라인 SVG stroke 두 개로 바꿨다. 두께 18, `stroke-linecap: round`. 끝을 둥글게 마감하는 시안을 그라데이션으로는 흉내 낼 수 없어서다.
- **눈금은 원이 아니라 타원이다.** Plugin API로 시안 벡터를 직접 읽어 확인했다: 반타원 가로 82 · 세로 89.27, 중심 (154,115.4). 원으로 그리면 가로를 맞추면 세로가 눌리고 세로를 맞추면 가로가 좁아져, 어느 쪽으로 잡아도 시안과 어긋난다.
- 처음엔 Figma가 주는 bounding box가 **선 두께를 뺀 값**인 걸 모르고 반지름 73짜리 원으로 그렸다. 눈금이 작아져 판멍이·바늘이 다 붙어 보였던 원인이다.
- 중심이 세 개다. 눈금 타원 (154,115.4) · 축 덮개 (154,108.87) · 바늘 회전축 (154,111). 시안이 그렇게 그려져 있어 그대로 옮겼다.
- 바늘 각도는 회전축에서 경계 지점을 바라보는 각을 `atan2`로 계산한다. 타원이라 `(비율 - 50) × 1.8` 같은 단순 비례로는 경계와 어긋난다.
- 둥근 끝(`ROUND` cap)은 선이 끝난 자리에서 9px 더 튀어나온다. 두 선을 같은 각도에서 끊으면 위에 그린 빨강 뚜껑이 파랑 쪽으로 9px 넘어와, **보이는 색 경계가 계산상 경계보다 왼쪽에 생긴다.** 바늘이 계산상 경계를 가리켜도 빨강 안쪽을 찌르는 것처럼 보였던 원인이다.
- 빨강 시작 각도를 뚜껑만큼 물려서 그려 봤더니 두 선 사이가 벌어져 끝 모양이 부자연스러웠다. 그래서 **눈금은 시안 그대로 두고, 바늘이 그 '보이는 경계'를 겨누게** 했다. 겨누는 몫 `NEEDLE_AIM_OFFSET`은 7.72도 — 시안 바늘 벡터(회전축 154,111 → 끝 142,49 = 왼쪽 10.95도)를 그대로 재현하는 값을 역산해 넣었다. 뚜껑 반지름이 만드는 6.29도보다 조금 큰데, 그 차이는 시안에서 손으로 놓은 몫이다. 비율이 바뀌어도 같은 규칙으로 따라간다.
- 판멍이 크기(58)와 바늘 길이(63)는 **시안 값 그대로**다. 중간에 판멍이를 10% 줄이고 바늘을 70으로 늘려 봤지만, 팀 결정으로 시안 값으로 되돌렸다.
- 다만 그 결과 판멍이에 가려 보이는 바늘이 15px 남짓이다. 흔들림이 잘 안 보이면 이 두 값이 후보다.
- 파랑·빨강이 갈리는 각도는 `180 - leftPercent * 1.8`로 매 렌더마다 계산한다. 비율이 바뀌면 색 면적과 바늘 각도가 같이 움직인다.
- 바늘은 4px 폭에 길이 63px, 끝이 둥근 막대로 바꾸고 회전축에 14 × 15.2 타원 축 덮개를 뒀다. 색은 시안 그대로 `#30333a`.
- 기준 각도(실제 비율)를 중심으로 ±2.2도 안에서만 배회하는 흔들림은 그대로 유지했다. 겉 span이 각도, 속 `<i>`가 흔들림을 맡는 두 겹 구조도 유지한다.
- 브라우저에서 눈금·바늘·축 덮개·판멍이·선택지·배지의 상대 좌표가 시안 값과 1px 안에서 맞는 것을 확인했다.


## 지훈 유사 사건(종결 사건, 투표X) 구현 (2026-09-11)

- Figma 개발 페이지 2064:16366(지난 사건)·2064:16252(투표 결과) 기준으로, 곽지훈 흐름의 카페 홍보영상 제작·잔금 갈등 사건을 추가했다.
- 광장 전체 사건의 첫 카드 제목을 수정 2회를 마쳤는데, 의뢰인이 잔금 지급을 미루고 있어요.로 바꾸고, 이 카드만 /cases/case-company-874에 연결했다.
- 지난 사건에는 투표 선택지를 노출하지 않고 투표 결과보기만 두었다. 결과 화면은 배심원 2심 61%·9%·27%·4%, AI 1심(확신도 73%), 댓글, 후일담을 표시한다.
- Figma 원본의 작성자·판결 일러스트를 src/assets/case/jihoon/에 저장했다.
- 검증: npm run typecheck, npm run lint, npm run build 통과. 격리 브라우저에서 광장 첫 사건 href와 상세·결과 화면을 확인했다.

### 결과 화면 간격·댓글 보정 (2026-09-11)

- 지훈 종결 사건 결과에서 배심원 결과와 AI 1심 사이 구분선을 제거하고 50px 간격으로 분리했다. AI 1심 아래 구분선은 유지하되 댓글 섹션과의 간격을 시안 기준 64px / 40px으로 조정했다.
- 댓글 표기를 `댓글 (46)`으로 맞추고, 첫 페이지에 Figma의 5개 댓글을 표시한다. 댓글은 페이지당 5개씩 이동하며 46개 기준 총 10페이지를 제공한다.
- 결과 본문 하단 패딩을 120px으로 늘렸다. `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과. 브라우저에서 결과 화면의 AI 판결·댓글 5개·페이지 1~5 노출을 확인했다.

### 축의금 사건 최신 개발 시안 보정 (2026-09-11)

- 최신 Figma 개발 프레임 `2064:16681`·`2064:16466` 기준으로 축의금 사건 상세의 사건 번호·시간을 `#CASE-FRIEND-87`·`7시간 전`으로 고쳤다.
- 결과 화면은 배심원 2심·실시간 투표 중·AI 1심 결과 라벨, AI 판결 카피, 결과와 AI 1심의 50px 간격, `댓글 (124)` 및 5개 댓글, 비슷한 사건의 후일담 카피를 최신 시안에 맞췄다.
- `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과. 브라우저에서 `/cases/case-wedding-gift/result`의 최신 라벨·댓글 수·페이지 1~5 노출을 확인했다.

### 공통 상태바 시안 보정 (2026-09-11)

- PC 기기 목업의 시스템 상태바를 제공된 iPhone 시안처럼 시간(좌측 53px), 125 × 37px 다이내믹 아일랜드, 신호·와이파이·배터리 상태 아이콘으로 교체했다.
- 상태바는 DeviceFrame 안에서 한 번만 렌더링하므로 모든 화면에 같은 형태로 적용된다. `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check` 통과.

## 왈가왈후 · 내 이야기 남기기 화면 추가 (2026-09-11)

- `/afterstory/mine`에는 화면 대신 `<Navigate to='/afterstory/write/friend'>` 리다이렉트만 있었다. 코드·CSS 어디에도 목록 화면이 없어서 시안대로 새로 만들었다.
- `MyAfterStoryPage`를 `AfterStoryPage.tsx`에 추가했다. 제목·안내 배너(판결이 끝난 사건 N건)·`내가 올린 사건` 카드 2장(친구/직장) 구성이며, 카드의 `후일담 작성하기`가 `/afterstory/write/:caseId`로 들어간다.
- 시안에 하단 내비게이션이 보여서 라우트를 `DetailLayout`에서 `MainLayout` 아래로 옮겼다.
- 왈가왈후 홈의 `내 이야기 남기기` 카드가 작성 화면으로 바로 가던 것을 이 목록 화면으로 돌렸다. 작성 화면의 뒤로가기도 목록으로 온다.
- 사건 목록은 `MY_CLOSED_CASES` 상수(시연용 고정 데이터)다. 의견·댓글 수까지 시안 값 그대로.
- 브라우저에서 카드 2장 렌더, 두 카드의 작성 버튼 이동(`/afterstory/write/friend`·`/company`), 뒤로가기 복귀, 홈 카드 진입을 모두 확인했다.

## 후일담 작성 화면 시연용 자동 입력 (2026-09-11)

- `후일담 내용 *` 오른쪽에 `내용 작성하기` 텍스트 버튼을 추가했다. 누르면 `DEMO_STORY` 예시 본문이 한 번에 채워지고 커서가 글 끝으로 간다. 발표 중 타이핑할 시간을 줄이려는 장치다.
- 기존 placeholder·직접 입력·미리보기 흐름은 건드리지 않았다. 글자수 카운터와 미리보기 버튼 활성화가 그대로 따라온다.
- 글자 크기는 라벨·본문과 같은 14px로 맞췄다.

## 왈가왈후 홈 하단·편지 위치 시안 대조 (2026-09-11)

- 시안 PNG를 픽셀로 재서 코드 값과 대조했다(프레임 390 × 1167, 크림 배경 시작점을 0으로 잡음).

| | 시안 | 수정 전 | 수정 후 |
|---|---|---|---|
| 진입 카드 | 147 ~ 327 | 144 ~ 324 | 그대로 |
| 제목 글자 | 356.5 ~ 372.1 | 358 ~ 374 | 그대로 |
| 봉투 상자 | 388 ~ 750 | 403 ~ 765 | 388 ~ 750 |
| 파란 띠 시작 | 743.6 | 764.1 | 749.1 |
| 파란 띠 보이는 높이 | 204.2 | 271 | 209 |
| 마지막 글자 ~ 내비게이션 | 63.5 | 25 | 63 |

- **편지지가 봉투 밖으로 새어 나왔다.** 편지지 이미지는 411px인데 봉투 상자는 362px이라 아래로 삐져나와 파란 띠 위에 흰 종이가 얹혀 있었다. `.afterstory-envelope`에 `overflow: hidden`을 줘서 잘랐다.
- **파란 띠 아래 여백을 125 → 163px로 늘렸다.** 하단 내비게이션이 100px 겹쳐 뜨므로, 시안의 `글자~내비게이션 63px`을 만들려면 63 + 100 = 163이 필요하다. 파란색은 내비게이션 뒤까지 이어진다.
- 봉투 위 여백 24 → 9px. 시안은 제목 글자 아래 20px에서 편지지가 시작하는데, 편지지 이미지에 위쪽 투명 여백이 15px 더 있어 그만큼 당겼다.
- 중간에 `.afterstory-home__scroll`에 하단 여백을 116~148px 준 적이 있는데, 파란 띠 아래로 크림색 빈 칸만 생겨 화면이 길어 보였다. 홈은 0으로 되돌렸다(목록 화면은 마지막이 흰 카드라 148px 유지).


## verdict-react-v2 독립 미리보기 추가 (2026-09-13)

- 사용자 ZIP의 React 프로젝트와 독립 HTML·참고 PNG 총 14개 파일을 루트 `verdict-react-v2/`에 원본 그대로 추가했다. 첨부 README의 통합 안내는 참고 자료로만 읽었으며 기존 앱에 컴포넌트를 이식하지 않았다.
- 작업 브랜치는 `hyomin`. 작업 전 수정·미추적 파일의 SHA-256 대조로 기존 내용을 모두 보존했음을 확인했다. 이 문서는 기존 내용 뒤에 작업 기록만 덧붙였다. 스테이징·커밋·푸시 없음.
- 미리보기: http://127.0.0.1:5182/ (Codex 브라우저). 새 의존성 설치가 npm registry DNS 오류(ENOTFOUND)로 실패해 기존 루트 node_modules의 React 19.2.8·Vite 8.2.2로 실행/빌드했다. ZIP 지정 React 19.1.0·Vite 6.1.0 조합은 미검증이며 두 package 파일은 보존했다.
- 재실행: `cd verdict-react-v2 && node ../node_modules/vite/bin/vite.js --host 127.0.0.1 --port 5182 --strictPort`. 단독 HTML은 같은 폴더의 `verdict-react-v2-preview.html`에 보관했다.
- 검증: v2 Vite 빌드, 기존 앱 `npm run build`(타입 검사 포함)·`npm run lint` 통과. 기존 앱의 500kB 청크 경고는 유지된다. 브라우저에서 화면 렌더·정지 모드 전환·재생 버튼 비활성·근거 펼치기를 확인했다. 실제 모바일 기기는 미검증.


## 개인 Figma 판결 불일치 히어로 병합 (2026-09-13)

- 사용자 지정 개인 파일 `xzXF3SIGO4rcu8wIM9002Z`, 화면 `817:3558`의 `VerdictDisagreementHero`(`820:1858`)를 첨부 이미지 구성으로 변경했다. 사건 종결·중앙 제목·공유 무대·판멍이/왈랑이/왈가닥이·61%·하단 다시 보기 구성을 354 × 570으로 배치했다.
- 기존 비교 제목/설명/두 열은 해당 히어로 안에서 숨겨 보존했다. 아래 AI 근거·투표 결과·댓글·후일담은 유지하며 부모 auto-layout으로 높이만 따라간다.
- v2의 원본 비트맵·벡터·판멍이 마스크·투표지 경로를 사용했다. 제목·설명·61%·하단 표기는 Figma 텍스트이며, Figma 다시 보기 버튼은 시각 요소만 구성했다(애니메이션 프로토타입 미연결).
- Figma 편집 환경에서 기존 Pretendard Variable 로드 실패를 확인하여 이 히어로의 신규 텍스트에 사용 가능한 Noto Sans KR Regular/Bold/Black을 적용했다. 다른 섹션 폰트는 변경하지 않았다.
- Figma 섹션 캡처로 비율 줄바꿈 수정 및 캐릭터/마스크/하단 도구를 확인했다. 로컬 v2 코드와 기존 앱 코드는 이번 작업에서 변경하지 않았다. 커밋·푸시 없음.

## 최신 개인 Figma → 실제 결과 페이지 구현 (2026-09-13)

- 사용자 지정 `http://localhost:5173/cases/case-company-874/result`의 React 화면에 최신 `817:3558`을 반영했다. 별도 `VerdictDisagreementHero` 컴포넌트로 제목·무대·캐릭터·61%·비교 문구를 구현했다. 402px 화면에서 히어로는 시안과 같은 354 × 492px이다. 최신 시안에서 제거된 사건 종결/다시 보기 영역은 제외했다.
- AI 설명 카드·투표 섹션 간격·댓글 아바타/배지·후일담 스타일을 반영했다. 투표 마지막 항목은 3%로 수정했다. 기존 댓글 등록·공감·페이지 이동 및 공통 헤더/하단바/챗봇을 유지했다.
- 원본 v2 캐릭터 이미지와 Figma에서 내보낸 벡터/아바타를 앱 전용 assets에 추가했다. 제목 및 지정 설명용 Paperlogy/Noto는 현재 문구의 글리프를 포함하는 WOFF2 서브셋(약 18/43kB)이며 문구 확장 시 글리프 추가가 필요하다. 라이선스 파일을 함께 보관했다. v2 원본은 변경하지 않았다.
- 검증: `npm run build`(TypeScript 포함), `npm run lint`, `git diff --check` 통과. 기존 500kB 초과 번들 경고는 유지된다. Chrome 360/390/402/430/1440px에서 이미지 누락·가로 넘침 없음. 결과 접기/펼치기, 댓글 46→47 등록, 페이지당 5개, 공감 상태의 페이지 이동 후 유지 확인. 테스트 댓글은 새로고침으로 초기화했다. 콘솔 오류/경고 없음. 실제 모바일 기기는 미검증이다.
- 작업 전 수정·미추적 파일의 SHA-256 비교로 기존 내용 보존을 확인했다. 문서는 기존 내용 뒤에 기록을 추가했다. 브랜치 `hyomin` 유지, 스테이징·커밋·푸시 없음.

### 결과 화면 최신 수정 및 인터랙션 복원 (2026-09-13)

- 개인 Figma `817:3558`을 재조회해 히어로 안내 문구 위치/색상/서체, `판멍이는 글쓴이` 표기, AI 요약 18px Bold 및 본문 행간을 반영했다.
- 히어로는 앱 전체 너비로 확장하고 무대 내부 클리핑을 제거했다. 374px 미만의 기존 18px 본문 패딩도 별도로 상쇄한다. 다른 본문은 기존 여백을 유지한다.
- Figma motion 응답에는 애니메이션이 없었다. 요청에 따라 v2 캐릭터 등장 타이밍을 재사용하고 화면 진입 시 한 번 재생, 0→61% 카운트, 다시 보기, reduced-motion 완성 장면 처리를 구현했다. 원본 v2는 그대로 유지했다.
- 빌드·린트 통과(기존 번들 크기 경고 유지). 브라우저에서 재생 직후 0%/상대방 opacity 0 → 완료 61%/opacity 1을 확인했다. 402/430px 및 PC 전체 너비 일치·이미지 누락 없음. 실제 모바일 기기는 미검증. 커밋·푸시 없음.

### HTML 원본 인터랙션 정정 (2026-09-13)

- Desktop/verdict-react-v2-preview.html의 실제 번들 코드를 대조했다. 이전 구현에서 빠진 투표지 22개 SVG 경로 애니메이션·주황빛을 원본 v2 소스에서 재사용하고, 캐릭터 transform-origin을 복원했다. 원본에 없는 숫자 카운트는 제거했다. 최신 Figma 문구·전체 너비는 유지했다.
- 빌드·린트·diff 검사 통과. 브라우저 다시 보기에서 투표지 22개와 시작 간격(0.45/0.495초), 고정 61% 및 순차 등장 확인. 커밋·푸시 없음.

### 다시 보기 아이콘 및 간격 업데이트 (2026-09-13)

- Figma 820:1858 및 부모 817:3558을 재조회했다. 다시 보기 텍스트를 Figma 원본 주황 Solar refresh-bold SVG로 교체하고, 비교 문구/버튼을 12px 간격의 세로 묶음으로 구성했다. 버튼 24×24px, 실제 아이콘 22.934×20.8px이며 접근성 이름과 확장 클릭 영역을 제공한다. 원본 아이콘 라이선스 기록을 추가했다.
- 나머지 주요 간격은 최신 시안과 같은 제목 줄 사이 5px, AI/투표 섹션 32px, AI 카드 안 24px, 댓글 페이지 42px, 후일담 50px임을 대조했다. 전체 너비와 원본 22개 투표지 인터랙션 유지.
- 빌드·린트·diff 검사 통과. 402px 브라우저에서 버튼 24px/간격 12px/히어로 402px, 아이콘 로드 및 클릭 후 애니메이션 재시작 확인. 커밋·푸시 없음.

- 제목 잘림 보정: Paperlogy 제목 행간을 1.2(43.2px), 위아래 패딩 2px로 조정하고 제목을 무대보다 위에 표시해 배경이 글자를 가리지 않게 했다. 요청대로 비교 문구/다시 보기 간격은 16px. 402px 브라우저에서 제목 표시와 계산된 행간/간격 확인, diff 검사 통과. 커밋·푸시 없음.

### 사용자 승인에 따른 hyomin 커밋·푸시 (2026-09-13)

- 이전 커밋·푸시 금지 지시를 사용자가 해제하고 본인 hyomin 브랜치에만 저장하도록 승인했다. 광장, v2 독립 미리보기, 결과 화면/작업 기록을 세 커밋으로 분리한다. main 병합 및 강제 푸시는 하지 않는다.
- 최종 npm run build·npm run lint·git diff --check 통과. 기존 500kB 번들 경고 유지.

### 판단 기준 비교 / 근거 펼침 / Figma 레이어 정리 (2026-09-14)

- 사용자 지정 결과 페이지 `843:3262`와 단독 근거 `843:3890`의 총 475개 레이어를 검토해 279개 이름을 정리했다. 루트는 `ClosedCaseResultPage`/`AiVerdictDetails`, 버튼은 `ReasonDetailsToggle`이다. 일반 자동 이름·형제 중복 이름은 0개이며 숨김 레거시도 구분했다. 각 이름 변경 직전·직후 위치·크기·표시 상태와 전체 텍스트 110개가 동일함을 확인했다. 공유 컴포넌트를 수정하거나 분리하지 않았다.
- Figma 쓰기는 이름 변경만 수행했다. 단독 근거의 전체 속성 지문은 일치한다. 페이지 복합 속성 지문은 직렬화 길이가 10자 달랐고 이전 전체값을 보관하지 않아 원인을 확정하지 못했다. 따라서 페이지 전체 속성이 완전히 동일하다고 단정하지 않는다. 최종 화면은 정상 확인했다.
- React `/cases/case-company-874/result`에 `VerdictReasonComparison`을 연결했다. 최신 파랑/크림 기준 카드·아이콘·문구·Pretendard 서체를 반영하고, 기본 접힌 AI 근거를 인라인으로 펼친다. 펼침 부분은 402px 화면에서 354×243px(버튼 아래 간격 12px 제외)이며 최신 단독 프레임과 대응한다.
- 투표 제목의 독립 토글·상단 구분선·박스 없는 집계를 반영했다. 히어로는 최신 본문 너비 354px, `판멍이는` 문구, 다시 보기 아이콘만 있는 하단으로 수정했다. 기존 제목 잘림 보정·22개 투표지 재생·댓글 기능은 유지했다.
- Figma SVG 4개와 토글 전용 Noto Sans KR Medium 서브셋·출처·OFL 라이선스를 추가했다. 폰트 cmap에 토글의 모든 글자가 포함됨을 확인했다. 전역 폰트와 토큰은 변경하지 않았다.
- 검증: `npm run build`(TypeScript 포함), `npm run lint`, `git diff --check` 통과. 기존 500kB 초과 청크 경고는 유지된다. Codex 브라우저에서 360/390/402/430/1366/1440px 가로 넘침 없음, 이미지·폰트 로드, 클릭/Space/Enter 펼침·접기, 두 상태의 독립성, 포커스 유지와 다시 보기의 투표지 22개를 확인했다. 콘솔 오류·경고 없음. 실제 모바일 기기는 미검증이다. 커밋·푸시 없음.

### 상세·결과 최신 시안 재반영 및 신규 댓글 표시 수정 (2026-09-14)

- 개인 Figma `843:3890`/`843:3762`/`843:3262`를 다시 조회했다. 상세 제목은 `의뢰인이` 뒤, 결과 제목은 `마쳤는데,` 뒤에서 줄을 나누며, 일반 제목과 각 화면의 표시 제목을 데이터로 구분했다. 상세 본문 첫 문장 뒤 줄바꿈·AI 핵심요약 카드 8px 반경·결과 페이지 번호 Inter Medium을 반영했다. 기존 Inter 에셋을 재사용하며 페이지 범위에서만 적용했다. 펼친 판단 근거는 현 구현과 일치함을 확인했다. Figma 자체는 수정하지 않았다.
- `JihoonSimilarComment`의 투표 정보가 없을 수 있도록 하고, 신규 댓글은 `voteId`/`voteLabel`을 null로 둔다. 투표 정보가 있는 기존 댓글에만 배지를 렌더한다. 신규 댓글 아바타는 `currentUser.anonymousAvatarUrl`을 사용하며 없으면 기존 첫 댓글 아이콘으로 대체한다. 세션 구조와 닉네임은 유지했다.
- 로그인 후 테스트 댓글 등록에서 46→47개, 캐릭터 아바타 이미지 정상 로드, 아바타 텍스트 없음, 신규 배지 0개, 기존 투표 배지 유지 확인. 테스트 댓글은 화면 이동으로 초기화하고 검사 전 로그인 상태로 복구했다. 상세→투표 결과 링크와 360/402/430px에서 가로 넘침 없는 것을 확인했다.
- 검증: 빌드(TypeScript 포함)·린트·diff 검사 통과. 기존 500kB 청크 경고 유지. 커밋·푸시 없음.

### 서아 공개댓글 프로필 지정 (2026-09-14)

- 사용자 요청에 따라 서아 닉네임 `익명의 왈가닥`을 유지하고 공개댓글용 아바타를 기존 `침낭펴야지?` 댓글과 동일한 `case/disagreement/comment-2.png`로 변경했다. 계정 데이터에서 연결해 공개댓글 화면에 공통 적용한다.
- 로그인 후 등록한 새 댓글에서 닉네임·두 아바타 src 동일·이미지 로드·투표 배지 없음 확인. 검사 댓글과 로그인 상태를 초기화했다. 빌드(TypeScript 포함)·diff 검사 통과, 기존 청크 크기 경고 유지.

## 비로그인 로그인 유도 팝업 (2026-09-14)

- Figma 개발 페이지 `2298:16848`(사건 접수) · `2298:17535`(MY) · `2298:17673`(AI 맞춤 추천) · `2298:17811`(공통) 기준으로 로그인 유도 팝업을 구현했다. 네 시안은 캐릭터 이미지와 제목·설명만 다르고 팝업 껍데기가 같아 `LoginPopUp` 한 컴포넌트의 variant로 넣었다.
- 라우트로 막지 않고 클릭 시점에 가로챈다. 시안 네 개가 모두 "보던 화면이 그대로 어두워지고 그 위에 팝업"이라서다. 라우트를 바꾸면 보던 화면이 사라져 시안과 달라진다. 닫으면 있던 자리에 그대로 남는다.
- `LoginGateProvider`가 `requireLogin(reason, destination)`을 제공한다. 로그인 상태면 `true`를 돌려주고 아무것도 하지 않으며, 비로그인이면 팝업을 띄우고 `false`를 돌려준다. 사건 접수는 DetailLayout, MY·홈은 MainLayout이라 공통 조상이 App뿐이라 거기에 뒀다.
- 연결한 진입점은 넷이다. 하단 내비의 `사건 접수`·`MY`(`NavItem.gate`), 홈 하단 AI 추천 카드, 그리고 왈가왈후~ 홈의 `내 이야기 남기기` 카드다. AI 카드는 비로그인일 때만 버튼이 되고 로그인 상태에서는 기존대로 정적 안내로 남는다. 홈·배심원 광장·왈가왈후~ 홈 자체는 둘러보기 대상이라 막지 않는다.
- 왈가왈후~ `내 이야기 남기기`(2026-09-14 추가)는 내 사건을 다루는 화면이라 공통 시안 `2298:17811`의 `default` variant를 쓴다. 하단 내비와 같은 방식으로 `<Link>`의 `onClick`에서 `preventDefault()`만 하고 이동을 막는다. 복귀 경로는 `/afterstory/mine`이다. 같은 줄의 `다른 이야기 읽어보기`는 공개라 막지 않았다.
- 댓글에는 아직 붙이지 않았다. 축의금 결과 화면(`CaseResultPage`)이 비로그인이면 `<Navigate to={loginPath} replace />`로 `/login`에 튕겨내고 있어 비로그인으로 댓글에 도달할 수 없다. 이 리다이렉트를 걷어낼지는 다른 담당자 코드라 정하지 않았다. → 확인 필요
- `로그인하기`는 `/login?from=<원래 가려던 경로>`로 이동하고, 로그인을 마치면 그 경로로 돌아온다. 복귀 경로는 `/`로 시작하고 `//`가 아닌 앱 내부 경로만 받는다.
- 팝업은 `#app-overlay-root`에 portal로 붙여 기기 내부에서만 어두워진다. 딤은 시안 값인 `#252525` 70%로, 기존 계정 전환 시트의 45% 검정과 다르다.
- 에셋은 `src/assets/auth/`에 넣었다. Figma 원본 PNG 4.5MB를 브라우저 캔버스로 720px WebP(품질 0.92)로 구워 189kB로 줄였다. 새 라이브러리는 설치하지 않았다. AI 시안만 이미지에 벡터 마스크가 걸려 있어 원본 SVG를 CSS mask로 쓴다.
- 에셋·컴포넌트·CSS 클래스 이름은 사용자 요청대로 기호 없는 camelCase다(`loginPopUpCaseFiling.webp`, `.loginPopUpOverlay`). 프로젝트의 기존 kebab-case 규칙과 다르다.
- 공통 팝업의 자물쇠(`loginImage04`)가 마스코트와 같은 277 × 205로 부풀어 설명 문구를 덮고 있었다. `.loginPopUpMascot img`의 `width/height: 100%`가 `.loginPopUpMascotKey`보다 우선순위가 높아 시안 치수가 무시된 것이다. 선택자를 `.loginPopUpMascot img.loginPopUpMascotKey`로 올려 고쳤다.
- 자물쇠 에셋도 다시 구웠다. 원본이 720 × 534 캔버스에 그림이 (197, 69) 389 × 451로 들어 있어 여백이 커서 `object-fit: cover`가 엉뚱하게 잘라냈다. 그림만 잘라 268 × 311로 바꿔 24.9kB → 18.7kB가 됐고, 이제 object-fit 없이 상자 크기만 맞추면 된다.
- 402px 폭 실측으로 시안과 대조했다. 자물쇠 67.00 × 76.99 @ (205.11, −60.86), 마스코트 276.30 × 204.95 @ (66.96, −164.47)로 시안과 0.4px 안에서 맞고 설명 문구와 겹치지 않는다.
- **직접 URL 접근은 아직 막지 않는다.** 비로그인으로 `/cases/new`나 `/my`를 주소창에 직접 치면 그대로 열린다. 시안이 클릭 시점의 팝업만 정의하고 있어 `RequireAuth` 가드는 넣지 않았다. → 확인 필요
- 검증: `npm run lint`·`npm run typecheck`·`npm run build` 통과. 브라우저에서 비로그인 상태로 사건 접수·MY·AI 카드 세 진입점의 팝업, 닫은 뒤 화면 유지, `로그인하기` → `/login?from=%2Fcases%2Fnew` → 데모 로그인 → `/cases/new` 복귀를 확인했다. 실기기는 미검증.

## 로그인·회원가입 시안 구현 (2026-09-14)

- 임시 데모 화면이던 `/login`과 `/signup`을 Figma 개발 페이지의 확정 시안으로 교체했다. 기준 프레임은 로그인 `2187:24325`(빈 상태)·`2264:13801`(입력됨)·`2264:13898`(비밀번호 보임), 회원가입 `2264:10514`·`2187:24632`·`2264:13995`, 동의 시트 `2264:13125`·`2264:13294`, 가입 완료 `2187:24600`이다.
- 전체 흐름이 이어진다. 비로그인으로 사건 접수 → 로그인 유도 팝업 → `/login?from=/cases/new` → `이메일로 회원가입` → `/signup?from=...` → 동의 시트 → `/signup/complete` → `왈가왈BOT 시작하기` → 원래 가려던 `/cases/new`. 복귀 경로는 각 단계에서 `from`으로 넘어간다.
- 로그인은 `서아 계정 로그인`을 누르면 이메일과 비밀번호가 한 번에 채워지고 문구가 `서아 계정 로그인 중`으로 바뀐다. 시안 메모(`2264:13811`)의 자동 입력 동작이다. 직접 입력도 그대로 되며 두 칸이 모두 차야 버튼이 주황으로 켜진다.
- 회원가입은 닉네임(2~6자)·이메일·비밀번호·비밀번호 확인이 필수, 생년월일이 선택이다. 비밀번호 두 칸이 같아야 `비밀번호 일치`가 뜨고 동의 시트를 열 수 있다. 발표용 `서아 계정으로 채우기` 버튼을 함께 뒀다.
- 동의 시트는 필수 두 항목에 동의해야 `회원 가입 완료하기`가 켜진다. 동의 상태는 갱신 함수로 넘긴다. 값을 펼쳐 넘기면 여러 항목을 빠르게 연달아 누를 때 앞 선택이 덮어써진다.
- 가입 완료 화면은 시안에 헤더가 없어 `AuthLayout` 밖 `/signup/complete`에 뒀다. `PATHS.signupComplete`를 추가했다.
- `AuthLayout`의 상단 바를 시안의 헤더로 바꿨다. 높이 66, 좌우 24, 아래 테두리 `--blue-050`, 제목은 경로로 고른다. 로그인과 회원가입 시안의 헤더가 같아 페이지마다 그리지 않는다. 뒤로가기는 기존 `tail-arrow-left.svg`를 재사용했다.
- `FloatingChatButton`을 `/login`·`/signup`·`/signup/complete`에서 숨겼다. 시안에 없고 하단 링크·버튼과 겹친다.
- 에셋은 `src/assets/auth/`에 넣었다. 카카오 로고는 말풍선과 TALK 네 글자가 따로 내려와 시안 좌표대로 하나의 SVG로 합쳤다. 가입 완료 캐릭터 PNG 1,063kB는 브라우저 캔버스로 700px WebP(품질 0.92) 113kB로 줄였다. 새 라이브러리는 설치하지 않았다.
- 시안의 서아 계정은 `seoa1001@walgawalbot.com` / `seoa123#` / 닉네임 `서아`다. `data/personas`의 `DEMO_ACCOUNTS.A`(`seoa_daily@gmail.com`)와 다르다. 그쪽은 MY·계정 전환 등 다른 담당자 화면이 쓰고 있어 건드리지 않고, 인증 화면 전용 값을 `pages/Auth/authDemoAccount.ts`에 따로 뒀다. → 확인 필요
- 동의 시트를 세 가지 고쳤다(2026-09-14).
- (1) `* 어플 이용시 동의가 필요해요` 줄이 아직 누를 수 없을 때 `opacity: .45`로 흐려졌다. 빠뜨리면 안 되는 항목이라 눈에 띄어야 한다는 팀 판단에 따라 불투명도를 걷어내고, 누를 수 있는지는 커서 모양으로만 구분한다.
- (2) 필수 두 항목(`terms`·`privacy`)을 처음부터 켜 둔다. 시안 `2264:13125`에도 켜진 채로 그려져 있다. `EMPTY_AGREE`를 `DEFAULT_AGREE`로 바꿨고 선택 두 개는 그대로 꺼져 있다. 필수를 끄면 완료 버튼이 다시 막히는 동작은 유지된다.
- (3) 시트 제목이 연 경로에 따라 달라진다. 동의 줄로 스스로 열면 `동의가 필요해요`, 동의를 거치지 않고 `회원 가입 완료하기`를 눌러 확인차 뜨면 `동의하고 회원가입 할게요`다. 어느 쪽이든 시트 안에서 완료를 한 번 더 눌러야 다음으로 넘어간다. `SignUpAgreeSheet`가 `openReason` prop을 받고, 화면은 `isAgreeOpen` 불리언 대신 연 경로(`agreeOpenReason`)를 기억한다.
- 검증: 브라우저 실측 — 비활성 상태에서도 동의 줄 opacity 1, 완료 버튼으로 열면 제목 `동의하고 회원가입 할게요`·체크 `[true, true, false, false]`·주소는 `/signup` 유지, 동의 줄로 열면 제목 `동의가 필요해요`, 시트 완료를 누르면 `/signup/complete`로 이동, 필수 하나를 끄면 완료 버튼이 막히고 되돌리면 다시 켜짐.
- 연결하지 않은 것: SNS 로그인 3종(시안에 카카오가 `비활성화`로 표시), `아이디 / 비밀번호 찾기`, 동의 2~4번 항목 펼치기, `[이용약관 전문 보기]`. 모두 이동할 화면 시안이 없다. → 확인 필요
- **로그인 완료 포인트 팝업(`2187:24702`·`2187:24768`·`2187:24831`·`2187:24894`·`2298:16744`)은 아직 만들지 않았다.** 10PT 코인이 내려와 그릇에 담기고 합계가 바뀌는 애니메이션이라 별도 작업이다. → 미구현
- 검증: `npm run lint`·`npm run typecheck`·`npm run build` 통과. 브라우저에서 사건 접수 팝업부터 `/cases/new` 복귀까지 전체 흐름, 로그인·회원가입 자동 입력, 비밀번호 보기 토글, 약관 펼치기, 필수 동의 전 버튼 비활성을 확인했다. 실기기와 키보드가 열린 상태는 미검증.

## 출석 포인트 팝업 (2026-09-14)

- 동전 낙하 모션을 다시 깔았다(2026-09-14). 시안 네 장의 좌표를 그대로 이어 붙였더니 구간별 낙하 거리가 63.9 → 43.6 → 91.4인데 시간은 35% → 35% → 30%라, 떨어지다 가운데서 느려졌다 다시 빨라졌다. x도 `226 → 192 → 188.31 → 191.28`로 끝에서 오른쪽으로 되돌아와 흔들렸고, easing이 구간마다 걸려 키프레임마다 멈칫했다.
- 시작 `(226, -28.85) ×1`과 끝 `(191.28, 170.03) ×0.682`는 시안 값 그대로 두고 그 사이만 낙하 곡선으로 채웠다. 키프레임 29개가 경로를 직접 들고 타이밍은 `linear`다. y는 `t²`(중력), x는 `easeOutCubic`(공기저항), 회전은 등속, 크기는 y와 같은 `t²`다. 86%에 그릇에 닿고 남은 14%는 공 더미에 5px 잠겼다 돌아오는 마무리다.
- 길이(820ms)와 지연(380ms)은 건드리지 않았다. 끝나는 시각 1200ms가 숫자를 `+10PT`로 바꾸는 시점과 맞춰 둔 값이라서다.
- 판멍이 몸통 이미지를 교체했다(2026-09-14). 사용자가 준 새 원본(3363 × 2030, 1.9MB PNG)을 `loginRewardMascotBase.webp` 990 × 660, 24kB로 구웠다.
- **기존 캔버스 규격에 맞춰 앉혔다.** 다섯 조각(귀·팔·몸통·동전·손)이 서로 맞물려 있어 몸통의 틀이 바뀌면 나머지가 전부 어긋난다. 기존 에셋은 612 × 408 캔버스에 그림이 (101, 59) 329 × 214였다. 같은 상대 위치가 되도록 1.61765배(표시 크기의 약 2배)한 990 × 660 캔버스의 (163.4, 95.4)에 532.2 × 346.2로 그렸다.
- 그 덕에 `.loginRewardBase`의 크롭 값(`top -24.13% / left -26.02% / width 161.79% / height 177.78%`)과 형제 조각 좌표를 하나도 고치지 않았다. 실측 상대 위치는 기존 `16.50, 14.46, 53.76, 52.45`(%) 대비 새 `16.46, 14.39, 53.84, 52.58`(%)로 0.13% 안에서 일치한다.
- 새 그림의 가로세로비가 기존보다 1.4% 달라 세로로 그만큼 늘렸다. 눈에 띄지 않는 차이이고, 늘리지 않으면 형제 조각과 접합부가 벌어진다.
- 검증: 애니메이션을 일시정지하고 시각을 직접 옮겨 82ms 간격으로 실측했다. 82ms당 낙하량이 `2.8 → 8 → 13.5 → 18.8 → 24.2 → 29.6 → 35 → 40.4`로 계속 빨라지고, 가로 이동은 `-10.7 → … → -0.2 → 0`으로 한 방향으로만 줄어든다(되돌아오는 구간 없음). 시작·끝 좌표와 배율은 시안 값과 일치한다.

- Figma 개발 페이지 `2187:24701` 섹션의 여섯 장(`loginRewardPopUp01`~`06`)을 한 흐름의 애니메이션으로 구현했다. 시안이 여러 장인 이유가 애니메이션 중간 장면을 그린 것이라, 각 장의 좌표를 이어 재생한다.
- 재생 순서: 팝업이 올라오고(420ms) → 판멍이가 팔을 내리며 동전을 놓고(380ms부터 900ms) → 동전이 그릇으로 떨어지며 작아지고(380ms부터 820ms) → 1200ms에 숫자가 `+10PT`로 바뀌며 한 번 튀고 → 2100ms에 새 합계로 정리되고 하단 안내 문구가 뜬다.
- 좌표·각도·크기는 시안 노드의 `relativeTransform` 실측값이다. 모든 조각을 `transform-origin: 0 0` + `translate() rotate()` 순서로 놓았다. 시안의 변환 행렬이 그 순서라 바꾸면 위치가 어긋난다. 그림의 크롭 비율도 시안 값 그대로라 CSS에 소수점이 남아 있다. `object-fit`으로 흉내 내면 잘리는 위치가 달라진다.
- 로그인과 회원가입이 같은 팝업을 쓴다. 다른 건 둘뿐이다.
  - 로그인: `○○님, 오늘도 어서오세요!` · 50PT에서 시작해 60PT로
  - 회원가입: `○○님, 처음을 환영해요!` · 0PT에서 시작해 10PT로 (막 가입한 참이라 처음부터다)
- 팝업을 띄우는 신호는 `state/loginRewardSignal.ts`가 들고 `LoginRewardHost`가 `useSyncExternalStore`로 읽는다. 라우터 상태로 넘기지 않은 이유는 뒤로가기로 그 기록에 돌아오면 팝업이 다시 뜨고, 상태를 지우려면 effect 안에서 이동을 다시 불러야 하기 때문이다.
- `prefers-reduced-motion: reduce`에서는 떨어지는 연출을 모두 끄고 마지막 장면(팔 내림·동전 그릇 안·합계 표시)만 보여준다.
- 판멍이는 부위별로 나뉘어 있다. 귀·팔·몸통·동전·손 순으로 겹치며, 팔과 손만 움직인다. 시안의 겹침 순서 그대로다.
- 에셋은 `src/assets/auth/`에 넣었다. Figma 원본 PNG 6장 2.4MB를 브라우저 캔버스로 표시 크기의 2배 WebP(품질 0.92)로 구워 129kB로 줄였다. 새 라이브러리는 설치하지 않았다.
- **Figma 레이어 이름도 함께 정리했다.** `구글 로그인`(실제로는 출석 포인트 팝업), `개체`, `mascot_right_arm_layer_v1 1` 같은 이름을 기호 없는 camelCase로 바꿨다 — `rewardPopUp` · `mascotEar` · `mascotHand` · `mascotBase` · `mascotRightArm` · `rewardCoin` · `rewardBowlWrap` · `rewardPointBadge` 등 148개. 프레임 이름도 `loginPopup05`가 둘이던 것을 `loginRewardPopUp01`~`06`으로 정리했다. 팝업 뒤에 비치는 홈 화면 복사본과 공용 버튼 컴포넌트 내부는 다른 담당자 영역이라 건드리지 않았다.
- 로그인 시작 포인트 50PT는 시안 값이고 `DEMO_ACCOUNTS.A.points`(10pt)나 MY 화면과 다르다. `LoginRewardHost`의 `LOGIN_START_POINT` 한 곳에만 있어 확정되면 바꾸면 된다. → 확인 필요
- 검증: `npm run lint`·`npm run typecheck`·`npm run build` 통과. 브라우저에서 로그인 흐름(50 → +10 → 60PT)과 회원가입 흐름(0 → +10 → 10PT), 인사말 두 종류, 시작 장면이 시안 `01`과 맞는지, 안내 문구가 한 줄로 나오는지 확인했다. 실기기는 미검증.

### main 충돌 해결 및 hyomin 통합 (2026-09-14)

- 사용자 요청으로 `origin/main`의 `a0ffae4`를 `hyomin`의 `f25a353`에 병합했다. 수동 충돌은 `PROJECT_CONTEXT.md`와 `FloatingChatButton.tsx` 두 파일이었다.
- 문서는 양쪽 작업 기록을 모두 보존했다. 챗봇 버튼은 hyomin의 구독 플랜 숨김 조건과 main의 로그인·회원가입·가입완료 숨김 조건을 함께 적용했다. main의 80px 아이콘과 로그인 유도·보상 팝업을 유지했다. 최신 결과 비교·서아 댓글 프로필·MY 플랜·광장 변경도 유지했다.
- 빌드(TypeScript 포함)·린트 통과, 기존 500kB 초과 청크 경고 유지. 브라우저에서 결과 근거 펼침과 독립 투표 상태, 결과 화면의 챗봇 표시 및 `/login`·`/signup`·`/my/plan`의 챗봇 숨김을 확인했다. 검사 중 콘솔 오류·경고 없음.
- 자동 병합된 라우트·Provider·세션 연동을 별도로 검토해 이번 병합으로 생긴 회귀는 발견하지 못했다. 기존 main의 챗봇 직접 진입 뒤로가기 기본 경로 누락과 B 계정 로그아웃 후 서아 데모 로그인 표기 불일치는 이번 충돌 수정 범위 밖으로 남긴다.
- 충돌 해결 커밋은 `hyomin`에 올린다. 최종 main 반영은 GitHub PR에서 진행한다.
## 광장 검색바 유리 효과 제거 (2026-09-15)
- 회전하는 연파란 테두리 애니메이션은 유지하고, 검색바 내부의 반투명 그라데이션·백드롭 블러·유리광 내부 그림자를 제거했습니다.
- 검색바 내부는 불투명한 흰색 단색 배경으로 표시됩니다.
## AI 챗봇 첫 진입 판멍이 아이콘 모션 (2026-09-15)
- `판멍이가 도와드릴게요!` 위의 판멍이 얼굴은 유지하고, 블루·하늘색·보라·오렌지 계열의 이중 회전 링과 호흡하는 광원을 추가했습니다.
- 두 링은 서로 반대 방향·다른 속도로 움직이며, 판멍이 얼굴은 2px 범위에서 천천히 떠올라 실시간 AI 대기 상태처럼 보입니다.
- `prefers-reduced-motion` 환경에서는 회전·호흡·부유 모션을 모두 비활성화합니다.
## 광장 검색바 회전선 색상 완화 (2026-09-15)
- 검색바 테두리 회전 애니메이션의 최고 명도를 `blue-300`에서 `blue-200` 수준으로 낮추고, 주변 구간도 연한 중간색으로 연결했습니다.
- 포커스 시 나타나는 파란 외곽광의 불투명도도 함께 낮춰 기존보다 부드럽게 보입니다.
## AI 챗봇 원형 아이콘 배경 보강 (2026-09-15)
- 첫 진입 판멍이 원형 아이콘의 내부 배경을 92% 불투명도의 흰색으로 채워 컬러 파동 위에서도 캐릭터가 흐려 보이지 않게 했습니다.
- 안쪽에 옅은 흰색 경계를 더해 원형 실루엣이 안정적으로 보이도록 했습니다.
## 홈 상단 검색·알림 인터랙션 (2026-09-15)
- 홈 상단 검색·알림 아이콘의 간격을 6px에서 14px로 넓혔습니다.
- 백엔드가 연결되지 않은 알림 버튼은 실제 `disabled` 상태와 30% 불투명도의 연회색 아이콘으로 표시합니다.
- 검색 아이콘을 누르면 네비 내부가 바뀌지 않고 홈 콘텐츠 가운데 상단에 높이 54px의 큰 검색창이 펼쳐지며 자동 포커스됩니다. 네비 검색 아이콘 재클릭, `Escape`, 검색창 닫기 버튼으로 접을 수 있습니다.
- 검색창은 홈 섹션을 아래로 밀지 않는 오버레이이며, 현재 스크롤 위치와 관계없이 상단 네비 바로 아래에 고정됩니다. 열려 있는 동안 홈 스크롤을 잠그고 콘텐츠 영역에 30% 어두운 딤을 적용하며, 딤 클릭으로도 닫을 수 있습니다.
- 지훈(B) 계정에는 검색창 아래에 `외주 잔금`, `수정 요청`, `프리랜서 계약` 최근 검색어 태그가 표시됩니다. 태그를 누르면 해당 검색어의 광장 결과로 바로 이동하며 서아(A) 계정에는 이 영역을 표시하지 않습니다.
- 각 최근 검색어 태그 오른쪽의 `×` 버튼으로 항목을 개별 삭제할 수 있습니다. 삭제 목록은 `sessionStorage`에 저장해 검색창을 닫았다 다시 열어도 현재 브라우저 세션 동안 유지하며, 모든 항목을 지우면 최근 검색어 카드도 숨깁니다.
- 큰 검색창의 돋보기는 상단 네비의 검정 아이콘과 분리한 Primary Blue(`#374BFF`) 전용 아이콘을 사용합니다. 검색창 닫기는 텍스트 `×` 대신 중심 좌표가 정확한 14px SVG를 30px 버튼 한가운데 배치합니다.
- 검색어 입력 후 Enter 또는 필드 안 돋보기를 누르면 `/plaza?section=cases&q=검색어`로 이동하며, 광장 사건 검색창에 같은 검색어와 데모 검색 결과가 즉시 표시됩니다.
- `prefers-reduced-motion: reduce`에서는 검색 필드 등장 모션을 비활성화합니다.
## 홈 투표 CTA 부유 모션 (2026-09-15)
- 오늘의 사건 `투표하러 가기` 버튼이 2.4초 주기로 최대 6px 떠오르고 내려오며, 높이에 따라 오렌지 그림자가 자연스럽게 넓어지는 부유 모션을 적용했습니다.
- 마우스 호버와 키보드 포커스 중에는 현재 위치에서 모션을 멈추고, `prefers-reduced-motion: reduce`에서는 애니메이션을 비활성화합니다.
## 로그인 전 AI 추천 카드 텍스트 정렬 (2026-09-15)
- 홈의 로그인 전 `AI 맞춤 추천` 소형 카드에서 캐릭터 크기·위치는 유지하고 텍스트 묶음만 오른쪽으로 12px 이동해 시각적 중심을 맞췄습니다.
- 로그인 후 추천 섹션에는 영향을 주지 않도록 `.ai-card--button`에만 적용합니다.
- 안내 문구는 첫 줄 `로그인하고`, 둘째 줄 `관심사에 맞는 사건을 찾아보세요!`로 바꾸고 둘째 줄에 기존 파란색 볼드 강조를 유지했습니다.
## 홈 판멍스쿨 배너 정렬 (2026-09-15)
- 한 장으로 합쳐진 판멍스쿨 배너 이미지를 12px 왼쪽으로 이동해 캐릭터와 `대화 조정 갈등 교육`·`판멍 스쿨` 문구가 함께 같은 간격으로 이동하도록 했습니다.
- 오른쪽에 빈 공간이 생기지 않도록 이동 폭만큼 이미지 표시 너비를 보정하고 기존 배너 영역에서 잘라냅니다.
## 공통 하단 네비 스크롤 초기화 (2026-09-15)
- 모든 주요 화면의 공통 하단 네비 메뉴를 누르면 `MainLayout`의 내부 스크롤 영역을 즉시 최상단으로 초기화합니다.
- 다른 메뉴로 이동할 때뿐 아니라 현재 활성 메뉴를 다시 눌렀을 때도 맨 위로 돌아갑니다.
- 비로그인 사용자가 사건 접수 또는 MY를 눌러 로그인 안내 팝업만 열린 경우에는 보고 있던 스크롤 위치를 유지합니다.
## 회원가입 완료 CTA 위치 통일 (2026-09-15)
- 회원가입 페이지의 `회원가입 완료하기` 버튼 하단 간격을 가입 완료 화면 등 다른 고정 CTA와 같은 기준으로 맞췄습니다.
- PC 기기 목업에서는 홈 인디케이터 공간을 포함한 46px, 실제 모바일에서는 22px과 기기 안전 영역을 합친 간격을 사용합니다.
## 홈 상단 계정 전환 버튼 (2026-09-15)
- 홈 상단 네비의 비활성 알림 아이콘을 제거하고, 오른쪽 액션 영역은 검색 아이콘 왼쪽·프로필 사진 오른쪽 순서로 배치했습니다.
- 로그인 상태에서는 오른쪽 끝에 의미가 바로 보이는 현재 계정의 원형 프로필 사진을 28px 크기로 표시합니다. 비로그인 상태에는 계정 전환 버튼을 노출하지 않습니다.
- 버튼을 누르면 MY와 동일한 계정 전환 시트가 열리며 프로필 전환·로그아웃과 토스트 안내도 동일하게 동작합니다.
- 앱 안의 계정 전환 시트에서 윤서아를 선택해도 로그인 상태를 유지하도록 홈과 MY의 전환 처리를 `signIn(nextPersona)`로 통일했습니다. 따라서 지훈에서 서아로 전환한 직후에도 홈 상단에 서아 프로필 사진이 동일하게 표시됩니다. 기기 바깥 시연 패널의 신규·기존 사용자 초기화 동작은 유지합니다.
- 비로그인 홈에서도 검색 오른쪽 자리를 비우지 않고 사진 없는 24px MY 사람 윤곽 아이콘을 표시합니다. 이 아이콘은 계정 전환 대신 MY용 로그인 안내 팝업을 엽니다.
- 비로그인 상태에서는 PC 배경 시연 계정의 서아·지훈 카드 모두 선택 테두리를 표시하지 않습니다. 로그인한 뒤에만 현재 계정 카드가 파랑 또는 오렌지로 활성화됩니다.
- 인증 상태 저장을 `localStorage`에서 `sessionStorage`로 변경해 새 브라우저 세션으로 배포 사이트에 처음 들어오면 항상 비로그인 상태에서 시작합니다. 같은 탭을 새로고침하거나 페이지를 이동하는 동안의 로그인은 유지됩니다.
- 상단 네비의 검색 아이콘과 비로그인 프로필 윤곽 아이콘은 모두 공통 진회색 `neutral-900`(`#252525`)을 사용합니다. 로그인 후 실제 프로필 사진에는 색상 필터를 적용하지 않습니다.
## 결과 페이지 댓글 수정·삭제 (2026-09-15)
- 진행 사건 결과와 지난 사건 결과의 댓글에서 `대댓글 달기` 문구를 제거했습니다.
- 사용자가 화면에서 새로 작성한 본인 댓글에는 점 3개 더보기 버튼이 표시되며, 팝오버에서 `수정`과 `삭제`를 선택할 수 있습니다. 다른 사용자의 시드 댓글에는 작성자 권한이 없으므로 더보기 버튼을 노출하지 않습니다.
- 수정은 댓글 자리에서 바로 입력·취소·저장할 수 있고 저장 후 `수정됨` 상태가 표시됩니다. 삭제하면 댓글 목록과 댓글 수에서 즉시 제거됩니다.
- 댓글 수정의 활성 `저장` 버튼은 공통 CTA와 같은 주황색을 사용하며, 내용이 비어 있을 때는 기존 회색 비활성 상태를 유지합니다.
- 댓글 더보기의 `삭제`를 누르면 즉시 지우지 않고 `댓글을 삭제하시겠습니까?` 확인 팝업을 표시합니다. 주황색 `삭제`를 눌러야 실제로 제거되며 `취소`, 배경 클릭, `Escape`는 댓글을 유지한 채 닫습니다.
## MY 계정별 활동 집계 (2026-09-15)
- 윤서아와 곽지훈 모두 `접수한 사건`과 `배심 참여`를 0건에서 시작합니다. 기존 지훈 계정의 시안용 고정값 4건·24건은 제거했습니다.
- 0건일 때 숫자와 단위를 회색 비활성 상태로 표시하고, 해당 계정으로 사건 접수 또는 투표가 기록되면 1건부터 주황색 활성 상태로 전환해 누적합니다.
- 투표는 같은 사건 ID를 계정별로 한 번만 집계하며, 사건 접수는 새로 완료한 제출 ID마다 한 번씩 집계합니다. 서아·지훈 기록은 서로 분리해 로컬 저장합니다.
- 실제 서버 API가 아직 없어 현재는 `SessionProvider`의 데모 저장소를 집계 원본으로 사용합니다. 새 0건 기준을 바로 확인할 수 있도록 활동 저장 키를 `activity:v2`로 올려 이전 시연 기록과 분리했습니다.
## 광장 최다 투표자 랭킹 (2026-09-15)
- 광장 `최다 투표자` 탭에 명판관 배심원과 다른 1·2·3위 데이터(`오늘도 소신` 1,428표, `한표의 용기` 1,286표, `투표하는 토끼` 1,137표)를 표시합니다.
- 최다 투표자 수치의 단위는 포인트가 아닌 `표`로 마무리하며 패널 제목과 설명도 투표 참여 기준으로 교체합니다.
- 랭킹 탭을 전환하면 패널을 새로 마운트해 광장 최초 진입과 같은 시상대 상승·순위별 팝업·폭죽 등장 애니메이션을 다시 재생하고, 세 트로피 반짝임은 이후에도 계속 반복합니다.
## 광장 사건 페이지네이션 스크롤 (2026-09-15)
- 광장 전체 사건의 페이지 번호와 이전·다음 버튼을 누르면 카드 목록을 바꾼 뒤 기기 내부 스크롤이 `전체 사건` 섹션 상단으로 부드럽게 이동합니다.
- 고정 상단 네비에 제목이 가려지지 않도록 공통 앱 헤더 높이만큼 스크롤 여백을 적용했습니다.
## 서아 사건 접수 완료 CTA (2026-09-15)
- 서아 계정의 사건 접수 완료 화면 하단 버튼을 `홈으로 돌아가기`에서 `접수한 내용 확인하기`로 변경했습니다.
- 버튼을 누르면 홈이 아니라 MY의 `내 사건` 목록(`/my/cases`)으로 바로 이동합니다. 지훈 계정의 기존 홈 이동 CTA와 완료 화면 상단 뒤로가기는 유지합니다.
## 내 사건 결과 상단 홈 이동 (2026-09-15)
- `MY > 내 사건 > 결과 확인하기` 화면의 상단 왼쪽 아이콘은 이전 내 사건 목록으로 돌아가지 않고 항상 홈(`/home`)으로 이동합니다.
## MY 하위 화면 슬라이드 진입 (2026-09-15)
- MY에서 진입하는 `내 사건` 목록·결과 확인 화면과 왈봇 플랜 선택·관리 화면은 오른쪽에서 왼쪽으로 약 340ms 동안 슬라이드되어 들어옵니다.
- 네 화면은 같은 easing과 이동 방향을 사용하며, 운영체제의 `동작 줄이기` 설정이 켜진 경우 진입 애니메이션을 비활성화합니다.
## 사건 접수 선택 버튼 체크 제거 (2026-09-15)
- 사건 접수 첫 단계의 관계 선택과 지훈 추가 질문의 답변 선택에서 선택 후 붙던 체크 이미지를 제거했습니다.
- 선택 여부는 주황색 배경과 흰색 텍스트만으로 표시하며 기존 선택 상태와 다음 단계 활성화 로직은 유지합니다.
## 사건 접수 필수 입력 조건 (2026-09-15)
- `상대와의 관계` 라벨에 `사건 내용`과 동일한 주황색 필수 표시 `*`를 추가했습니다.
- 관계 선택과 사건 내용 입력을 모두 완료해야 하단 `다음` 버튼이 활성화되며, 관계 선택 영역에는 접근성 필수 속성을 함께 적용했습니다.
## 사건 접수 계정별 폼 규격 통일 (2026-09-15)
- 지훈 사건 접수의 `상대와의 관계` 라벨과 선택 버튼 간격을 서아와 같은 16px로 맞췄습니다.
- 지훈의 사진·파일 첨부 영역도 서아 기준으로 통일해 제목은 `사진·파일 첨부(선택)`으로 한 줄에 표시하고, 라벨과 개인정보 도움말의 회색·크기·행간을 동일하게 적용했습니다.
## 사건 접수 상단 네비 통일 (2026-09-15)
- 서아 사건 접수에만 적용되던 3열 그리드와 임시저장 별도 정렬을 제거해, 뒤로가기·제목·임시저장으로 구성된 상단 네비를 지훈 사건 접수의 공통 헤더 규격으로 통일했습니다.
- 서아 전용 진행률과 본문 레이아웃 차이는 그대로 유지합니다.
## 사건 접수 첫 폼 레이아웃 통일 (2026-09-15)
- 동일 디자인인 서아·지훈 사건 접수 첫 폼에 서로 다른 간격을 만들던 서아 전용 `case-submit--writing` 레이아웃 적용을 제거했습니다.
- 두 계정 모두 공통 본문 간격 24px, 텍스트 영역 높이 200px, 동일 진행률·푸터 스타일을 사용하며 예시 내용과 다음 단계 분기만 계정별로 유지합니다.
## 사건 접수 단계바 스크롤·서아 입력 예시 (2026-09-15)
- 서아와 지훈의 사건 작성·추가 질문·요약 확인·접수 화면에서 단계바를 본문 스크롤 영역 안으로 옮겨, 상단에 고정되지 않고 폼과 함께 스크롤되도록 변경했습니다. 상단 사건 접수 헤더와 하단 CTA는 기존처럼 고정됩니다.
- 서아 사건 내용 입력창에 동아리 축제 홍보물 마감 갈등을 반영한 `예)` 플레이스홀더를 추가했습니다.
## 사건 접수 연속 진행 바 (2026-09-15)
- 사건 접수 단계바의 `1 / 3`, `2 / 4` 같은 전체 단계 숫자와 단계별 분할 칸을 제거해 총 단계 수가 화면에 직접 드러나지 않도록 했습니다.
- 현재 단계명과 하나의 연속 진행 바만 표시하며, 각 단계 진입 시 이전 진행 위치에서 현재 위치까지 주황색 바가 440ms 동안 부드럽게 늘어납니다. `동작 줄이기` 설정에서는 모션을 비활성화합니다.
## 사건 내용 입력창 자동 높이 (2026-09-15)
- `내용 작성하기`로 긴 시연 문구를 채우거나 사용자가 직접 입력할 때 사건 내용 입력창이 실제 텍스트 높이만큼 자동으로 늘어납니다.
- textarea 내부 세로 스크롤바는 숨기고, 늘어난 입력 영역은 사건 접수 폼의 본문 스크롤로 자연스럽게 확인하도록 했습니다.
## 사건 접수 완료 판멍이 고화질 교체 (2026-09-15)
- 사건 접수 완료 화면의 판사 판멍이 이미지를 사용자 제공 `Judge.png` 기반 고화질 에셋 `src/assets/submit/panmung-judge-hq.png`로 교체했습니다.
- 컴포넌트의 기존 표시 크기는 유지해 완료 화면 레이아웃에는 영향을 주지 않습니다.
## 사건 접수 완료 진입 인터랙션 (2026-09-15)
- 사건 접수 완료 화면 진입 시 판멍이가 탄성 있게 올라오고, 뒤의 오렌지 완료 링과 체크 배지가 순서대로 나타납니다.
- 완료 문구·접수 내용 카드·안내 문구·하단 CTA가 짧은 간격으로 차례대로 올라와 접수가 마무리됐다는 흐름을 전달하며 전체 모션은 약 1초 안에 끝납니다.
- 운영체제의 `동작 줄이기` 설정에서는 완료 화면의 모든 진입 모션을 비활성화합니다.
## 사건 접수 완료 계정별 위치 통일 (2026-09-15)
- 서아 완료 화면에만 있던 56px 상단 여백을 완료 화면 공통 스타일로 옮겨 지훈과 서아의 판멍이·완료 문구·접수 카드 세로 위치를 동일하게 맞췄습니다.
## 서아 결과 후일담 연결 (2026-09-15)
- 서아 `MY > 내 사건 > 결과 확인하기` 화면의 비활성 `후일담 작성하기` 버튼을 활성화했습니다.
- 버튼을 누르면 왈가왈후 `내 이야기 남기기` 페이지(`/afterstory/mine`)로 이동하며 호버·눌림·키보드 포커스 상태를 제공합니다.
## 후일담 작성 완료 화면 통일 (2026-09-15)
- 후일담 작성 완료 화면에 사건 접수 완료와 같은 56px 상단 완충 영역과 24px 세로 간격을 적용해 판멍이·완료 문구·내용 카드의 세로 위치와 중앙축을 맞췄습니다.
- 동일한 고화질 판사 판멍이를 사용하고 완료 링·체크 배지·문구·내용 카드·안내·CTA가 순차적으로 나타나는 약 1초의 완료 인터랙션을 적용했습니다. `동작 줄이기` 설정에서는 모션을 끕니다.
## 후일담 완료 홈 CTA (2026-09-15)
- 후일담 작성 완료 화면의 하단 버튼을 `후일담 홈으로`에서 `홈으로`로 변경하고 클릭 시 앱 홈(`/home`)으로 이동하도록 수정했습니다.
- 목적지에 맞춰 하단 안내 문구도 `홈에서 다른 사건도 둘러보세요.`로 변경했습니다.
## 후일담 공통 연속 진행 바 (2026-09-15)
- 후일담 작성·게시 확인 화면의 `1 / 2` 숫자와 두 칸 분할 바를 제거하고 사건 접수의 `CaseSubmitProgress` 컴포넌트를 그대로 재사용합니다.
- 작성 화면은 `후일담 작성`, 미리보기 화면은 `게시 확인`으로 표시하며 화면 전환 시 연속 주황 바가 50%에서 100%까지 부드럽게 채워집니다.
- 진행바는 사건 접수와 동일하게 폼 본문 스크롤 영역 안에 있어 내용과 함께 스크롤됩니다.
## MY 활동 수치 이동 연결 (2026-09-15)
- 서아와 지훈 모두 MY 프로필 카드의 `접수한 사건`과 `배심 참여`가 0건일 때는 기존 회색 비활성 상태를 유지하고, 1건 이상이면 주황색 수치와 함께 클릭 가능한 버튼으로 활성화됩니다.
- 활성 `접수한 사건`은 MY의 내 사건 목록(`/my/cases`)으로, 활성 `배심 참여`는 참여한 오늘의 사건 결과(`/cases/case-wedding-gift/result`)로 이동합니다.
## 지훈 월간 플랜 카드 가독성 개선 (2026-09-15)
- 지훈 MY의 월간 왈봇 플랜 카드에서 연한 노란 상세 영역의 저대비 회갈색(`#8D786D`) 본문을 진회색 계열로 변경했습니다.
- 혜택 문구는 `neutral-900` 600, 결제일은 `neutral-800` 500, 관리 링크는 `dark-brown` 700으로 정보 위계를 분리하고 구분선은 연한 오렌지로 바꿔 가독성을 높였습니다.
## MY 활동 수치 호버 색상 제거 (2026-09-15)
- 활성화된 `접수한 사건`과 `배심 참여`의 이동 기능은 유지하되, 마우스 호버 시 라벨이 주황색으로 바뀌던 스타일을 제거해 기본 색상을 그대로 유지합니다.
## 지훈 월간 플랜 텍스트 강도 조정 (2026-09-15)
- 노란 상세 영역의 혜택 문구는 `neutral-900` 600에서 `neutral-700` 500으로 낮춰 검고 두꺼워 보이던 인상을 완화했습니다.
- `다음 결제일` 스타일은 유지하고, `결제 수단 및 내역 관리`의 굵기만 700에서 600으로 한 단계 낮췄습니다.
## 광장 카테고리 검색 정확 필터 (2026-09-15)
- 광장 검색창에 `전체`, `연인`, `친구`, `가족`, `직장`, `학업` 중 하나를 정확히 입력하면 해당 카테고리의 사건만 표시합니다. 앞뒤 공백은 제거한 후 비교합니다.
- 정확 일치 검색은 현재 선택한 보기 기준의 순서를 유지하며, 입력한 카테고리 칩도 활성 상태로 표시합니다. 그 외 검색어는 기존 시연용 혼합 결과 동작을 유지합니다.
## 10년 지기 친구 결혼식 사건번호 통일 (2026-09-15)
- 축의금 사건의 표시 사건번호를 `#CASE-FRIEND-87`로 확정했습니다.
- 라우팅용 내부 사건 ID는 기존 링크 호환성을 위해 변경하지 않았습니다.
## 홈·오늘의 사건 참여 인원 연동 (2026-09-15)
- 홈의 실시간 참여 인원 애니메이션이 진행되는 중 `투표하러 가기`를 누르면 그 순간의 숫자를 사건별 브라우저 세션 저장소에 기록합니다.
- 오늘의 사건 상세 화면은 기존 고정 611명보다 저장된 홈 숫자를 우선 표시하며 천 단위 쉼표를 적용합니다. 홈으로 돌아와도 저장된 숫자부터 최대 1,420명까지 애니메이션을 이어갑니다.
- 실제 백엔드 API가 없는 현재 데모 범위의 세션 연동이며, 서버가 연결되면 같은 읽기·쓰기 지점을 사건 참여 인원 API로 교체해야 합니다.
## 홈 네비 검색창 닫기 아이콘 통일 (2026-09-15)
- 홈 상단 네비에서 펼쳐지는 검색창의 닫기 아이콘을 광장 검색창의 기본 검색 취소 아이콘과 같은 배경 없는 중성 회색 X 형태로 변경했습니다.
- 프로필 닫기 SVG 재사용을 제거하고 CSS 두 선으로 그려 중심을 고정했습니다. 기존 홈 검색창의 30px 클릭 영역과 14px 아이콘 영역은 유지했습니다.
## 광장 검색창 X 아이콘 공통화 정정 (2026-09-15)
- 광장 검색창의 브라우저 기본 검색 취소 아이콘을 숨기고 홈 네비 검색창과 동일한 공통 회색 CSS X를 표시하도록 정정했습니다.
- 두 검색창은 30px 클릭 영역과 14px 아이콘 영역, 선 길이·굵기·색상을 함께 사용합니다. 광장 X는 검색어가 있을 때만 나타나며 클릭 시 검색어를 지우고 입력 포커스를 유지합니다.
## MY 접수한 사건 최대 1건 제한 (2026-09-15)
- 윤서아와 곽지훈의 `접수한 사건` 수치는 계정별로 0건에서 시작하고 첫 접수 후 1건까지만 증가합니다. 0건의 회색 비활성 상태와 1건의 주황색 활성·내 사건 이동은 유지합니다.
- 첫 접수 이후 추가 제출 ID는 활동 집계에 더하지 않으며, 기존 로컬 저장 데이터에 여러 접수 ID가 있어도 복원할 때 첫 1건만 유지합니다. 배심 참여 집계는 기존대로 누적됩니다.
## 로그인 페이지 뷰포트 고정 (2026-09-16)
- 로그인 화면은 앱의 `100dvh` 높이 안에서 상단 64px 헤더를 제외한 남은 영역을 정확히 채우도록 `height: 100%`로 고정했습니다.
- 로그인 경로의 AuthLayout 스크롤 컨테이너는 하단 패딩과 세로 스크롤을 제거해 마우스 휠·터치로 화면이 움직이지 않습니다. 본문에 직접 `100vh`를 더해 헤더 높이만큼 넘치는 방식은 사용하지 않았습니다.
## 로그인 상단 파비콘 로고 적용 (2026-09-16)
- 로그인 상단의 별도 캐릭터 PNG를 사이트 파비콘 원본 `/favicon/favicon.svg`로 교체했습니다.
- 파비콘 SVG 자체에 220/1024 비율의 둥근 모서리 클리핑이 포함되어 있어 로그인 CSS의 별도 `border-radius`와 `object-fit`을 제거했습니다. 기존 표시 크기 62×62px은 유지합니다.

## 로그인 하단 인증 묶음 위치 조정 (2026-09-16)
- 로그인 화면의 하단 여백을 36px에서 56px로 늘려 `서아 계정 로그인`부터 `이메일로 회원가입`까지의 인증 묶음을 20px 위로 올렸습니다.
- 상단 파비콘·서비스 로고·소개 문구의 위치와 인증 요소 사이의 기존 간격 체계는 유지합니다.

## 필수 항목 별표 색상 통일 (2026-09-16)
- 회원가입의 닉네임·이메일·비밀번호·비밀번호 확인 및 이용 동의 앞 필수 별표를 사건 접수와 같은 Primary Blue `#374BFF`로 변경했습니다.
- 사건 접수·후일담·약관 시트의 필수 별표는 이미 Primary 계열이라 기존 색상을 유지합니다.

## 회원가입 닉네임 특수기호 검증 (2026-09-16)
- 닉네임 기본 안내를 `특수기호(!, @, # 등) 없이 적어주세요.`로 구체화했습니다.
- 한글·영문·숫자만 유효하게 처리하며 실제 특수기호 입력 시 `특수기호 없이 적어주세요.` 오류 문구와 접근성 오류 상태를 표시합니다. 한글 조합 중 자모는 허용합니다.

## 오늘의 사건 AI 핵심요약 순차 등장 (2026-09-16)
- 오늘의 사건 상세에서 `AI 핵심요약` 박스가 기기 내부 화면의 중앙 16% 영역을 통과할 때 1·2·3 항목이 약 650ms 간격으로 순차 등장합니다. 브라우저 전체 뷰포트가 아니라 `.app-viewport`를 관찰 기준으로 사용합니다.
- 1번 숫자에서 3번 숫자까지 이어지는 세로선은 1.4초 동안 위에서 아래로 채워지며, 2번과 3번 숫자의 등장 시점에 맞춰 해당 위치를 통과합니다.
- 연결선은 각 항목의 실제 높이에 맞춘 구간으로 분리해 1번 아이콘 위와 3번 아이콘 아래로 선이 삐져나오지 않도록 보정했습니다.
- 인터랙션은 최초 진입 시 한 번만 실행하며, 움직임 줄이기 설정과 IntersectionObserver 미지원 환경에서는 내용을 즉시 표시합니다. 지난 사건 상세에는 적용하지 않습니다.

## 오늘의 사건 투표 선택 인터랙션 (2026-09-16)
- 로그인 후 네 개 투표 선택지를 누르면 카드가 짧게 눌렸다가 탄성 있게 복원되고, 선택 색상과 그림자·오렌지 잔상·캐릭터 팝업·우측 상단 체크가 순차적으로 반응합니다.
- 다른 선택지로 바꾸면 이전 카드는 기본 상태로 부드럽게 복귀하고 새 카드에서 선택 모션이 다시 실행됩니다. 키보드 포커스와 움직임 줄이기 설정도 지원합니다.
- 마우스 호버 상태에서는 카드의 위치·색·테두리를 변경하지 않으며, 시각 반응은 클릭·키보드 포커스·선택 상태에만 적용합니다.

## 주황색 주요 CTA 공통 클릭 모션 (2026-09-16)
- 앱 화면 안에서 폭 160px 이상·높이 38~72px인 Primary Orange `#FF9524` 버튼과 링크를 공통 주요 CTA로 감지합니다. 로그인·회원가입·사건 접수·투표·후일담·플랜 등의 큰 주황 버튼에 동일하게 적용되며 작은 페이지네이션·태그는 제외합니다.
- 누르는 동안 0.97배로 눌리고, 클릭하면 밝기·크기·얕은 링이 280ms 동안 복원된 뒤 기존 클릭 동작이나 내부 화면 이동을 실행합니다. 호버 효과는 없으며 움직임 줄이기 설정에서는 지연과 모션 없이 기존 동작을 즉시 실행합니다.

## 댓글 왈가닥이·왈랑이 스티커 (2026-09-16)
- 제공된 `스티커 아이콘.zip`에서 왈가닥이·왈랑이의 경청·공감·궁금·기본·기쁨·생각 PNG 12종을 `src/assets/case/stickers/`에 추가했습니다.
- 축의금 결과와 지난 사건 결과 댓글 입력창의 기존 웃는 이모지 자동 입력을 캐릭터 스티커 선택창으로 교체했습니다. 현재 계정 캐릭터 탭이 먼저 열리며 두 캐릭터 탭을 자유롭게 전환할 수 있습니다.
- 텍스트와 스티커를 함께 등록하거나 스티커만 단독 등록할 수 있고, 등록 전 미리보기·선택 취소 및 등록 댓글의 실제 스티커 이미지 표시를 지원합니다. 비로그인 지난 사건에서는 기존 로그인 안내를 먼저 유지합니다.

## 축의금 결과 AI 판결 제목 줄바꿈 보존 (2026-09-16)
- `aiVerdictTitle` 데이터의 `\n`이 일반 공백으로 합쳐지던 원인은 `.ai-verdict__card h3`의 기본 `white-space: normal` 동작이었습니다.
- 제목에 `white-space: pre-line`을 적용해 `10만 원은 통상적인 축의금이고,` 뒤의 지정 줄바꿈이 화면에 반영되도록 수정했습니다.

## 더미 댓글 캐릭터 스티커 배치 (2026-09-16)
- 축의금 결과 더미 댓글 2개에 왈랑이 공감·왈가닥이 생각 스티커를, 지난 사건 결과 더미 댓글에 왈랑이 경청 스티커와 왈가닥이 기쁨 단독 댓글을 배치했습니다.
- 새로고침마다 댓글 구성이 바뀌지 않도록 실제 난수 대신 자연스럽게 흩어진 고정 시연 데이터로 구성했습니다.

## AI 판결 문구 수동 줄바꿈 (2026-09-16)
- 결과 페이지의 AI 판결 제목뿐 아니라 근거 본문에도 `white-space: pre-line`을 적용해 데이터 문자열의 `\n`이 실제 줄바꿈으로 표시되도록 했습니다.

## 댓글 스티커 선택창 닫기 (2026-09-16)
- 스티커를 선택하지 않아도 선택창 바깥 영역을 클릭하거나 Escape 키를 누르면 선택창이 닫히도록 했습니다.

## AI 판결 근거 강제 줄바꿈 (2026-09-16)
- AI 판결 근거 문자열의 `\n`을 렌더링 시 실제 `<br>` 요소로 변환해, 브라우저의 공백 처리나 CSS 우선순위와 무관하게 지정한 위치에서 줄바꿈되도록 했습니다.

## 결과 페이지 제목-카드 간격 통일 (2026-09-16)
- `1심 · 판멍이의 판단`과 `판멍이가 주목한 점` 제목에 동일한 좌우 패딩 8px을 적용하고, 두 카드의 상단 간격도 동일한 10px 규칙으로 묶었습니다. 첫 섹션에만 있던 제목 고정 높이와 별도 세로 정렬은 제거했습니다.

## 홈 → 오늘의 사건 화면 전환 (2026-09-16)
- 홈의 `투표하러 가기`로 진입할 때 오늘의 사건 화면 헤더와 본문이 오른쪽에서 왼쪽으로 340ms 동안 들어오는 모바일식 전환을 적용했습니다.
- 하단 내비게이션은 고정되며, 모션 최소화 환경에서는 애니메이션을 생략합니다.

## 홈 배심원 수 오도미터 모션 (2026-09-16)
- 배심원 참여 수가 갱신될 때 이전 숫자와 결과 숫자만 교체하지 않고, 각 자릿수가 사이 숫자를 순서대로 지나 최종 값에 도착하는 세로 오도미터 모션으로 변경했습니다.
- 증가량에 따라 이동하는 숫자 칸 수와 재생 시간이 달라지며, 올림이 발생하는 십·백 자리도 함께 굴러갑니다.

## 오늘의 사건 결과 상단 화살표 (2026-09-16)
- 오늘의 사건 투표 결과 페이지의 상단 왼쪽 화살표는 브라우저 이전 기록 대신 홈으로 바로 이동합니다. 투표 전 사건 상세와 지난 사건 화면의 기존 뒤로가기는 유지합니다.

## 홈 배심원 수 멈춤 조건 수정 (2026-09-16)
- 저장된 시연 숫자가 상한 1,420명에 도달한 상태로 홈을 다시 열면 기본 참여 수부터 실시간 집계를 다시 시작하도록 했습니다.
- 모션 최소화 설정에서도 숫자 집계 자체는 계속되며, CSS 애니메이션만 생략됩니다.

## 결과 페이지 → 홈 스크롤 초기화 (2026-09-16)
- 오늘의 사건 결과 상단 화살표로 홈에 이동할 때 `MainLayout`의 공통 내부 스크롤을 먼저 0으로 초기화해 홈 최상단에서 시작하도록 했습니다.

## 댓글 페이지네이션 스크롤 이동 (2026-09-16)
- 오늘의 사건 결과와 지난 사건 결과에서 댓글 페이지 번호·이전·다음 버튼을 누르면 댓글 목록을 교체한 뒤 댓글 섹션 시작 지점으로 부드럽게 스크롤합니다.

## 결과 페이지별 더미 댓글 확장 (2026-09-16)
- 기존 5개 댓글을 반복 복제하던 방식을 제거하고, 축의금 사건은 관계·참석·금액 기준을 조합한 고유 댓글을 생성합니다.
- 지난 직장 사건은 수정 횟수·요청 반영·잔금·계약 범위를 조합한 고유 댓글을 생성하며, 두 사건 모두 작성자·시간·반응 수와 일부 캐릭터 스티커를 분산했습니다.

## 댓글 페이지네이션 5페이지 제한 (2026-09-16)
- 결과 페이지 댓글은 페이지당 5개, 최대 5페이지(화면 노출 25개)로 제한했습니다.
- 5페이지에서는 공통 `Pagination`의 마지막 페이지 상태에 따라 오른쪽 화살표가 회색 비활성 상태가 되며, 댓글을 추가해도 6페이지는 생성되지 않습니다.

## 더미 댓글 상대 시간 표기 (2026-09-16)
- 생성 댓글은 59분까지 `분 전`으로 표시하고, 60분부터는 경과한 시간 기준의 `1시간 전`, `2시간 전`, `3시간 전` 형식으로 자동 전환합니다.

## 홈 판멍스쿨 배너 위치 (2026-09-16)
- 공통 `AdBanner`의 판멍스쿨 이미지를 최초 위치에서 왼쪽으로 총 13px 이동하고, 오른쪽에 빈틈이 생기지 않도록 이미지 폭도 같은 만큼 보정했습니다.

## 로그인 후 AI 맞춤추천 도입 문구 (2026-09-16)
- 서아·지훈 공통 추천 카드에서 `관심사에 맞는 사건을 AI챗봇 판멍이가 추천해드려요.` 보조 문구를 제거했습니다.
- 이후 `내 고민과 닮은 사건을 찾았어요!` 제목도 제거하고, 카드 맨 위에 계정별 `서아님/지훈님 맞춤 관계 갈등 사건 9건을 추천해요.` 문구를 배치했습니다.
- 판멍이 아이콘은 맞춤 추천 문구 바로 아래 가운데에 배치했습니다.

## AI 추천 일치·불일치 태그 정렬 (2026-09-16)
- 로그인 후 추천 사건 행의 불필요한 오른쪽 28px 패딩을 제거해 `AI와 배심원 의견 일치/불일치` 태그의 오른쪽 끝을 목록 기준선에 맞췄습니다.

## AI 추천 카드 내부 간격·더보기 정리 (2026-09-16)
- 사건 키워드·의견 일치 태그 행과 사건 제목 사이 간격을 9px에서 16px로 넓혔습니다.
- 목적지가 정해지지 않은 `관련 사건 더보기+` 버튼은 로그인 후 추천 카드에서 제거했습니다.
- 추천 사건 구분선 아래 다음 카드까지의 상단 여백은 20px에서 12px로 줄였습니다.

## AI 맞춤 추천 실시간 상태 모션 (2026-09-16)
- 큰 알약 형태를 16px 라운드의 컴팩트 AI 분석 카드로 바꾸고, 별도 상태 라벨 없이 계정별 추천 결과만 표시합니다.
- 바깥 테두리에는 연한 파란 포인트가 순환하며, 별도의 상태 점은 표시하지 않습니다.
- 판멍이 이미지는 118×100px로 키워 카드 아래 중앙에 배치하고, 약한 그림자 후광과 상하 부유 모션을 적용했습니다. 모션 최소화 환경에서는 모든 반복 모션이 정지합니다.
- AI 분석 카드와 판멍이는 홈 배경 위에 직접 배치하고, 추천 사건 3개 목록만 별도의 흰색 카드로 묶었습니다.
- AI 분석 카드와 판멍이 사이는 14px, 판멍이 영역과 추천 사건 흰 카드는 20px로 두어 두 간격의 위계를 구분했습니다.
- 계정별 관심사를 분리해 서아는 관계·학업 사건 3개, 지훈은 의뢰인 잔금·직장 내 성과·퇴근 후 업무 지시 관련 `직장 갈등 사건` 3개를 표시합니다. 광장 필터에 없는 `금전` 카테고리는 사용하지 않습니다.
- 지훈 추천 사건 제목은 `잔금 지급을 미루면서`, `자신의 공로인 것처럼`, `단체 대화방으로 온 업무 지시`처럼 의미와 조사가 자연스럽게 이어지도록 문장을 다듬었습니다.

## 잔금·원본 파일 사건 의견 상태 정합성 (2026-09-16)
- 광장 `case-company-874`의 카드 내용을 잔금 사건으로 교체할 때 이전 카드의 `isVerdictAligned: true`가 남아 상세 결과와 어긋나 있었습니다.
- 상세 결과의 `AI는 글쓴이 / 배심원 다수는 상대방` 판정에 맞춰 광장 상태를 `false`로 수정해 `AI와 배심원 의견 불일치`로 표시합니다.

## 밸런스 게임 다시 하기 노출 시점 (2026-09-16)
- 섹션 제목 옆 재시작 아이콘은 제거하고, 결과 애니메이션이 끝난 뒤 선택 결과 말풍선 바로 아래에만 표시합니다.
- 버튼 외형과 텍스트 없이 기존 새로고침 아이콘만 30px로 표시하되, 실제 클릭 영역은 44×44px로 확보했습니다. 선택 전 화면과 게임 전환 동작은 변경하지 않았습니다.
- 우측 게임 전환은 분리된 `1/2 + 다음` 텍스트 대신 기존 pill·페이지네이션 형태를 응용한 단일 컨트롤로 묶었습니다. 현재 순서 칩, `다른 게임` 라벨, 화살표가 함께 표시됩니다.
- 밸런스 게임과 막상막하는 동일한 `CompactSwitchButton`을 공유합니다. 막상막하의 큰 상하 화살표는 제거하고 `현재 순서 + 다른 사건 + 6×10px 화살표`로 통일했습니다.

## 왈가왈후 편지 열림 속도 조정 (2026-09-16)
- 봉투 전환 → 편지지 상승 → 내용 노출 순서는 유지하면서 전체 완료 시간을 약 0.96초에서 1.2초로 늘렸습니다.
- 편지지는 680ms 동안 올라오고, 내용은 620ms 뒤부터 480ms 동안 나타나도록 단계 사이의 여유를 늘렸습니다.

## 로그인 전 AI 맞춤추천 순환 보더 (2026-09-16)
- 비로그인 AI 맞춤추천 카드 테두리에도 로그인 후 추천 박스와 같은 3.2초 순환 모션을 적용했습니다.
- 테두리는 Primary Blue 계열만 사용하며 내부 흰 배경·레이아웃은 유지합니다. 모션 최소화 환경에서는 정지된 파란 테두리로 표시합니다.

## 광장 사건 상세 뒤로가기 위치 복원 (2026-09-16)
- 광장 사건 카드를 열기 직전의 보기 기준·카테고리·검색어·페이지와 기기 내부 스크롤 위치를 세션에 보관합니다.
- 사건 상세 상단의 뒤로가기로 광장에 복귀하면 눌렀던 카드가 있던 위치를 그대로 복원하며, 복원 정보는 한 번 사용한 뒤 제거해 하단 내비게이션의 새 광장 진입에는 영향을 주지 않습니다.

## 시연 사건 시간·메타데이터 정합성 (2026-09-16)
- 축의금 사건의 `26/09/08 00:08:34 · 7시간 전`을 근거로 앱 내부 기준 시각을 `2026-09-08 07:08:34 KST`로 명시했습니다. 이에 따라 `26/09/05 21:13`에 작성된 잔금 사건은 약 58시간 전이므로 `2일 전`으로 바로잡았습니다.
- 광장 잔금 사건의 제목·요약·댓글 수는 상세 데이터에서 직접 가져오도록 연결했습니다. 제목은 `수정 2회를 마쳤는데, 의뢰인이 잔금 지급을 미루고 있어요.`로 통일했습니다.
- 결과 댓글은 5개×5페이지 구조에 맞춰 두 사건 모두 25개로 통일했습니다. 축의금 사건의 기본 참여 수도 홈과 상세가 같은 1,245명에서 시작하도록 한 곳의 값을 공유합니다.
- 잔금 사건에서 원본 파일 제공이 계약에 포함됐다는 본문과 AI가 계약 밖 요구라고 판단하던 모순을 제거하고, 축의금 사건의 AI·배심원 비교 문구도 실제 투표 우세 방향과 맞췄습니다.

## 지훈 AI 맞춤 추천 사건 연결 (2026-09-16)
- 지훈 로그인 후 AI 맞춤 추천의 `수정 2회를 마쳤는데, 의뢰인이 잔금 지급을 미루고 있어요.` 카드 전체를 누르면 해당 지난 사건 상세 `/cases/case-company-874`로 이동합니다.
- 나머지 추천 카드는 목적지가 확정되지 않아 기존 정적 표시를 유지하며, 연결된 카드에는 키보드 포커스 표시와 구체적인 접근성 이름을 제공합니다.
- 추천 카드 안에서는 `수정 2회를 마쳤는데, 의뢰인이 잔금 지급을 / 미루고 있어요.`로 줄을 나누고 해당 카드 제목의 글자 크기·자간을 미세 조정해 첫 줄이 들어가도록 했습니다. 상세·광장의 줄바꿈은 변경하지 않습니다.

## 사건 상세 AI 핵심요약 공통 모션 (2026-09-16)
- 오늘의 사건과 지난 사건의 AI 핵심요약을 `AiSummary` 공통 컴포넌트로 묶었습니다.
- 두 페이지 모두 사용자가 기기 내부를 스크롤해 요약 박스가 화면 중앙 부근에 도달하면 숫자 1→2→3, 내용, 연결선이 기존 순서대로 등장합니다. 이후 요약 컴포넌트의 동작 변경은 두 페이지에 함께 적용됩니다.

## 고정 하단 버튼과 스크롤 본문 간격 (2026-09-16)
- 사건 접수 공통 푸터는 상단 패딩이 0이어서 스크롤 본문과 버튼 영역이 바로 맞닿았습니다. 상단 16px, 본문 하단 24px로 조정해 마지막 항목이 경계에 붙거나 잘린 듯 보이지 않게 했습니다.
- 같은 푸터를 쓰는 서아·지훈 사건 접수 전 단계와 왈봇 플랜에 함께 적용됩니다. 회원가입·후일담의 고정 버튼 영역은 이미 상단 패딩을 사용하고 있어 변경하지 않았습니다.

## 사건 상세 AI 핵심요약 순환 보더 (2026-09-16)
- 공통 `AiSummary` 카드에 로그인 전 AI 추천 박스와 같은 파란색 계열의 3.2초 순환 테두리를 적용했습니다. 내부 흰 배경과 1→2→3 등장 모션은 유지합니다.
- 오늘의 사건과 지난 사건 상세에 함께 반영되며, 움직임 줄이기 설정에서는 정지된 파란 테두리로 표시합니다.

## 사건 접수·후일담 안내 왈랑이 에셋 통일 (2026-09-16)
- 파란 몸통 바깥에 검정 외곽선이 들어간 구버전 `imgCharacterWalangJoy.svg` 사용처를 검정 몸통 외곽선이 없는 최신 `wallang-joy.png`로 교체했습니다.
- 사건 접수 첫 단계·지훈 추가 질문 단계의 안내 아이콘, 후일담 작성의 개인정보 안내 아이콘에 함께 적용됩니다. 다른 캐릭터/아바타 에셋은 변경하지 않았습니다.

## 내 사건 결과의 비활성 댓글 버튼 (2026-09-16)
- 서아 내 사건 `결과 확인하기`의 `배심원 의견 댓글 보기`는 여전히 기능이 없는 비활성 버튼입니다. `disabled` 상태에 회색 배경·회색 글씨·비활성 커서를 적용해 눌러지는 버튼으로 보이지 않게 했습니다.

## 하단 주요 버튼 위치 상향 (2026-09-17)
- 사건 접수 전 단계와 접수 완료, 같은 공통 푸터를 쓰는 왈봇 플랜, 후일담 작성·게시 확인·완료, 회원가입·가입 완료의 하단 주요 버튼을 16px 위로 올렸습니다.
- 버튼 크기와 상단 간격은 유지하고 하단 여백을 늘렸습니다. 목업의 홈 인디케이터 자리와 실제 모바일 안전 영역은 계속 확보합니다.
- `npm run build`와 `git diff --check`를 통과했습니다.

## 두 사건 결과 카드 영상 교체 (2026-09-17)
- 오늘의 사건(축의금)과 배심원 광장의 부모님 간섭 사건이 공유하는 1심 판결 카드의 정지 이미지를 사용자 제공 `panmung-scale-v2.mp4`에서 만든 `panmung-scale-once.mp4`로 교체했습니다.
- 카드 비율·둥근 모서리·위에 얹힌 사건별 문구는 유지합니다. 원본의 앞 1초와 끝 0.5초를 제거한 약 3.28초 영상이 결과 페이지 진입 1초 뒤 0.8배속·음소거로 한 번 재생됩니다. 대기 화면은 잘린 영상의 첫 프레임이고 재생 시작 시 240ms 동안 자연스럽게 전환합니다. 영상 끝 약 0.8초 전 판멍이 머리 오른쪽 위에 `이쪽!` 말풍선이 나타나며, 재생 완료 시 새 마지막 프레임과 문구를 유지합니다.
- 지훈 지난 사건의 별도 판결 이미지는 그대로 유지합니다. `npm run build`를 통과했습니다.

## 판결 영상 문구 위치 조정 (2026-09-17)
- 짧은 강조 문구의 말풍선은 분리된 삼각형 꼬리 대신 몸통과 꼬리가 한 번에 이어진 SVG 윤곽선으로 정리했습니다. 영상 카드 높이 기준 55%·너비 기준 65%에 두어 꼬리가 판멍이 머리 오른쪽 위를 가리킵니다.

## 광장 나머지 18개 사건 상세 연결 (2026-09-17)
- 전체 20개 카드가 각각 상세 화면으로 이동합니다. 기존 부모님·잔금 사건을 제외한 18개에는 카드 제목에 맞춘 사연, 확인할 점, AI 판단, 댓글을 추가했습니다.
- 진행 사건은 투표 후 결과 화면에서 집계 중 상태를 보여주고, 해결 사건은 배심원 결과와 AI 의견의 일치 여부를 보여줍니다. 댓글 초기 개수는 카드 숫자와 같으며, 새 댓글은 브라우저 세션 동안 유지되어 목록 숫자에도 반영됩니다.
- 최신·진행·해결·추천 목록은 같은 게시 순서를 기준으로 정렬합니다. 검색은 사건 제목·요약·분야를 실제로 대조합니다. 새 사건의 상대 시각과 작성 시각은 기존 시연 기준인 2026-09-08 07:08 KST에 맞췄습니다.
- 20개 목록 ID, 18개 추가 상세, 댓글 수, 판결 일치 상태를 확인했고 빌드와 수정 파일 린트를 통과했습니다.

## 광장 사건 결과 화면 공통화 (2026-09-17)
- 18개 추가 사건의 별도 결과 화면을 제거하고 오늘의 사건 결과 화면을 공유하도록 연결했습니다. 기존 판결 영상 카드, AI 근거 카드, 댓글 UI를 그대로 사용합니다.
- AI 1심 판단은 글쓴이 편과 상대방 편으로 정리하고, 각 사건의 카드 판결 일치 여부와 2심 다수 의견을 맞췄습니다. 진행 사건은 집계 중, 해결 사건은 최종 배심원 결과를 표시합니다.
- 진행 중인 여행 경비 사건의 상대방 편 1심 결과는 제공받은 `panmung-scale-left.mp4`에서 앞 1초와 끝 0.5초를 자른 별도 영상을 사용합니다. 첫 화면도 이 영상에서 추출해 재생 전 카드에 표시합니다. 재생 지연 1초·0.8배속·한 번 재생 후 마지막 장면 유지 규칙은 글쓴이 편 영상과 같습니다.
- 판결 영상 종료 0.8초 전부터 `이쪽!` 말풍선을 다시 보여줍니다. 글쓴이 편은 판멍이 머리 오른쪽, 상대방 편은 왼쪽에 놓고 꼬리와 강조색을 좌우에 맞춥니다. 영상이 끝난 뒤에도 말풍선을 유지합니다.
- 말풍선이 글자에 비해 커 보인다는 피드백을 반영해 외곽을 112×62px에서 84×48px로 줄이고 꼬리와 그림자도 함께 다듬었습니다.
- 2심이 끝난 광장 사건은 기존 지난 사건의 `ResultBreakdown` 컴포넌트를 공유합니다. 가장 많이 선택된 입장을 맨 위에 두고, 기존 노란 제목 영역·펼침 동작·스크롤 시 막대 애니메이션을 그대로 사용합니다. 투표 중인 사건은 집계 중 상태를 유지합니다.

## 광장 해결 사건별 2심 배심원 투표 (2026-09-17)
- 추가된 해결 사건 10개의 2심 투표 수를 사건별로 정했습니다. 네 선택지의 득표 수에서 표시 비율을 계산하고, 정수 비율 합계는 항상 100%가 되도록 배분합니다.
- 사건별 최다 득표 선택지가 카드의 AI·배심원 일치/불일치 상태와 맞습니다. 최다 득표율은 표시상 모두 50%를 넘고, 상세 화면의 참여 인원은 실제 네 선택지 득표 합계와 같습니다.
- 댓글의 투표 입장은 해당 사건의 득표 분포를 따라 생성합니다. AI와 배심원 판단이 같은 사건은 판단 비교 카드의 설명도 같은 쟁점을 봤다는 흐름으로 표시합니다. 기존 잔금 사건의 고유 결과와 진행 중 사건의 집계 중 상태는 그대로 사용합니다.

## 댓글 페이지네이션 중복 제거 (2026-09-17)
- 광장에 추가한 18개 사건의 시연 댓글이 서로 같은 본문을 반복하던 생성 방식을 수정했습니다. 각 댓글은 사건별 확인 사실·투표 입장·서로 다른 논점을 조합해 작성합니다.
- 오늘의 사건, 부모님 사건, 잔금 사건, 광장 추가 사건, 후일담을 포함한 댓글 화면 27곳의 688개 댓글을 최신순·등록순과 모든 페이지 기준으로 검사해 동일 본문과 중복 ID가 없음을 확인했습니다.

## 사건 결과에서 광장 위치 복원 (2026-09-17)
- 광장에서 들어온 사건의 결과 화면 상단 이전 버튼은 사건 상세를 거치지 않고 광장으로 바로 돌아갑니다. 사건을 열 당시의 보기 기준·카테고리·검색어·페이지·스크롤 위치를 복원합니다.
- 지난 사건과 진행 사건 모두 상세→결과 이동에 광장 출발 상태를 전달합니다. 브라우저 뒤로가기는 실제 광장으로 돌아갈 때만 광장 복원 상태를 활성화합니다.
- 광장 복원 상태는 렌더 중 읽어도 바로 지우지 않고, 광장 화면이 값을 받은 뒤 정리합니다. 개발 환경의 중복 렌더에서도 선택했던 필터가 초기화되지 않습니다.

## 지난 사건 판단 비교의 AI 입장 통일 (2026-09-17)
- `판단이 엇갈렸어요` 레이아웃을 쓰는 해결 사건에서 판멍이 1심은 모두 글쓴이 편입니다. 기존에 상대방 편이던 여행 취소, 가족 생활비, AI 과제 사건의 1심을 수정했습니다.
- 세 사건의 배심원 최다 선택은 각각 상대방 편·양쪽 모두·양쪽 모두로 조정해 목록의 AI·배심원 불일치 태그를 유지합니다. 2심 비율과 상세 참여 인원은 같은 득표 수에서 계산하며 댓글 입장 분포도 새 결과와 맞춥니다.

## 광장 추가 사건 작성자 프로필 색상 (2026-09-17)
- 광장에 추가한 18개 사건 상세의 작성자 프로필은 댓글과 같은 컬러 원형 아바타를 사용합니다. 기존 확대 크롭 때문에 검정 얼굴만 보이던 표시를 원본 전체 표시로 바꿨습니다.
- 이후 사용자 제공 SVG 8종으로 작성자·댓글 프로필을 전면 교체해, 확대 크롭 규칙은 제거했습니다.

## 사용자 제공 프로필 SVG 8종 적용 (2026-09-17)
- `blue01/02`, `green01/02`, `orange01/02`, `pink01/02`를 `src/assets/profile-icons`에 복사하고 `profileAvatars.ts`에서 관리합니다.
- 사건 상세 작성자, 사건 결과·광장 댓글, 후일담 작성자·댓글, 시연 계정의 익명 댓글 프로필, 직접 가입 계정의 캐릭터 프로필에 새 SVG를 적용했습니다. 실제 인물 사진을 쓰는 시연 계정 전환·MY·홈 프로필은 유지합니다.
- 48×48 원본을 기존 슬롯 크기(댓글 32px, 사건 작성자 38px, 후일담 작성자 36px, 직접 가입 MY 48px 등)에 맞춰 축소하며, 이전 아바타용 확대 크롭은 제거했습니다.

## 프로필 형태 원형 통일 (2026-09-17)
- 새 SVG의 배경이 네모로 드러나지 않도록 사건 작성자·결과 댓글·후일담 작성자 프로필을 원형으로 잘라 표시합니다.
- 직접 가입 계정의 MY 카드·홈 상단·계정 전환 시트에서도 원형 마스크를 유지합니다. 프로필 크기와 본문 배치는 그대로입니다.

## 접수·후일담 완료 화면 공통화 (2026-09-17)
- 두 완료 화면의 판멍이, 완료 제목, 아래에서 올라오는 폴더, 공개 범위 안내를 `CompletionScene`으로 공유합니다. 각 화면의 헤더와 하단 이동 버튼은 기존 목적지를 유지합니다.
- 후일담 완료 폴더에는 작성한 후일담 제목과 `배심원 광장에 공개` 상태를 표시합니다. 게시 확인 화면의 연결 사건 폴더와 작성 화면의 사건 카드는 변경하지 않았습니다.
- 린트·빌드를 통과했고, 후일담 완료 화면에서 폴더와 하단 버튼 배치를 확인했습니다.

## 후일담 게시 확인 편지 미리보기 (2026-09-17)
- 게시 확인 화면의 연결 사건 폴더를 누르면 기기 내부가 어두워지고 기존 후일담 상세의 편지지 에셋 위에 방금 작성한 제목·본문이 아래에서 올라옵니다. 긴 본문은 편지지 안에서 스크롤합니다.
- 편지 바깥, 닫기 버튼, Escape로 닫습니다. 키보드로 폴더를 열 수 있으며 닫은 뒤 폴더로 초점이 돌아갑니다. 배경은 팝업 동안 조작되지 않습니다.
- 예시 후일담을 작성 화면에서 채워 게시 확인으로 이동한 뒤 열기·바깥 클릭·Enter·Escape 동작과 기기 상태바까지 어두워지는 배치를 확인했습니다.
- 작성 화면의 예시 채우기는 `afterStoryLetter.lines`를 그대로 사용합니다. 노트북 고장과 이후 대화까지 포함한 최신 후일담 문구가 작성 입력창과 편지 미리보기에 동일하게 표시되는 것을 확인했습니다.
- 폴더와 공개 안내 박스 사이에 밸런스 게임의 손가락 아이콘과 `파일을 누르면 후일담을 미리 볼 수 있어요.` 안내를 추가했습니다. 린트·빌드와 diff 검사를 통과했습니다.
- 처음 전달된 스토어 배지는 잘못 보낸 파일이라 제거하고, 새로 전달된 `icon-park_click-tap.svg`(24×24)로 교체했습니다. 아이콘은 3초 주기로 손가락이 두 번 짧게 누르고 터치 지점에 파란 파동이 생긴 뒤 쉬는 동작을 반복합니다. 안내 버튼은 폴더와 같은 최대 너비에서 가운데 정렬합니다. 폴더와 안내 문구 모두 편지 미리보기를 열며, 움직임 줄이기 설정에서는 반복 애니메이션을 끕니다. 안내 버튼 클릭과 린트·빌드를 확인했습니다.
- 후일담 작성 예시와 게시 확인 편지 미리보기에서 `아직 가끔 어색하지만, 예전보다...`가 한 문장으로 바로 이어집니다. 좁은 화면에서 `가끔`과 `어색하지만`이 갈라지지 않도록 그 사이에 줄바꿈 방지 공백을 넣고, `어색하지만,` 뒤의 강제 줄바꿈을 제거했습니다. 실제 작성→게시 확인→폴더 열기에서 확인했습니다.
- 편지 미리보기의 글꼴 문자 `×`를 계정 전환 팝업에서 쓰는 대칭 SVG로 교체했습니다. 두 화면의 X를 공통 `IconCloseButton`으로 연결해 32px 버튼 안에 24px 아이콘을 중앙 배치합니다. 편지 팝업에서 시각적 정렬과 닫기 동작을 확인했습니다.
- 팝업 닫기 X 사용처를 점검해 편지 미리보기, 계정 전환, 프로필 전환 확인, 로그인 안내, 출석 보상, 회원가입 동의, 홈 검색 패널을 공통 `IconCloseButton`으로 연결했습니다. 32px 원형 연회색 배경과 중앙 24px 대칭 SVG를 사용하며, 편지 미리보기에서 원형 배경 표시를 확인했습니다.
- 후일담·광장 검색어 지우기, 회원가입 비밀번호 지우기, 최근 검색어 삭제, 결과·댓글 스티커 삭제의 X는 닫기 버튼이 아닌 해당 항목 삭제 기능이므로 기존 표시를 유지합니다.
- 공통 닫기 X의 선 색을 진한 `#252525`에서 한 단계 부드러운 `#59565B`(neutral-700)로 낮췄습니다. 원형 배경과 아이콘 크기·두께는 그대로입니다.

## 광장 랭킹 전체보기 안내 (2026-09-17)
- 광장의 `랭킹 전체 보기` 버튼을 누르면 앱의 공통 토스트로 `랭킹 전체보기는 업데이트 예정입니다.`를 표시합니다. 전체 랭킹 화면이 없는 시연 범위와 일치하며, 버튼의 비활성 ARIA 표시는 제거했습니다.
- 린트·빌드·diff 검사 통과. 광장 화면에서 버튼 클릭 후 안내 문구가 공통 토스트로 나타나는 것을 확인했습니다.
- 후일담 작성 입력창의 잔금 지급(지훈 사건) 예시 문구를 서아의 팀원 공개 지적 사건에 맞는 연락·사과·마감 공유 예시로 바꿨습니다. 실제 예시 채우기와 편지 미리보기의 본문은 기존 서아 사건 흐름을 유지합니다.

## 사건 접수·후일담 예시 입력 토글 (2026-09-17)
- 사건 접수의 사건 작성, 지훈 추가 질문, 공개 범위와 후일담 작성에 있는 `예시 한번에 채우기` 버튼을 다시 누르면 각 단계의 예시 값이 해제됩니다. 선택되지 않은 초기 상태로 돌아가며 필수값이 비면 다음 버튼도 다시 비활성화됩니다.
- 사건 본문의 `내용 작성하기`도 예시 내용이 들어간 상태에서는 `예시 내용 지우기`로 바뀌어 다시 누르면 본문만 비웁니다. 첨부파일은 유지합니다.
- 질문 단계의 버튼은 세 답이 단순히 채워졌다는 이유만으로 취소 상태가 되지 않고, 실제 예시 답과 모두 같을 때만 취소 상태를 표시합니다.
- 린트·빌드와 diff 검사를 통과했습니다.

## 오늘의 사건에서 비슷한 후일담으로 이동 (2026-09-17)
- 오늘의 사건 결과 하단 `비슷한 사건의 후일담` 카드 전체를 누르면 `늦게라도 마음을 전하고, 친구와 오해를 풀었어요.` 후일담 상세로 이동합니다.
- 후일담 상세의 상단 이전 버튼은 오늘의 사건 결과로 돌아가며, 카드를 누르기 전 기기 내부 스크롤 위치와 결과 화면 이동 상태를 복원합니다. 다른 경로에서 후일담 상세로 들어온 경우의 기존 이전 동작은 유지합니다.
- 린트·빌드·diff 검사를 통과했습니다. 시연 로그인 상태에서 카드 클릭으로 해당 후일담에 진입하고 상단 이전 버튼으로 오늘의 사건의 카드 위치에 돌아오는 것을 확인했습니다.

## 사건 재방문 시 중복 투표 방지 (2026-09-17)
- 시연 계정별 투표 기록에 사건 ID와 선택한 입장을 함께 저장합니다. 같은 사건은 재진입·새로고침 후에도 다시 투표할 수 없습니다. 기존에 사건 ID만 저장된 기록도 투표 완료로 인정하되 알 수 없는 선택지는 추측해 표시하지 않습니다.
- 진행 중 사건 상세의 사건 내용과 AI 요약은 유지하고, 하단 투표 선택지 대신 `이미 투표했어요`·나의 선택·`투표 결과 보기` 영역을 보여줍니다. 완료 사건의 기존 결과 링크는 유지합니다.
- 결과 화면에서 직접 댓글을 남기는 경우에도 저장된 입장을 사용합니다. 린트·빌드 통과. 상대방 입장으로 투표한 뒤 다시 사건을 열어 선택지가 사라지고 결과 링크가 나타나는 것을 화면에서 확인했습니다.

## main 동기화와 결과 댓글 기록 연결 (2026-09-17)
- 원격 main의 5개 커밋을 빠른 병합으로 받았습니다. 원격에서 삭제한 미사용 `Home-1.css`의 로컬 닫기 버튼 수정은 실제 사용 중인 `Home.css`에도 동일하게 있어 삭제 상태를 유지했습니다.
- 원격에서 새로 추가된 MY 댓글 기록 기능과 합친 뒤, 진행 중·종결 사건 결과 화면의 댓글 저장 호출에 필수 본문 값을 전달하도록 수정했습니다. 병합 후 린트·빌드·diff 검사를 통과했습니다.

## 맨 위로 버튼 유리 질감 (2026-09-17)
- 기존 흰색 불투명 바탕을 옅은 푸른색 반투명 바탕으로 바꾸고, 버튼 뒤 콘텐츠에 12px 배경 흐림과 채도 보정을 적용했습니다. 화살표는 선명하게 유지하고 테두리와 그림자는 가볍게 조정했습니다.
- 사건 결과 화면에서 스크롤 중 배경이 흐려져 비치는 모습과 버튼을 누른 뒤 맨 위로 이동하는 동작을 확인했습니다.

## MY 내가 쓴 댓글 아이콘 (2026-09-17)
- Figma 노드 `1913:12525`의 새 말풍선 SVG를 `comment.svg`에 적용했습니다. 기존 MY 메뉴 공통 아이콘 컴포넌트를 재사용해 댓글이 생기면 위의 `내가 참여한 사건`과 같은 주황색 활성 스타일로 전환됩니다.
- 아이콘 안쪽은 Figma 기준 16px로 맞추고, 비활성 상태의 회색 도형은 `neutral-500`, 원형 배경은 기존 `neutral-250`을 사용합니다. MY 화면에서 비활성 표시를 확인했습니다.

## 홈 왈가왈후 더보기 시작 위치 (2026-09-17)
- 홈 왈가왈후 섹션의 `더보기`로 목록에 진입하면 공통 스크롤 영역을 맨 위로 이동합니다. 홈에서의 스크롤 기억과 다른 진입 경로는 유지합니다.
- 린트·빌드·diff 검사를 통과했고, 실제 화면에서 `더보기` 후 `/afterstory`의 첫 화면과 스크롤 위치 0을 확인했습니다.

## 서아 사건 참여·의견 수 일치 (2026-09-17)
- 서아 사건의 배심원 참여 수 611명과 후일담 연결 카드의 의견 수를 같은 값으로 연결했습니다. 카드의 댓글 44개는 별개의 댓글 수로 유지합니다.
- 린트·빌드·diff 검사를 통과했습니다.

## 사건 접수 완료 화면 이전 버튼 (2026-09-17)
- 접수 흐름에 들어올 때 직전 방문 기록을 기억하고, 완료 화면의 상단 이전 버튼은 작성·요약·접수 단계를 건너뛰어 접수를 시작한 화면으로 돌아갑니다. 앱 안에서 시작한 기록이 없는 경우에만 홈으로 이동합니다.
- 하단 `접수한 내용 확인하기`는 기존처럼 내 사건 목록으로 연결됩니다. 린트·빌드·diff 검사를 통과했고, 서아 계정으로 광장→접수 완료→상단 이전 버튼을 눌러 광장으로 바로 돌아오는 동작을 확인했습니다.

## 새 파비콘 세트 적용 (2026-09-17)
- 전달받은 `favicon-og-set.zip`에서 실제로 변경된 파비콘 이미지 7개(ICO, 16/32 PNG, Apple touch, 192/512 PNG, maskable)만 `public/favicon`에 교체했습니다.
- 압축파일의 매니페스트는 기존 파일과 동일해 유지했습니다. 로그인 화면에서 사용하는 SVG, Windows 타일 이미지, OG·Twitter 공유 이미지는 압축파일에 새 버전이 없고 사용자 요청 범위가 파비콘 교체라서 그대로입니다.
- 교체한 7개 파일이 압축파일 원본과 SHA-256으로 일치하고 빌드·diff 검사를 통과했습니다.

## MY 섹션 제목 아이콘 제거 (2026-09-17)
- MY의 `나의 활동`, `설정` 제목 오른쪽 접기 아이콘을 제거했습니다. 제목을 누르는 펼침·접힘 기능은 유지합니다.
- 미사용 아이콘 import와 해당 이미지 CSS를 정리하고 린트·빌드·diff 검사를 통과했습니다.

## 원격 main 동기화 (2026-09-17)
- 원격 main의 `9d9d797` 커밋을 빠른 병합으로 받았습니다. 후일담 파일의 import 충돌에서는 원격의 댓글 시간 처리와 로컬의 서아 배심원 611명 연결을 모두 유지했습니다.
- 통합 후 린트·프로덕션 빌드·diff 검사를 통과했습니다.

## 지훈 계정의 지난 댓글 12건 (2026-09-17)
- 지훈은 기존 사용자인데 MY > 내가 쓴 댓글이 비어 있어 앞뒤가 맞지 않았습니다. 광장에 이미 있는 사건 12건에 지훈의 댓글을 미리 심어 목록이 처음부터 채워지게 했습니다.
- 사건을 새로 만들지 않고 각 사건의 댓글 한 자리를 대신 씁니다. 그래서 목록 카드에 적힌 댓글 수와 실제 보이는 댓글 수가 그대로 같고, MY에서 카드를 누르면 그 사건 결과 화면의 댓글 목록에 같은 글이 실제로 있습니다.
- 대상 12건은 연인 3 · 친구 4 · 가족 2 · 직장 2 · 학업 1로 분야를 섞고 진행·해결 사건을 함께 넣었습니다. 지훈 본인이 올린 `case-company-874`와 별도 콘텐츠 파일을 쓰는 `case-parents-interfere`는 제외했습니다(공통 댓글 생성을 거치지 않는 사건).
- 각 댓글의 시각은 그 사건 나이(`28 + 목록 순서 × 37`분)의 45% 지점입니다. 사건보다 오래된 댓글이 생기지 않고, 위아래로 다른 배심원 댓글이 함께 보입니다. MY 목록에는 46분 전 ~ 5시간 전으로 표시됩니다.
- 문구는 지훈의 성격(감정보다 사실·기록·기준을 먼저 확인하는 프리랜서)에 맞춰 사건마다 다르게 썼습니다. 서아는 신규 가입이라 아무것도 심지 않고, 직접 쓴 댓글만 쌓입니다.
- 새 파일 `src/data/personas/jihoonComments.ts`에 데이터와 계산을 모으고, `utils/myComments.ts`가 읽을 때 저장된 댓글과 합쳐 시각 순으로 내놓습니다. 직접 쓴 댓글을 저장할 때는 심어 둔 댓글을 다시 저장하지 않습니다.
- 타입체크·린트를 통과했고, 12건이 모두 사연이 있는 사건이며 중복이 없고 사건 나이를 넘지 않는 것을 확인했습니다.

## 다른 후일담 목록 시각 재배치 (2026-09-17)
- 25건이 6시간 45분 안에 몰려 있어 페이지를 넘겨도 같은 시각처럼 보였습니다. 앞쪽은 분 단위, 뒤로 갈수록 시간·일 단위로 벌려 마지막 5페이지가 3일 전이 되게 했습니다.
- 1페이지 7분~2시간 · 2페이지 4~13시간 · 3페이지 15~23시간 · 4페이지 1~2일 · 5페이지 2~3일입니다.
- 한 시간이 넘어가면 분은 적지 않습니다. `1시간 30분 전` → `1시간 전`, `3시간 40분 전` → `3시간 전`. 하루가 넘으면 `N일 전`으로 묶습니다.
- 이 문구는 후일담 상세의 댓글 시각을 정하는 기준이기도 해서, `parseElapsedMinutes`가 `N일`도 읽도록 넓혔습니다. 빠뜨리면 하루가 넘은 글의 댓글이 글보다 오래된 시각으로 남습니다.

## 지훈 지난 댓글 시각 확장과 후일담 목록 반응 (2026-09-17)
- 지훈의 댓글 12건이 5시간 안에 몰려 있어 한번에 쓴 것처럼 보였습니다. 18분 전부터 3일 전까지 벌렸습니다.
- 광장 사건은 가장 오래된 것도 12시간 전 글이라 그 위에 며칠 전 댓글을 둘 수 없습니다. 그래서 8건은 광장 사건(18분~6시간 전), 4건은 후일담(20시간~3일 전)에 나눠 심었습니다. 후일담은 목록 뒤쪽이 2~4일 전이라 글보다 오래된 댓글이 되지 않으면서 3일 전까지 내려갑니다.
- 각 댓글은 자기가 달린 글의 나이 안쪽에 있습니다. 5일 전이던 두 글은 3일·4일 전으로 조정해 목록의 시각이 위에서 아래로 계속 오래된 순서가 되게 했습니다.
- `COMMUNITY_AFTER_STORIES`를 화면 파일에서 `src/data/common/afterStoryList.ts`로 옮겼습니다. 목록 제목과 주소를 MY 기록에서도 써야 해서, 데이터를 화면 밖으로 꺼냈습니다.
- 후일담 카드의 공감 수를 2~108로 다시 잡았습니다. 전부 한 자리 수라 표처럼 보였습니다. 오래된 글일수록 대체로 쌓이되, 같은 2일 전 글에 23과 76이 함께 있도록 편차를 뒀습니다.
- 타입체크·린트를 통과했고, 12건 모두 자기 글보다 나중 시각인지와 목록 5페이지의 시각·공감 분포를 확인했습니다.

## 후일담을 해결 사건에만 연결 (2026-09-17)
- 후일담은 판결이 끝난 뒤에 쓰는 글이라, 광장에서 아직 투표 중인 사건의 후일담은 있을 수 없습니다. 진행 사건과 이어져 있던 9건(휴대폰 알림, 여행 경비, 단체 대화방, 부모님 간섭, 퇴근 후 지시, 전 애인 초대, 이사 일정, 신입 교육, 실험 데이터)을 목록에서 뺐습니다.
- 광장에 사건이 없는 4건(생일 선물 오해, 답장 속도, 명절 일정, 회식 불참)은 3~5일 전 글로 두고 목록 뒤쪽에 남겼습니다. 광장은 최근 20건만 보여주므로, 그보다 오래된 사건의 후일담은 여기에만 남는다는 설정입니다.
- 남은 16건은 앞의 12건이 해결 사건과 이어지고, 뒤의 4건이 예전 사건입니다. 8분 전부터 5일 전까지 이어집니다.
- 한 장에 5건이면 마지막 장에 한 건만 남아, 광장과 같은 4건 단위로 바꿨습니다. 4장 × 4건입니다.
- 지훈의 후일담 댓글 4건은 남은 글에 맞춰 옮겼습니다. 생활비 사건은 광장 쪽에 이미 지훈 댓글이 있어, MY에서 같은 제목이 두 번 나오지 않도록 다른 글을 씁니다.
- 공감 수도 16건 기준으로 다시 잡았습니다(3~124). 오래된 글일수록 대체로 쌓이되 같은 날짜 안에서도 갈리게 뒀습니다.

## 해결 사건 13건 전부 후일담 연결 (2026-09-17)
- 해결 사건 중 `case-friend-mistake`(오랜 친구가 제 실수를 계속 이야기해요)만 후일담이 없었습니다. 광장 목록 20건 밖에 있고 홈의 AI 맞춤 추천에만 나오는 사건이라 빠져 있었습니다.
- 광장에 사건이 없던 예전 후일담 4건 중 `reply-pace`(답장 속도)를 빼고 이 사건의 후일담을 넣었습니다. 목록은 16건 그대로라 4장 × 4건 구성도 유지됩니다.
- 이제 해결 사건 13건에 모두 후일담이 있고, 진행 사건 9건에는 없습니다. 예전 사건 후일담은 생일 선물 오해·명절 일정·회식 불참 3건입니다.

## 후일담 상세에서 원래 사건으로 가는 버튼 (2026-09-17)
- 후일담 상세 작성자 줄 오른쪽에 `그날의 사건 보기 ›` 버튼을 넣었습니다. 바로 아래 편지 제목이 `그날 이후`라, 위아래로 사건 → 그 이후 순서가 읽힙니다.
- 후일담 16건 중 광장에 사건이 있는 13건에만 표시합니다. 예전 사건 3건은 갈 곳이 없어 그리지 않습니다. 목적지는 목록에 새로 붙인 `caseId`로 정하고, 화면에서 이름으로 추측하지 않습니다.
- 테두리만 있는 파란 알약 버튼입니다. 편지지 위라서 꽉 찬 버튼은 시선을 뺏습니다. 닉네임이 길면 이름 쪽이 말줄임 되고 버튼은 밀리지 않습니다.

## 잔금 사건 결과의 후일담 카드 연결 (2026-09-17)
- `이 사건의 후일담` 카드의 화살표가 눌리는 것처럼 보였지만 실제로는 아무 데도 가지 않았습니다. 카드 전체를 `afterstory-video-payment`(카페 홍보영상 후일담)로 잇고, 돌아올 위치와 스크롤을 같이 넘겨 뒤로가기가 보던 자리로 돌아오게 했습니다.
- 같이 발견한 문제: CSS가 `.after-story>article`이라 오늘의 사건 결과처럼 카드를 `Link`로 감싼 화면에서는 선택자가 빗나가 배경과 여백이 통째로 빠져 있었습니다. 자손 선택자로 바꾸고 `.after-story__link`에 링크 기본 스타일과 포커스 표시를 넣었습니다.

## 해결 사건 결과마다 후일담 카드 연결 (2026-09-17)
- 후일담이 있는 해결 사건 13건의 결과 화면 맨 아래에 `이 사건의 후일담` 카드를 넣고, 카드 전체를 그 후일담으로 이었습니다. 전에는 축의금과 잔금 두 사건에만 있었습니다.
- 카드 인용문은 목록의 한 줄 요약 대신 두세 줄짜리 `quote`를 새로 썼습니다. 한 줄만 넣으면 그 자리가 앙상해 보입니다. 12건을 사건별 결말에 맞게 작성했고, 잔금 사건은 전용 화면이 기존 데이터를 그대로 쓰고 있어 비워 뒀습니다.
- 카드를 그릴지는 후일담 목록의 `caseId`로 판단합니다. 투표 중인 사건은 후일담이 없으므로 이 자리가 비어 있습니다.
- 돌아올 위치와 스크롤을 함께 넘겨, 후일담에서 뒤로가기하면 결과 화면의 보던 자리로 돌아옵니다.

## 후일담 카드 위치 정정 — 해결 사건 결과 화면 (2026-09-17)
- `이 사건의 후일담` 카드를 `CaseResultPage`에 넣었는데 화면에 나오지 않았습니다. `AppRoutes`가 status가 closed인 광장 사건의 결과를 `ClosedCaseResultPage`로 보내기 때문입니다. `CaseResultPage`로 오는 광장 사건은 투표 중이라 후일담이 없어, 넣어 둔 코드가 한 번도 실행되지 않았습니다.
- 카드를 `ClosedCaseResultPage`로 옮기고 `CaseResultPage`에서는 뺐습니다. 이제 해결 사건 13건의 결과 화면 맨 아래에 모두 나옵니다.
- 브라우저에서 13건을 하나씩 열어 카드 표시·링크 주소·배경색을 확인했고, 투표 중인 사건에는 카드가 없는 것도 함께 확인했습니다. 반대 방향(후일담 → `그날의 사건 보기`)도 같이 확인했습니다.

## 다른 후일담 5페이지로 채우기 (2026-09-17)
- 목록이 16건(4장)이라 5페이지를 채우기 위해 예전 사건 후일담 4건을 뒤에 더했습니다. 데이트 비용 분담, 룸메이트 청소 당번, 동생의 무단 사용, 스터디 지각 — 연인·친구·가족·학업으로 분야를 나눴습니다.
- 5~7일 전 글로 두어 목록 맨 뒤에 놓입니다. 광장에는 올리지 않으므로 사건 상세로 가는 버튼은 뜨지 않고, 상세의 본문은 기존 공통 문안을 씁니다.
- 공감 수는 71~152로 앞쪽 글보다 대체로 높되 편차를 뒀습니다.
- 브라우저에서 5페이지를 모두 넘겨 4건씩 채워지는 것과 새 글 상세가 열리는 것을 확인했습니다. 총 20건 = 해결 사건 연결 13 + 예전 사건 7입니다.

## 다른 후일담 머리글 간격과 검색창을 광장과 맞춤 (2026-09-17)
- 제목과 검색창 사이가 광장보다 넓어 두 화면의 머리글 높이가 달라 보였습니다. 제목~설명 4px, 설명~검색창 22px로 광장(`.case-feed`의 gap 22px, `.case-feed__description`의 margin 4px)과 같은 값으로 맞췄습니다.
- 전에 쓰던 32px는 안내 문구 바로 아래에 메모 목록이 붙어 있을 때 정한 값입니다. 지금은 사이에 검색창이 들어와 그만큼 띄울 이유가 없습니다.
- 검색창도 광장과 같은 모양으로 바꿨습니다. 단색 테두리 대신 conic-gradient를 border-box에 깔고 안쪽을 흰색으로 덮어, 각도를 돌리면 밝은 부분이 테두리를 따라 2.6초에 한 바퀴 돕니다. 포커스 시 그림자도 같습니다.
- 각도 변수 이름은 광장 것과 나눴습니다. 같은 이름을 쓰면 한쪽 CSS만 불러온 개발 환경에서 `@property` 선언이 없어 애니메이션이 멈춥니다. 움직임 줄이기 설정에서는 테두리가 돌지 않습니다.
- 두 화면을 실제로 띄워 간격을 재서 4px / 22px로 같은 것을 확인했습니다.
## 홈 AI 추천 완료 상태 문구 (2026-09-17)
- 홈 AI 맞춤 추천의 종료된 사건 카드 상태를 MY 서아 사건과 같은 `투표 완료`로 맞췄습니다. 진행 중 카드의 `투표 중`과 AI·배심원 의견 일치 태그는 유지합니다.
- 린트·빌드·diff 검사를 통과했습니다.

## 홈 막상막하 카드 안내 (2026-09-17)
- 막상막하 하단의 주황색 투표 CTA(로그인 상태별 링크·비활성 버튼)를 제거했습니다. 현재 상단 게이지 카드를 누르면 공통 토스트 `막상막하 사건 상세는 업데이트 예정입니다.`가 나옵니다.
- 아래 `치열한 공방 중` 카드를 누르면 해당 사건이 위 게이지 카드로 올라가고 기존 상단 사건이 아래로 내려갑니다. `다른 사건` 버튼도 같은 교체 동작을 사용합니다. 짧은 퇴장·진입 애니메이션을 넣고 움직임 줄이기 설정에서는 즉시 교체합니다.
- 두 카드의 키보드 포커스와 클릭 영역을 마련했습니다. 아래 카드를 두 번 눌러 사건이 왕복 교체되고 토스트가 뜨지 않는 것을 홈 화면에서 확인했습니다. 린트·빌드·diff 검사를 통과했습니다.

## 홈 밸런스게임 선택 비율 (2026-09-17)
- 선택 결과의 말풍선 안에 선택 문구와 비율을 함께 보여줍니다. 깻잎은 `상관 없음 38% / 절대 안됨 62%`, 민트초코는 `좋다 57% / 싫다 43%`로, 두 선택지는 합계 100%인 시연용 고정 값입니다.
- 비율은 결과 단계에만 나타나며 기존 선택 색을 따릅니다. 결과 말풍선은 180px로만 넓히고, 외곽선 SVG도 해당 너비로 다시 그려 꼬리와 둥근 모서리가 납작하게 늘어나지 않게 했습니다. 선택 후 접시 확대 단계까지는 기존 140px을 유지하고, 숫자가 나타나는 결과 단계에서 너비가 360ms 동안 커집니다.

## 홈 막상막하 교체 모션과 호버 제거 (2026-09-17)
- 아래 `치열한 공방 중` 카드의 호버·포커스 시 테두리와 그림자 변화를 제거했습니다. 키보드로 조작할 수 있는 버튼의 포커스 윤곽은 유지합니다.
- 카드 교체는 후속 피드백에 맞춰 위·아래 카드가 서로를 향해 92px 움직이도록 다시 키웠습니다. 200ms 교차 이동 후 내용을 바꾸고, 460ms 동안 반대 방향으로 제자리에 오면서 살짝 튀어 오른 뒤 안정됩니다. 중간에도 카드를 68% 불투명도로 남겨 통째로 사라지는 느낌을 막았습니다. 움직임 줄이기 설정은 기존처럼 즉시 교체합니다.
- `src`의 CSS `:hover` 스타일과 로그인 선택 목록의 마우스 진입 선택을 제거했습니다. 앞으로 새 인터랙션에도 호버 전용 표시를 추가하지 않습니다. 밸런스게임의 드래그 중 접시 미리보기와 페이지네이션의 누름 상태 정리용 `onPointerLeave`는 호버 표시가 아니라 조작 상태 처리이므로 유지합니다.
- 홈에서 아래 카드를 눌렀을 때 중고거래 사건이 위 게이지 카드로 바뀌고 반려견 사건이 아래로 내려가는 것을 확인했습니다. 린트·빌드·diff 검사를 통과했습니다.

## 홈 밸런스게임 접시 직접 선택 (2026-09-17)
- 깻잎과 민트초코의 양쪽 접시에 투명한 선택 버튼을 올려, 말풍선 클릭·소재 드래그와 같은 결과 흐름으로 연결했습니다. 선택 후에는 접시 버튼을 비활성화합니다.
- 드래그 시작 범위의 위아래 여유를 24px에서 30px로 넓혔습니다. 사용자 후속 요청에 따라 옅은 원형 테두리와 배경은 제거해 잡는 범위가 화면에 드러나지 않게 했습니다. 안내 문구에는 접시 누르기를 추가했습니다.
- 별도 홈 미리보기에서 깻잎 왼쪽 접시 클릭, 깻잎 오른쪽 드래그, 민트초코 오른쪽 접시 클릭을 확인했습니다. 양쪽 접시 중심이 드래그 영역에 가리지 않고 선택 버튼에 닿는 것도 확인했습니다.

## 공통 하단 네비 선택 표시 이동 (2026-09-17)
- 홈·배심원 광장·왈가왈후~·MY의 활성 아이콘 뒤에 옅은 파란 선택 표시를 두고, 메뉴 이동 시 이전 위치에서 새 위치로 460ms 동안 이동시켰습니다. 아이콘·글자 색도 260ms에 걸쳐 바뀝니다.
- 선택 표시 위치는 실제 메뉴 너비를 측정해 정하므로 좁은 화면에서도 탭 가운데에 놓입니다. 처음 진입과 움직임 줄이기 설정에서는 이동 효과 없이 제자리에 표시합니다. 중앙 사건 접수 CTA의 원형 디자인과 비로그인 차단 흐름은 유지합니다.
- 별도 미리보기에서 홈→배심원 광장→왈가왈후~를 눌러 활성 메뉴와 선택 표시의 위치·이동 클래스를 확인했습니다. 린트·빌드·diff 검사 통과.

## 시연 콘텐츠 시간축 통합 (2026-09-18)
- 브라우저의 첫 접속 시각을 로컬 저장소에 기록하고, 새 방문에서도 같은 값을 사용합니다. 사건 작성 일시·접수일은 이 시점에 맞춰 표시하며 상대 시간은 실제 경과에 따라 갱신합니다. 다른 브라우저나 저장소 초기화 후에는 그곳의 첫 접속 시각을 새로 잡습니다.
- 광장의 최신순 사건에 명시적인 게시 나이를 두고, 연결된 후일담이 원래 사건보다 먼저 올라온 것으로 보이지 않도록 순서와 간격을 조정했습니다. 사건 댓글, 후일담 댓글, 지훈의 MY 댓글은 같은 첫 접속 시점을 공유합니다.
- 사용자가 직접 접수하거나 후일담을 게시한 시각은 활동 기록에 따로 저장해 MY의 접수일·내 후일담 시각에 사용합니다. 로그아웃하더라도 첫 접속 시각은 남겨 같은 브라우저의 시연 콘텐츠가 갑자기 다른 날짜로 이동하지 않습니다.
- 기존 투표 결과와 진행·완료 상태는 포트폴리오 시연 데이터로 유지합니다. 마감 카운트다운은 현재 24시간 투표 기간을 사건 작성 시각에 연결합니다.
- 시간 계산 단위 확인, 린트, 타입 검사, 배포용 빌드, diff 검사를 통과했습니다.

## 판결 완료 사건의 24시간 투표 기간 (2026-09-18)
- 광장의 해결 사건과 추천 전용 판결 완료 사건의 작성 시각을 모두 첫 방문 기준 24시간보다 오래전으로 옮겼습니다. 연결된 후일담은 작성 시각이 투표 마감보다 뒤가 되도록 맞췄고, 광장 최신순 목록도 실제 작성 순서로 재정렬했습니다.
- 진행 중 사건의 남은 시간은 화면을 연 순간부터 새로 세지 않고, 사건 작성 시각에서 정확히 24시간 뒤까지 계산합니다. 홈·상세·결과 화면이 같은 마감 시각을 공유합니다.
- 해결 사건 13건(광장 11건·추천 전용 2건)이 모두 작성 후 24시간 이상이고, 연결 후일담 13건이 각각 투표 종료 뒤에 있는지 확인했습니다. 최신순 20건의 작성 시각도 역전되지 않았습니다.

## 홈 AI 맞춤 추천을 투표 가능한 사건으로 변경 (2026-09-18)
- 서아와 지훈의 추천 후보를 광장에 있는 진행 사건으로 교체했습니다. 지훈의 기존 배심 참여 기록에 있는 진행 사건은 추천하지 않습니다.
- 홈 추천은 현재 계정에서 아직 투표하지 않은 진행 사건을 최대 3건 보여줍니다. 참여하면 남은 후보가 다음 카드로 올라옵니다. 제목·분야·댓글 수는 광장 사건 데이터에서 직접 가져오며, 클릭하면 해당 사건 상세의 투표 영역으로 이동합니다.
- 후보를 모두 투표하면 빈 카드 목록 대신 참여 완료 안내를 보여줍니다.

## 투표 직후 1심 영상 표시 복구 (2026-09-18)
- 진행 사건 투표 뒤 결과 화면에는 영상과 첫 장면 이미지가 렌더됐지만, 비디오 배치와 첫 장면 숨김 스타일이 누락되어 영상이 이미지에 가려졌습니다.
- 영상과 첫 장면을 같은 크기로 겹치고 재생이 시작되면 첫 장면이 부드럽게 사라지도록 수정했습니다. AI가 글쓴이/상대방 중 어느 편인지에 따라 해당 영상의 첫 장면을 사용하고, 영상 말미에는 기존 말풍선이 보이게 했습니다.
- 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 광장 분야와 사건 번호 코드 정합성 (2026-09-18)
- 광장 필터는 전체를 제외한 연인·친구·가족·직장·학업 5개입니다. 번호 코드를 각각 `LOVE`·`FRIEND`·`FAMILY`·`COMPANY`·`SCHOOL`로 맞췄습니다. 직장 사건 생성 코드에서 쓰던 `WORK`는 기존 지훈 사건의 `COMPANY`와 달라 통일했습니다.
- 광장 데이터에서 생성하는 번호와 별도 상세 데이터를 쓰는 두 사건의 번호가 달라질 수 있어, 부모님 사건은 `CASE-FAMILY-125`, 의뢰인 잔금 사건은 `CASE-COMPANY-874`를 상세·결과에 공통으로 사용합니다.
- 실제 광장 사건에는 없는 `금전`·`이웃`을 사건 카테고리 타입과 색상 매핑에서 제거했습니다. 일반 문구의 금전·이웃 언급은 분야 필터가 아닙니다. 과거 서아 결과 직접 링크의 `CASE-SEOA-01` 별칭은 기존 링크 호환용으로만 남아 있고 현재 사건 번호로 표시되지 않습니다.
- 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 홈 검색창 닫기 동작 (2026-09-18)
- 검색 중에는 홈 스크롤을 잠그고, 검색창 바깥·닫기 버튼·Esc·상단 검색 버튼으로 닫으면 입력 포커스를 해제한 뒤 검색창·최근 검색어·어두운 배경이 180ms 안에 사라지도록 했습니다.
- 닫는 동안 패널을 유지해 전환이 갑자기 끊기지 않으며, 패널이 사라진 뒤 기존 홈 스크롤 위치에서 이어집니다. 움직임 줄이기 설정에서는 전환을 기다리지 않고 바로 닫습니다.
- 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 하단 네비와 맨 위로 버튼 간격 (2026-09-18)
- 하단 네비 배경이 실제 네비 상단보다 19px 돌출되는데, 맨 위로 버튼의 하단 위치는 네비 위 12px이라 시각적으로 겹쳤습니다. 공통 버튼을 네비 위 36px로 올려 배경과 약 17px 간격을 확보했습니다.
- 위에 쌓이는 판멍이 플로팅 버튼도 함께 올려 두 버튼 사이 6px 간격을 유지했습니다. 모바일 안전 영역 때문에 네비 높이가 늘어나는 경우도 두 버튼의 기준 높이에 반영합니다.
- 공통 컴포넌트의 위치를 바꿔 홈·광장·후일담·사건 상세/결과 등 하단 네비가 있는 모든 화면에 적용했습니다. 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 왈봇 플랜 토스트와 버튼 간격 (2026-09-18)
- 무료체험 신청 토스트가 고정 버튼 푸터의 윗선에 거의 붙어 있던 위치를 조정했습니다.
- 하단 네비 위 토스트에서 이미 사용하던 32px 간격을 공통 값으로 뽑아, 사건 접수와 왈봇 플랜의 고정 푸터 위 토스트에도 동일하게 적용했습니다. 플랜 전용 예외 위치값은 제거했습니다.
- 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 지훈 왈봇 플랜 관리 토스트 위치 (2026-09-18)
- 지훈의 플랜 관리 화면은 고정 푸터가 없어 토스트가 화면 하단 기본 위치(24px)에 나왔습니다.
- 고정 버튼이 있는 서아 화면과 같은 높이는 과해, 지훈 화면은 기본 24px에 공통 토스트 간격 32px만 더한 위치로 조정했습니다. 서아 화면의 고정 버튼 위 간격은 유지합니다. 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## MY 접수 사건 명칭 (2026-09-18)
- MY의 `내가 참여한 사건` 메뉴는 배심 참여가 아니라 본인이 접수한 사건 목록(`/my/cases`)으로 이동하므로 `내가 접수한 사건`으로 수정했습니다. 목록 본문 제목·접근성 이름도 같은 명칭으로 맞췄습니다.
- 배심 투표 기록(`/my/jury`)의 `내가 참여한 사건`은 실제 참여 목록이므로 그대로 유지했습니다. 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 비로그인 안내 팝업 문구 간결화 (2026-09-18)
- `로그인이 필요해요` 제목을 쓰는 MY·공통 팝업의 긴 기능 설명을 짧은 혜택 문구로 교체했습니다. MY는 `내 사건과 참여 기록을 한눈에 확인해요.`, 공통은 `내 생각을 남기고 다양한 사건에 참여해 보세요.`입니다.
- 제목·버튼·로그인 이동 흐름은 유지했습니다. 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 판멍 스쿨 배너 안내 (2026-09-18)
- 홈의 판멍 스쿨 광고 배너를 누르면 `판멍 스쿨은 준비 중이에요.` 토스트가 뜹니다. 배너의 이미지와 크기는 유지하고 키보드로도 누를 수 있게 했습니다.
- 린트·타입 검사·빌드를 통과했습니다.

## 토스트 문구 줄바꿈 정리 (2026-09-18)
- 공통 토스트가 긴 문구를 글자 단위로 잘라 마지막 한 글자만 다음 줄에 남기는 문제를 줄이도록 어절 단위 줄바꿈과 균형 잡힌 줄 배치를 적용했습니다.
- 플랜 관리의 결제수단·결제 내역·연간 변경·구독 해지 문구를 한눈에 읽히도록 줄였습니다. 홈 막상막하, 광장 랭킹, 무료체험 안내 문구도 간결하게 맞췄습니다.
- 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## 페이지 본문 줄바꿈 정리 (2026-09-18)
- 한국어 본문이 글자 중간에서 끊기지 않도록 기본 줄바꿈을 어절 단위로 맞추고, 매우 긴 단어만 공간이 부족할 때 나누도록 했습니다.
- 1심 판단 근거의 파란 박스 요약·본문과 진행 사건의 AI 판단 카드에 자연스러운 행 끝 처리를 적용했습니다. 축의금 사건 근거의 `부담을/줄 수 있어요` 강제 개행은 제거하고, 의도된 제목 개행은 유지했습니다.
- 후일담 카드·편지, MY 댓글, 결과 댓글, 접수 완료 파일 제목에서도 글자 단위 강제 줄바꿈을 제거했습니다. 닉네임과 이메일 등 공간이 좁거나 긴 식별자는 기존 처리 방식을 유지했습니다.
- 린트·타입 검사·빌드·diff 검사를 통과했습니다.

## MY에서 돌아올 때 화면이 두 번 움직이던 문제 (2026-09-18)
- MY > 내가 쓴 댓글에서 사건이나 후일담으로 들어갔다가 뒤로 가면, 앞 화면이 오른쪽으로 빠진 뒤 도착한 목록이 또 오른쪽에서 들어왔습니다. 같은 방향으로 두 번 움직여 화면이 떴다가 다시 미끄러지는 것처럼 보였습니다. 측정해 보니 도착 화면이 애니메이션 시작 전 세 프레임 동안 제자리에 먼저 그려진 뒤 translateX(243px)로 튀어 다시 들어왔습니다.
- `useDetailSlide`에 되돌아가는 길 표시(`skipEnterMotion`)를 추가했습니다. `leave`가 이동할 때 이 표시를 state에 실어 보내고, 받은 화면은 들어오는 슬라이드를 재생하지 않습니다. 앞 화면이 빠지는 모션 한 번으로 끝납니다. 뒤로 가기를 직접 처리하는 `CaseHeader`의 이동에도 같은 표시를 붙였습니다.
- 종결 사건 결과(`ClosedCaseResultPage`)와 후일담 상세(`AfterStoryDetailPage`)에도 진행 중 사건 결과와 같은 좌우 슬라이드를 붙였습니다. 전에는 이 두 화면만 전환 없이 뜨고 뒤로 갈 때도 즉시 이동해서, 내가 쓴 댓글에서 무엇을 누르느냐에 따라 움직임이 달랐습니다. 광장이나 홈에서 들어올 때는 전과 같이 전환이 없습니다.
- `CaseResultPage`에서 `useFocusComment`가 조기 return 뒤에 있어 렌더마다 훅 순서가 달라질 수 있었습니다. 댓글 목록을 만드는 부분을 조기 return 위로 올려 훅이 항상 같은 순서로 불리게 했습니다.
- `useFocusComment`가 effect 안에서 바로 상태를 바꾸던 부분을, 스크롤을 미루는 타이머 안으로 옮겼습니다. 페이지를 넘기는 렌더에 겹쳐 들어가 렌더가 한 번 더 도는 것을 막습니다.
- 내가 쓴 댓글에서 진행 중 사건 · 종결 사건 · 후일담 세 경우를 모두 실제로 띄워, 들어갈 때 슬라이드가 돌고 돌아온 뒤에는 도착 화면에서 애니메이션이 재생되지 않는 것을 확인했습니다. 광장에서 들어간 사건과 MY 배심 기록 왕복도 전과 같은지 함께 확인했습니다. 타입 검사와 배포용 빌드를 통과했습니다.
- 남은 것: `react-hooks/purity`가 `CaseResultPage`와 `ClosedCaseResultPage`의 댓글 등록 핸들러 안 `Date.now()`를 렌더 중 호출로 보고 있습니다. 같은 코드가 `CommentThread`에는 있으나 거기서는 걸리지 않아 원인을 아직 못 찾았습니다. 이 작업 전부터 있던 문제입니다.

## 지훈 추가 질문 페이지 문항 간격 (2026-09-18)
- 사건 접수 `몇 가지만 더 알려주세요` 화면만 문항 라벨이 `<p>`여서 브라우저 기본 여백 14px이 위아래로 붙었습니다. 그래서 질문과 선택 버튼 사이가 22px로, 첫 화면(8px)보다 넓었습니다.
- 공통 `.case-submit__label`에 `margin: 0`을 추가했습니다. 세 문항 모두 질문↔버튼 8px, 문항 사이 28px로 첫 화면과 같아졌습니다. 첫 화면의 `legend`는 더 구체적인 선택자가 8px을 유지해 변화가 없습니다.
- 지훈 계정 375px 화면에서 두 페이지 간격을 실측했습니다. 타입 검사·빌드·diff 검사 통과. 린트는 기존 결과 화면 `Date.now()` purity 오류 2건만 남아 있고 이번 변경과는 관계없습니다.
- 후속 요청: 선택 버튼 줄이 연달아 붙어 빼곡해 보여, 이 화면에서만 문항 사이 간격을 28px → 40px로 넓혔습니다(`case-submit--questions` 범위의 `margin-top: 12px`). 안내 문구 위아래와 다른 접수 화면의 간격은 그대로입니다. 375px 실측으로 질문↔버튼 8px, 문항 사이 40px을 확인했습니다. 타입 검사·빌드·접수 폴더 린트·diff 검사 통과.

## 후일담 상세 맨 위로 버튼 (2026-09-18)
- 후일담 상세(`판결 이후의 이야기들`)는 헤더를 고정하고 본문 `.afterstory-detail__scroll`만 스크롤합니다. 맨 위로 버튼은 공통 `.main-layout__scroll`만 보고 있어서 이 화면에서는 끝내 나타나지 않았습니다.
- 챗봇 위치는 홈과 같은 값(하단 182px, 오른쪽 10px)이었습니다. 그 아래 맨 위로 버튼 자리가 비어 있어서 챗봇만 떠 보였던 것입니다. 위치값은 바꾸지 않았습니다.
- `ScrollTopButton`이 경로가 바뀔 때 영역을 미리 찾지 않고, 기기 화면에서 캡처 단계로 받은 scroll 이벤트의 대상(공통 영역 또는 후일담 상세 본문)을 따라가게 했습니다. 경로 변경 직후에는 새 화면의 영역이 아직 없어 이전 화면의 영역을 잡는 문제가 있었습니다. 뒤로 가기로 복원된 위치는 한 프레임 뒤 가장 안쪽 영역의 위치로 반영합니다.
- 375px에서 후일담 목록→상세 이동 시 버튼 숨김, 본문 스크롤 후 표시, 클릭 시 본문 영역이 맨 위로 이동, 두 버튼 위치가 홈과 같음(맨 위로 하단 130px·오른쪽 18px, 챗봇 하단 182px·오른쪽 10px, 버튼 사이 12px)을 확인했습니다. 홈 스크롤 복원 후 버튼 표시도 유지됩니다. 브라우저 창이 백그라운드여서 애니메이션 프레임은 타이머로 대신해 검증했습니다. 타입 검사·빌드·수정 폴더 린트·diff 검사 통과.

## MY 하위 화면 맨 위로 버튼 (2026-09-18)
- MY > 내가 쓴 댓글·배심 참여·내가 접수한 사건(`.my-cases-page__content`)과 내 사건 결과(`.my-case-result-page__content`)는 본문만 스크롤해서, 챗봇만 있고 맨 위로 버튼이 나타나지 않았습니다.
- 두 버튼이 공유하는 규칙을 `components/common/floatingButtonLayout.ts`로 모았습니다. `FLOATING_SCROLL_SELECTOR`(맨 위로 버튼이 따라가는 스크롤 영역 목록)와 `hasBottomNavigation`(내비 위로 올릴 화면)입니다. 새 화면이 자체 스크롤 영역을 쓰면 이 목록에 추가하면 됩니다.
- 하단 내비가 없는 화면은 맨 위로 버튼을 하단 28px, 챗봇을 그 위 74px(버튼 40 + 간격 6)에 쌓아 홈과 같은 모양으로 맞췄습니다. 모바일 안전 영역만큼 함께 올라갑니다. 이 때문에 내비 없는 화면의 챗봇 기본 위치가 16px → 74px로 바뀌었습니다.
- 사건 접수·후일담 작성·왈봇 플랜·로그인 계열은 챗봇을 숨기고 하단 고정 버튼이 있는 흐름이라 맨 위로 버튼을 추가하지 않았습니다.
- 375px에서 내가 쓴 댓글·배심 참여(지훈), 내 사건 결과(서아)의 버튼 표시, 클릭 시 맨 위 이동, 스크롤 끝에서 두 버튼 아래 가려지는 링크·버튼 없음, 버튼 사이 12px(홈과 같음)을 확인했습니다. 내용이 짧은 지훈의 내가 접수한 사건은 스크롤이 없어 버튼이 뜨지 않습니다. 홈 위치(챗봇 하단 182px)는 그대로입니다. 타입 검사·빌드·수정 폴더 린트·diff 검사 통과.

## 분야 키워드 표기·색상 통일 (2026-09-18)
- 왈가왈후 `다른 후일담` 메모 카드는 `friend`/`company` 두 톤만 있어 연인·가족·학업도 파랑으로 보였습니다. 광장 사건 카드와 같은 `categoryDotColor`로 분야 색을 정합니다(연인 분홍·친구 파랑·가족 초록·직장 주황·학업 밝은 파랑). `CommunityAfterStory.category`를 `CaseCategory`로 좁혀 다섯 분야 외 값은 타입 검사에서 걸립니다.
- 모든 데이터·화면의 분야 표기를 검사해 다섯 분야(연인·친구·가족·직장·학업) 밖의 표기를 고쳤습니다.
  - 광장 `전 애인을 친구 모임에 초대해도 괜찮을까요?`의 `연애` → `연인`. 분류는 이미 연인이었고 표시 문구만 달랐습니다. 지훈 AI 맞춤 추천의 마지막 후보도 이 데이터를 씁니다. 09-13 광장 시안 반영 때 넣은 `연애` 표기를 사용자 요청으로 대체합니다.
  - 홈 `최근 본 사건` 카드의 `친구 · 모임` → `친구`. `생활 · 이웃`(세탁기 소음) 카드는 다섯 분야에 들지 않아 광장의 가족 사건 `부모님 병원 동행을 저에게만 부탁하는 형제자매`로 바꿨습니다.
  - 후일담 작성 화면의 연결 사건 표기 `친구 · 투표 종료` → `친구 · 판결 완료`(게시 확인 화면과 같은 상태 문구).
  - 지훈 AI 추천 안내 `일·관계 갈등 사건` → `직장·관계 갈등 사건`.
- AI 맞춤 추천의 학업 색이 짙은 갈색이라 광장(밝은 파랑)과 달랐습니다. 광장 색으로 맞췄습니다. MY 내 사건, 내 이야기 남기기의 분야 색은 이미 광장과 같았습니다.
- 그대로 둔 것: 후일담 상세의 `직장 · 후일담`처럼 분야 뒤에 글 종류를 붙인 표기, 챗봇 안내 문장의 `금전·법률`(분야 표기가 아닌 문장), 사건 접수의 관계 선택지 `기타`(입력 선택지).
- 지훈 계정으로 홈 AI 추천·최근 본 사건, 왈가왈후 다섯 분야 필터별 카드 색, 광장 카드 색과 전 애인 초대 사건의 `연인` 표기를 확인했습니다. 타입 검사·빌드·수정 폴더 린트·diff 검사 통과.
- 후속 요청: 홈 `최근 본 사건` 메모지의 분야 표기만 알약 배경 태그였고, 색도 메모지 색(파랑/노랑)을 따라가 가족이 갈색으로 보였습니다. 광장 카드와 같은 5px 점 + 12px 굵은 분야 색 글자로 바꿨습니다. `RecentCase`에 `category`를 추가해 `categoryDotColor`로 색을 정합니다. 화면에서 두 카드의 점 크기·간격·글자 크기·굵기·색이 광장 카드와 같음을 확인했습니다. 타입 검사·빌드·수정 폴더 린트·diff 검사 통과.

## 홈 AI 맞춤 추천 조회수·댓글 표기 (2026-09-18)
- 추천 카드 하단의 `댓글 24개`를 광장 사건 카드와 같은 `조회수 197  댓글 24`로 바꿨습니다. 값은 광장 카드와 같은 데이터(조회수, 기본 댓글 수 + 직접 단 댓글 수)를 씁니다. 글자 크기·색은 그대로 두고, 항목 사이의 `·` 구분점만 빼고 광장처럼 14px 띄웠습니다.
- 지훈 계정에서 추천 3건의 수치가 광장의 같은 사건 카드와 같은 것을 확인했습니다. 타입 검사·빌드·홈 폴더 린트·diff 검사 통과.

## 홈 최근 본 사건 클릭 안내 (2026-09-18)
- 최근 본 사건 메모지 두 장은 이어질 상세 화면이 없어 눌러도 반응이 없었습니다. 막상막하 카드와 같은 방식으로 카드 전체를 덮는 투명 버튼을 두고, 누르면 공통 토스트 `최근 본 사건 상세는 준비 중이에요.`를 띄웁니다. 키보드 포커스 표시와 카드별 접근성 이름을 넣었습니다.
- 지훈 계정 홈에서 카드 클릭 시 토스트 표시를 확인했습니다. 타입 검사·빌드·홈 폴더 린트·diff 검사 통과.

## 광장 해결 사건 보기 대표 사건 고정 (2026-09-18)
- 광장 `해결 사건` 보기에서 `수정 2회를 마쳤는데, 의뢰인이 잔금 지급을 미루고 있어요.`(`case-company-874`)를 맨 위에 고정합니다. 나머지 해결 사건은 기존 최신순을 유지합니다. 고정 사건 id는 `CaseFeedSection.tsx`의 `PINNED_CLOSED_CASE_ID`입니다.
- 왈가왈후의 `다른 후일담 보러가기`(`?view=closed`)로 들어와도, 카테고리 `직장`을 함께 골라도 첫 카드임을 확인했습니다. 타입 검사·빌드·광장 폴더 린트·diff 검사 통과.

## 투표 완료 팝업 동안 뒤 화면 고정 (2026-09-18)
- 진행 사건에서 `투표하기`를 누르면 투표가 바로 저장돼, `투표 완료!` 팝업이 떠 있는 1초 동안 뒤 화면이 `이미 투표한 사건이에요`로 바뀌었습니다. 팝업과 뒤 화면이 함께 움직여 시선이 흩어졌습니다.
- `CaseDetailPage`에서 방금 투표한 경우(`confirmedVote`)에는 완료 안내 대신 선택한 투표 화면을 그대로 두고, 팝업 뒤 결과 화면으로 넘어갑니다. 다시 들어온 경우에는 기존처럼 `이미 투표한 사건이에요`와 나의 선택을 보여줍니다. 팝업 딤이 화면 전체를 덮어 그동안 선택을 바꿀 수 없습니다.
- 지훈 계정으로 진행 사건 투표 → 팝업 중 투표 화면 유지 → 결과 화면 이동 → 재방문 시 완료 안내를 확인했습니다. 타입 검사·빌드·수정 파일 린트·diff 검사 통과.

## 1심 판결 영상 말풍선 꼬리 방향 (2026-09-18)
- `1심 · 판멍이의 판단` 영상 끝에 뜨는 `이쪽!` 말풍선 중 상대방 편 판결(주황)은 판멍이 머리 왼쪽(`left: 13%`)에 두는데, 꼬리는 글쓴이 편(파랑)과 같이 왼쪽 아래를 향해 머리 반대쪽을 가리켰습니다. 주황 말풍선의 도형만 좌우로 뒤집어(`scaleX(-1)`) 꼬리가 오른쪽 아래를 향하게 했습니다. 위치·색·글자는 그대로입니다.
- 여행 경비 사건 결과 화면에서 주황 말풍선의 왼쪽 위치 유지, 도형만 뒤집힘, 글자는 뒤집히지 않음을 확인했습니다(영상이 백그라운드 창에서 재생되지 않아 말풍선을 임시로 붙여 확인). 빌드·diff 검사 통과.

## 온보딩 단계 전환·홈 진입 모션 (2026-09-18)
- 온보딩 세 단계(일상 속 고민 → 배심원 → 판결 그 후의 이야기)가 즉시 바뀌던 것을 좌우 밀기로 바꿨습니다. 다음으로 가면 이전 단계가 왼쪽으로 나가고 새 단계가 오른쪽에서 들어오며, 뒤로 가거나 앞 단계 점을 누르면 반대 방향입니다. 460ms, `cubic-bezier(.32, .72, 0, 1)`. 헤더와 하단 점·다음 버튼은 고정입니다.
- 문구+장면을 `StepPanel`로 묶고 밀기 동안만 두 패널을 함께 그립니다. 나가는 패널은 같은 key를 유지해 영상·등장 애니메이션이 다시 시작되지 않습니다. 밀기 도중의 추가 입력은 무시합니다. 단계별 문구 위치 규칙은 `.onboarding-page--step-2`에서 `.onboarding-page__panel--step-2`로 옮겼습니다.
- 마지막 단계의 오른쪽 아래 화살표와 SKIP은 온보딩 전체를 320ms 동안 살짝 확대(1.06)하며 흐리게 지운 뒤 홈으로 이동합니다. 홈은 `appLaunch` 상태를 받은 `AppViewport`가 0.96 크기·투명에서 제자리로 420ms 동안 나타나게 합니다(앱 첫 화면 진입식 줌 + 페이드). 첫 단계의 뒤로가기(홈 이동)는 기존대로 즉시 이동합니다.
- 모션 줄이기 설정에서는 밀기·진입 전환 없이 바로 바뀝니다. 공통 파일 변경은 `AppViewport.tsx`의 `appLaunch` 진입 애니메이션 한 곳입니다.
- 브라우저에서 1→2→1, 점으로 1→3 이동의 방향별 애니메이션, 밀기 중 연타 무시, 나가는 패널의 DOM 유지, 마지막 화살표의 퇴장 → `/home` 이동 → 홈 진입 애니메이션을 확인했습니다. 타입 검사·빌드·수정 폴더 린트·diff 검사 통과.

## 홈 왈가왈후 편지 열림 그림자 잘림 (2026-09-18)
- 편지를 열 때 편지지(top -10px)의 위쪽 그림자(`drop-shadow` 약 14px)가 카드 위로 약 14px 나가는데, 열리는 동안 `.letter`가 `overflow: hidden`이라 잘려 있다가 1.2초 뒤 `letter--open`에서 `overflow: visible`로 바뀌는 순간 한꺼번에 나타났습니다.
- `overflow: hidden`을 `clip-path: inset(-48px -48px 0 -48px)`로 바꿔 아래쪽만 자르고 위·옆은 처음부터 보이게 했습니다. 닫힘 상태에서 봉투 아래로 내려가 있는 편지지는 계속 가려집니다. 열림이 끝나면 `clip-path: none`입니다. 열림 끝에 카드 아래로 새로 드러나는 요소가 없음(편지지는 자체 clip-path로 아래가 가려짐)을 확인했습니다. 빌드·diff 검사 통과.

## 홈 밸런스 게임 다시하기 버튼 (2026-09-18)
- 결과 아래 새로고침 아이콘만으로는 배경에 묻혀 잘 안 보인다는 의견이 있어, 같은 자리의 버튼을 흰 원(44px, 옅은 주황 테두리, 그림자) 안에 주황 새로고침 아이콘(22px)을 둔 원형 버튼으로 바꿨습니다. 결과는 가리지 않습니다. 말풍선 아래 약 12px, 게임판 가운데이며 깻잎·민트초코 양쪽 선택 모두 같은 자리입니다.
- 거쳐 간 안: 게임판을 흐리게 덮고 가운데에 버튼을 띄우는 방식(결과를 가리고, 흐림은 보통 잠긴 콘텐츠에 쓰는 표현이라 제외), 아이콘 + `다시하기` 글자 알약(이 섹션에 글자 버튼이 이미 많아 제외). 두 안의 코드와 CSS는 삭제했습니다.
- 후속 요청: 원형 버튼을 두 배(88px, 아이콘 44px)로 키우고, 질문 줄과 결과 말풍선 사이 가운데(게임판 기준 중심 114px, top 70px)로 올렸습니다. 결과 그림(깻잎 더미·민트초코 그릇) 가운데 위에 얹힙니다. 네 경우 모두 질문 줄·말풍선과 각각 102px 간격, 가로 가운데임을 확인했습니다. 빌드·diff 검사 통과.
- 후속 요청: 원형 버튼을 반투명 흰색(배경 62%, 흰 테두리 85%)으로 바꾸고 64px(아이콘 30px)로 줄였습니다. 중심은 그대로 114px(top 82px)입니다. 결과 뒤 연출(접시 이동 0.9초, `절대 안됨`의 젓가락 놓기 0.14초 지연 + 0.9초)이 모두 끝난 1.1초 뒤에 나타나며, 그 전에는 보이지 않고 눌리지 않습니다. 모션 줄이기에서는 바로 보입니다.
- 후속 요청: 다시하기 아이콘을 사용자 제공 두 화살표 아이콘(`src/assets/home/figma/balance-retry-arrows.svg`)으로 바꿨습니다. `homeAssets.ts`의 `refreshIcon`이 이 파일을 가리키며, 이전 아이콘 `refresh.svg`는 파일로 남겨 두었습니다.
- 후속 요청: 반투명 흰색이 결과 그림에 묻혀 너무 흐려 보인다는 의견으로, 원형 버튼 배경을 불투명 흰색(#fff)과 옅은 주황 테두리로 되돌렸습니다. 크기(64px)·위치·1.1초 뒤 등장은 그대로입니다.
- 아이콘만 있던 이전 모양으로 되돌리려면 `BalanceGameSection.tsx`의 `RETRY_STYLE`을 `'icon'`으로 바꾸면 됩니다.
- 타입 검사·빌드·홈 폴더 린트·diff 검사 통과.

## 깻잎 잎 그림의 젓가락 잔상 제거 (2026-09-18)
- `절대 안됨`을 고르면 젓가락이 깻잎을 놓고 잎이 떨어지는데, 떨어지는 잎 위에 흐린 젓가락 끝 모양이 따라다녔습니다. 원인은 `perilla-leaf.png` 자체였습니다. 원본에서 젓가락을 떼어 낼 때 젓가락이 덮던 띠(잎 윗부분 줄기와 오른쪽 위 잎자락)에 흐린 젓가락 색이 남아 있었고, 잡고 있는 동안은 젓가락에 가려 안 보이다가 떨어질 때 드러났습니다.
- 1차 수정(띠 안의 잔상 색을 주변 잎 색으로 매끄럽게 칠함)은 갈색은 없앴지만 흐린 얼룩이 남아 여전히 잔상처럼 보였습니다(사용자 재지적, 화면 캡처로 확인).
- 최종: 두 젓가락 그림(`perilla-chopsticks.png`, `perilla-chopsticks-full.png`)이 덮는 자리를 4px 넓힌 띠 안에서, 진한 초록인 줄기(g가 r·b보다 40 이상 큰 픽셀)는 그대로 두고 나머지 1,339개 픽셀은 띠에 수직 방향으로 비켜 난 깨끗한 잎 결(22~46px 옆)을 옮겨 채웠습니다. 경계 1px은 이웃과 섞어 이음매를 없앴습니다. 잎 윤곽(알파)은 원본 그대로입니다. 파일 98.6KB → 83.5KB, 원본은 git 기록에 있습니다.
- 화면에서 잎이 떨어지는 도중(0.33초) 프레임을 캡처해 줄기 주변에 얼룩·잔상이 없음을 확인했고, 확대 비교로 줄기가 끝까지 이어지는 것을 확인했습니다. 빌드·diff 검사 통과.

## 지훈 왈봇 플랜 결제일을 첫 접속일 기준으로 (2026-09-18)
- MY의 구독 카드와 `/my/plan` 관리 화면에 `다음 결제일 2026.09.25`, 결제 내역 `2026.08.25`·`2026.07.25`가 고정돼 있었습니다. 다른 시연 날짜처럼 사용자의 첫 접속일(`getDemoFirstVisit`, 한국 시각)을 기준으로 바꿨습니다: 다음 결제일 = 한 달 뒤, 결제 내역 = 이번 달(첫 접속일)·지난달. 첫 접속일을 이번 결제일로 봅니다(처음에 한 달 전·두 달 전으로 넣었다가, 다음 결제일 10/18이면 내역은 9/18·8/18이어야 한다는 사용자 지적으로 고쳤습니다).
- `demoClock.ts`에 `formatDemoMonthOffset(months)`를 추가했습니다. 옮겨 간 달에 같은 날이 없으면 그 달 마지막 날로 맞춥니다(1/31 → 2/28). 월말·연도 경계·한국 시각 자정 전후 9가지 경우를 직접 실행해 확인했습니다.
- 시연 초기화로 첫 접속일이 바뀔 수 있어 모듈 상수가 아니라 렌더할 때 계산합니다. 첫 접속 9/18 기준으로 MY와 관리 화면 모두 다음 결제일 2026.10.18, 결제 내역 2026.09.18·2026.08.18로 표시되는 것을 확인했습니다. 결제 금액·수단은 기존 시연 값 그대로입니다. 타입 검사·빌드·수정 파일 린트·diff 검사 통과.

## 원격 main 4커밋과 합친 뒤 댓글 수 함수 맞춤 (2026-09-18)
- 원격 main의 `20d1ae1`·`aadfa9b`·`33f21a9`·`aa4b50b`를 받아 로컬 커밋을 그 위로 rebase했습니다. 파일 충돌은 없었습니다.
- 원격 커밋이 `utils/plazaComments.ts`를 계정별 저장으로 바꾸면서 `readPlazaComments`가 `readThreadComments(personaId, threadId)`로 바뀌어, 홈 AI 맞춤 추천의 댓글 수가 빌드 오류를 냈습니다. 광장 `CaseFeedCard`와 같은 방식으로 현재 계정 기준 `readThreadComments(personaId, item.id)`를 쓰도록 맞췄습니다.
- 합친 뒤 빌드 통과. 린트는 기존 결과 화면 두 곳의 `Date.now()` purity 오류 2건만 남아 있습니다(`CaseResultPage.tsx`, `ClosedCaseResultPage.tsx`).

## 온보딩 2번째 영상 재생 구간 (2026-09-18)
- 온보딩 2단계(배심원) 영상(`onboarding.mp4`, 전체 5.21초)을 2~5초 구간만 재생합니다. 파일은 그대로 두고 주소 끝에 미디어 프래그먼트 `#t=2,5`를 붙여, 브라우저가 2초부터 틀고 5초에서 멈춥니다. 구간 값은 `OnboardingPage.tsx`의 `ONBOARDING_VIDEO_START`·`ONBOARDING_VIDEO_END`입니다. 재생 속도는 사용자가 바꾼 0.8배 그대로라 3초 분량이 실제로 약 3.75초 동안 재생됩니다.
- 브라우저에서 시작 위치 2초대, 5.01초에서 자동 정지를 확인했습니다. 구간이 확정되면 파일 자체를 잘라 교체할 수 있습니다(현재 PC에는 ffmpeg가 없음). 빌드·온보딩 폴더 린트 통과.
- 후속 요청: 5초에서 뚝 멈추지 않도록 끝부분을 감속합니다. 끝나기 전 0.5초분(영상 기준 4.5~5초)에서 재생 속도를 0.8배에서 0.4배까지 부드러운 곡선(smoothstep)으로 줄입니다. 재생 중 매 프레임 남은 구간을 보고 속도를 정하며, 값은 `ONBOARDING_VIDEO_RATE`·`ONBOARDING_VIDEO_EASE_SECONDS`·`ONBOARDING_VIDEO_END_RATE`입니다. 처음 0.9초·0.3배로 넣었다가 사용자가 0.5초·0.4배로 조정했습니다. 마지막 0.5초분이 실제로는 약 0.9초 동안 재생됩니다. 감속 곡선이 영상 위치에 따라 속도를 줄이는 것을 확인했습니다. 빌드·온보딩 폴더 린트 통과.
- 속도감 재조정(사용자가 맡김): 영상을 0.1초 간격으로 넘겨 프레임 변화량을 재 보니 마지막 프레임까지 움직임이 일정했습니다(스스로 멈추는 장면이 없음). 0.5초·0.4배 감속으로는 멈추는 순간에도 평소의 50% 속도라 움직이던 도중에 얼어붙어 보였습니다. 감속 구간을 영상 기준 마지막 0.8초(4.2~5초), 멈출 때 속도를 0.15배(평소의 19%)로 바꾸고, 끝에서 오래 끄는 smoothstep 대신 사인 곡선을 씁니다. 감속 구간은 실제로 약 1.7초입니다. 위치별 속도 4.4초 0.75 → 4.6초 0.61 → 4.8초 0.4 → 5초 0.15를 확인했습니다. 빌드·온보딩 폴더 린트 통과.

## 광장 시상대 트로피 푸딩 인터랙션 (2026-09-18)
- 시상대 트로피 세 마리는 원래 시상대와 한 장의 그림(`ranking-podium.png`, 1536 × 1024)이라 따로 움직일 수 없었습니다. 받침 기둥이 가장 가는 목 높이(은 y 512, 금 y 398, 동 y 500)에서 머리를 잘라 `ranking-trophy-head-1/2/3.png`로 떼고, 시상대에는 받침과 목만 남긴 `ranking-podium-base.png`를 만들었습니다. 머리 뒤는 원래 투명 배경이라 빈 자리가 드러나지 않고, 네 장 합계 약 800KB로 원본(908KB)보다 가볍습니다. 원본 `ranking-podium.png`은 파일로 남겨 두었습니다.
- `RankingHeroSection`의 `.ranking-podium__image`를 이미지 대신 묶음으로 바꿔 받침 그림과 머리 버튼 세 개를 넣었습니다. 기존 시상대 등장 애니메이션·동작 줄이기 규칙이 묶음에 그대로 적용됩니다. 머리 위치는 `TROPHY_HEADS`에 원본 그림 기준 %로 두어 어떤 크기에서도 제자리에 겹칩니다.
- 머리를 누르면 목 중심을 축으로 820ms 동안 눌림(1.14 × 0.84) → 당겨짐(0.84 × 1.2) → 점점 작아지는 출렁임으로 돌아옵니다. 가로·세로를 반대로 바꿔 부피가 유지되는 것처럼 보입니다. 연타하면 처음부터 다시 출렁이고, 동작 줄이기 설정에서는 움직이지 않습니다. 속도 곡선은 구간마다 걸었습니다(전체에 걸면 12%·32% 지점의 눌림·늘어남이 거의 사라지는 것을 확인해 고침).
- 375px에서 머리 세 개의 위치가 원본 그림과 소수점 둘째 자리까지 일치, 버튼 클릭 판정, 가만히 있을 때 이음새 없음, 가장 늘어난 순간에도 목이 붙어 있음, 구간별 변형 값을 확인했습니다. 타입 검사·빌드·광장 폴더 린트·diff 검사 통과.

## 떠 있는 두 버튼 높이 조정 (2026-09-18)
- 챗봇 버튼이 높게 떠 보인다는 피드백에 따라 16px 내렸습니다. 내비가 있는 화면은 내비 위 36px에서 20px로, 내비가 없는 화면은 화면 아래 28px에서 12px로 바뀝니다. 8px을 먼저 적용해 확인한 뒤 8px을 더 내린 값입니다.
- 맨 위로 버튼도 같은 16px을 내렸습니다(122px→106px, 114px→98px). 두 버튼은 챗봇 높이 80px 위에 6px을 띄워 쌓는 관계라, 한쪽만 내리면 간격이 벌어집니다.
- 배심원 광장과 MY > 내가 쓴 댓글을 실제로 띄워 재 봤습니다. 광장은 챗봇 아래 120px·맨 위로 206px, MY는 12px·98px이고 둘 사이 간격은 그대로 6px입니다. 챗봇 아래쪽과 내비 윗변 사이에 20px이 남아 메뉴를 가리지 않습니다.
- 내비가 없는 화면의 아래 여백에는 `env(safe-area-inset-bottom)`을 더해, 홈 인디케이터가 있는 기기에서도 버튼이 화면 끝에 붙지 않습니다.
- 타입 검사와 배포용 빌드를 통과했습니다. 린트는 이 작업 전부터 있던 `react-hooks/purity` 2건(사건 결과 화면의 `Date.now()`)만 남아 있습니다.

## 트로피 대기 출렁임·사건 카드 전환·투표 중 점 (2026-09-21)

- **트로피가 가만히 있을 때도 살짝 출렁인다.** 눌러보고 싶게 하려는 표시다. 누를 때의 출렁임은 안쪽 `img`가 Web Animations API로 맡고, 이 대기 모션은 바깥 버튼(`.ranking-podium__head`)이 CSS로 맡는다. 서로 다른 요소라 변형이 겹쳐 적용되고 어느 쪽도 끊기지 않는다. 한 요소에 같이 두면 WAAPI가 CSS 애니메이션을 덮어써서, 클릭 출렁임이 끝나는 순간 대기 모션이 주기 중간으로 튄다.
- 기준점은 누를 때와 같은 목 중심(`TROPHY_HEADS.pivotX`)이라 버튼에도 인라인 `transformOrigin`을 넣었다. 주기 2.9초 중 앞 52%(약 1.5초)만 움직이고 나머지는 쉰다. 최대 변형 3.4%(약 2.5px)다. 은→금→동 순으로 1.5·1.9·2.3초 늦게 시작해 셋이 한 몸처럼 움직이지 않는다. 반짝임(3.2초)과 주기가 달라 시간이 갈수록 서로 어긋난다.
- **광장·MY 사건 카드에서 상세로 갈 때 화면이 툭 바뀌지 않는다.** 홈의 오늘의 사건 카드가 쓰던 `entryMotion: 'slide-forward'`를 `CaseFeedCard`의 링크 state에도 실어, 상세가 오른쪽에서 340ms 동안 덮고 들어온다. 카드를 누르는 동안에는 카드가 0.985배로 눌리고 그림자가 낮아진다.
- 이 누름 반응을 위해 카드 등장 애니메이션의 `animation-fill-mode`를 `both` → `backwards`로 바꿨다. `both`면 끝난 뒤에도 애니메이션이 transform을 붙잡고 있어 `:active`의 `scale()`이 먹지 않는다. 끝 장면이 원래 모습(`translateY(0) scale(1)`)과 같아서 보이는 결과는 전과 같다.
- **`투표 중` 배지의 점을 파동으로 바꿨다.** 밝기만 오르내리던 2.4초 호흡은 움직이는지 알아보기 어려웠다. 이제 점이 한 번 뛰고(1.24배) 그 자리에서 파동이 2.9배까지 퍼지며 사라진다(1.7초). 파동은 점의 `::after`라 점의 확대가 함께 곱해지고, 그만큼 배율을 작게 잡았다. 홈 AI 맞춤 추천과 광장 카드가 같은 컴포넌트를 쓰므로 두 화면에 함께 적용된다.
- 세 가지 모두 `prefers-reduced-motion: reduce`에서 멈춘다(트로피는 대기 모션 정지, 카드는 전환·누름 반응 없음, 점은 파동 숨김).
- 검증: 브라우저에서 트로피 대기 모션 3개가 각자의 지연으로 재생되는 것과 최대 변형 3.4%를 실측했고, 클릭 출렁임이 대기 모션과 별개로 돌아가는 것(버튼은 CSS, img는 JS)을 확인했다. 카드 등장 애니메이션이 끝난 뒤 `transform: none`이 되어 누름 반응이 걸리는 것, 카드 클릭 시 상세에 `case-detail--slide-forward`가 붙고 기존 `fromPlaza`·`returnTo` 복원이 유지되는 것, 파동이 1 → 2.9배로 커지며 투명해지는 것을 확인했다. `npm run typecheck`·`npm run build`·수정 범위 린트·`git diff --check` 통과. 기존 500kB 청크 경고는 유지된다. 실기기 터치는 미검증.

## 신규 계정 AI 맞춤 추천 근거 문구 (2026-09-21)

- 추천 부제가 계정과 무관하게 `자주 참여했던 기록을 반영했어요.`로 고정돼 있었다. 가입 직후 서아는 활동이 0건인데 같은 화면 흐름의 MY가 `접수한 사건 0건 / 배심 참여 0건`이라, 시스템이 스스로 밝힌 근거가 화면에서 반박됐다. 발표에서 MY를 함께 보면 바로 드러나는 자리다.
- 생년월일로 방어하기도 어렵다. 가입 폼에서 생년월일은 **선택 항목**이라 비워도 가입된다. 나이만으로 관계·학업을 특정한다는 설명은 근거로 서지 않는다.
- **퍼소나가 아니라 실제 활동 건수로 문구를 고른다.** `activityStats.juryParticipations + activityStats.submittedCases > 0`이면 기존 문구, 0이면 콜드스타트 문구다. MY와 같은 `activityStats`를 보므로 "기록을 반영했다"고 말하는데 MY는 0건인 상황이 원천적으로 생기지 않는다.

| 상태 | 부제 | 본문 |
| --- | --- | --- |
| 활동 0건 | `아직 활동 기록이 없어 지금 투표 중인 사건을 골랐어요.` | `서아님이 시작하기 좋은 / 투표 중인 사건 3건이에요` |
| 활동 1건 이상 | `자주 참여했던 기록을 반영했어요.` | `지훈님에게 맞는 / 직장·관계 갈등 사건 3건을 찾았어요!` |

- **후보를 어디서 뽑는지도 활동 기록이 정한다.** 기록이 있으면 계정별 관심 분야 목록(`personalizedRecommendation`)에서, 없으면 `getPopularVotingCases()`가 내놓는 진행 사건 조회수 순에서 앞의 세 건을 쓴다. 처음에는 콜드스타트도 개인화 목록 앞에서 세 건을 자르고 있었는데, 개인화할 근거가 없다고 말하면서 개인화 목록을 쓰는 셈이라 정렬 기준도 없었다(조회수 197·149·189로 뒤죽박죽).
- 기준을 조회수로 잡은 이유는 **카드에 그 숫자가 이미 찍혀 있어서**다. 말한 기준과 화면을 바로 대조할 수 있다. 조회수+댓글 같은 합성 점수는 보는 사람이 확인할 방법이 없다. 진행 중인 사건만 담는다 — 첫 참여를 권하는 자리라 투표할 수 없는 사건은 소용이 없다.
- 추천 목록·순서·카드·계정별 관심 분야(`personalizedRecommendation`)는 건드리지 않았다. 문구만 `recommendationCopy`(`data/common/homeContent.ts`)로 분리했고, 중복을 남기지 않도록 `homeSectionTitles.aiRecommend.description`은 제거했다.
- 시연에서는 이렇게 읽힌다 — 서아는 신규라 콜드스타트로 시작하고, 첫 투표(축의금·친구)나 사건 접수(동아리 축제·학업)를 하는 순간 관계·학업 맞춤 추천으로 바뀐다. 지훈은 배심 참여 12건을 갖고 시작하므로 처음부터 기존 문구다.
- 검증: 브라우저에서 서아 신규일 때 조회수 257·197·189 내림차순(직장·친구·연인)으로 나오는 것을 확인했다. 서아 신규(콜드스타트) → 추천 사건 투표 1건 → 홈 복귀 시 기존 문구로 전환, 같은 시점 MY가 `배심 참여 1건`으로 일치하는 것을 확인했다. 지훈 로그인 직후에는 처음부터 기존 문구가 나온다. 시연 초기화 후 다시 콜드스타트로 돌아오는 것도 확인했다. `npm run typecheck`·`npm run build`·수정 범위 린트·`git diff --check` 통과. 기존 500kB 청크 경고는 유지된다.

## 로그인 안내 팝업 강조 색 (2026-09-21)

- 사건 접수 팝업 본문의 `AI판결` 강조를 Primary blue(`--blue-700` `#374BFF`)로 바꿨다. `.loginPopUpBody b`에 색만 더했고 굵기·크기·줄바꿈은 그대로다. 이 `b`는 현재 사건 접수 팝업 한 곳에서만 쓰여 다른 팝업(MY·AI 추천·공통)에는 영향이 없다.
- 표기는 사용자가 `AI판결` → `AI 판결`(띄어쓰기)로 함께 수정했다. 시안은 붙여쓰기라 시안 쪽도 맞출지 확인 필요.

## 최근 본 사건을 실제 열람 기록으로 (2026-09-21)

- 시안용 두 건(`case-always-paying`·`case-family-care`)이 `homeContent.ts`에 박혀 있어, 가입 직후 서아에게도 `최근 본 사건`이 이미 있었다. 한 번도 연 적 없는 사건이 `최근 본`으로 뜨는 셈이라 신규 가입 스토리와 맞지 않았다. 두 건은 광장에 사건도 없어 카드를 눌러도 `준비 중` 안내만 떴다.
- **사건 상세를 연 순서대로 쌓이게 바꿨다.** 아무것도 열지 않았으면 섹션을 아예 그리지 않는다. 한 건 열면 한 장, 두 건 열면 두 장이 되고, 그다음부터는 최신 두 건만 남는다.
- 기록은 `utils/recentViewedCases.ts`가 계정별 localStorage 키(`…:<persona>:recent-cases:v1`)로 들고 있다. 배심 참여 기록과 같은 저장소다 — 사건을 열고 홈으로 돌아오는 사이 새로고침이 끼어도 남아야 하고, 투표 기록만 남고 열람 기록만 사라지면 둘이 어긋난다. 키가 `DEMO.storagePrefix`로 시작해 바깥 `시연 초기화`가 함께 비운다. 화면에는 두 장만 쓰지만 6건까지 들고 있다 — 앞쪽 사건이 카드로 만들 수 없을 때 뒤엣것으로 채우기 위해서다.
- 로그인 여부는 따지지 않고 기록한다. 둘러보다 가입하는 서아의 흐름에서 가입 전에 본 사건도 본인이 본 것이 맞다. 그래서 가입 직후 홈에 조금 전 본 축의금 사건이 올라온다.
- **같은 사건은 한 번만 기록한다(`recordedCaseRef`).** 사건을 보는 도중 계정을 전환하면 효과가 다시 도는데, 그때 기록하면 서아가 보던 사건이 지훈 홈에 올라온다. 실제로 재현해 확인하고 막았다. 사건 주소만 바뀌어 같은 화면이 다시 쓰이는 경우는 id가 달라 정상 기록된다.
- 카드에 쓸 사건 정보는 `getRecentCaseCard`(`plazaCaseStories.ts`)가 만든다. 광장 카드가 이미 분야·제목·요약을 갖고 있어 그대로 쓰고, 축의금 사건만 따로 적었다 — 광장 목록에 없고(홈 `오늘의 사건` 전용) 상세의 `summary`가 AI 핵심요약 배열이라 카드 한 줄로 쓸 수 없다.
- 카드를 누르면 `준비 중` 토스트 대신 **그 사건 상세로 간다.** 실제로 열어 본 사건이라 상세가 반드시 있다. 목록→상세와 같은 `slide-forward` 전환을 쓴다.
- **카드 형식을 제목 두 줄 + 요약 한 줄로 고정했다.** 제목은 시안처럼 손으로 끊지 않고 카드 폭에 맞춰 흘린 뒤 두 줄에서 말줄임으로 자르고(`-webkit-line-clamp: 2`), 한 줄짜리 제목도 `min-height: 2.6em`으로 두 줄 자리를 차지한다. 요약은 원래대로 한 줄 말줄임이다. 줄 수가 사건마다 달라지면 카드끼리 요약·시각 높이가 어긋나고 긴 제목은 176px 고정 카드를 밀어낸다. 압정 색(왼쪽 파랑·오른쪽 노랑)은 사건이 아니라 자리로 정한다. 기록이 더 쌓여도 화면은 늘 두 장까지다(`MAX_CARDS`).
- 검증: 시연 초기화 → 서아 로그인 시 섹션 없음 → 사건 1건 열람 후 카드 1장 → 2건째 열람 후 2장(최신이 왼쪽), 카드 클릭 시 해당 상세로 이동하고 `case-detail--slide-forward`가 붙는 것을 확인했다. 제목 길이가 다른 두 사건으로 형식 고정을 실측했다 — 두 카드 모두 제목 2줄·요약 1줄이고 제목/요약/시각의 상단 좌표가 52·102·144px로 같으며 카드 넘침 0이다. 한 줄짜리 제목을 넣어도 요약 위치가 102px로 유지된다. 사건을 보는 도중 지훈으로 전환했을 때 지훈 기록이 비어 있는 것도 저장소 값으로 확인했다. `npm run typecheck`·`npm run build`·수정 범위 린트·`git diff --check` 통과. 기존 500kB 청크 경고는 유지된다.
- 홈 AI 맞춤 추천 카드에도 `slide-forward`를 넘겨 광장·최근 본 사건과 전환을 맞췄다. 이제 목록에서 사건으로 가는 경로는 모두 같은 슬라이드를 쓴다.

## 판멍이 1심 도장 비틀림 제거 (2026-09-21)

- 사건 접수 `AI 1심`(`/cases/new/opinion`)의 판멍이 도장이 찍힌 뒤 비틀리는 것처럼 보였다. `case-opinion-seal`이 `-26deg`에서 내려와 `-9deg`로 닿은 다음 `11deg`까지 20° 더 돌아가서, 착지 후에 회전이 이어진 것이다.
- 전 구간을 최종 각도 `11deg`로 고정했다. 닿고 나서 바뀌는 것은 눌렸다 돌아오는 크기뿐이다(2.1 → 0.9 → 1.03 → 1). 내려오는 속도 곡선·지연(920ms)·먼지 효과는 그대로다.
- 검증: 애니메이션을 멈추고 0~100% 여덟 지점의 변환 행렬에서 각도를 되뽑아 전 구간 11deg인 것을 확인했다. `npm run typecheck`·`npm run build`·수정 범위 린트·`git diff --check` 통과.

## 막상막하 아래 카드 줄다리기·불꽃 모션 (2026-09-21)

- 위 게이지는 바늘이 흔들려 팽팽해 보이는데, 아래 `치열한 공방 중` 카드의 비율 바는 멈춰 있어 같은 섹션 안에서 온도가 달랐다.
- **바의 파랑·빨강 경계가 실제 비율을 중심으로 서로 밀고 밀리게 했다**(`swap-bar-tug`, 3.6초, ±6px 안에서 불규칙). 폭(`width`)은 실제 비율 그대로 두고 `transform`만 움직여서 사건 교환 때의 폭 전환(280ms)과 섞이지 않는다. 파랑이 오른쪽으로 밀릴 때 왼쪽 끝이 비지 않도록 `::before`로 16px을 덧댔다. 바늘과 같이 게이지가 보인 뒤(`close-section--gauge-visible`) 1.5초 지연으로 시작한다.
- **태그는 글자는 두고 불꽃 아이콘만 일렁이게 했다**(`swap-tag-flame`, 1.4초). 아래를 축으로 세로로 솟았다 기우는 정도라 `투표 중` 배지의 점 파동과 같은 크기의 신호다.
- 둘 다 `prefers-reduced-motion: reduce`에서 멈춘다.
- 검증: 모바일 뷰포트 브라우저에서 바의 `translateX`가 -5.8 ~ 5.0px 사이로 바뀌고, 불꽃이 세로 0.95 ~ 1.12배로 움직이는 것을 실측했다. `npm run typecheck`·`npm run build` 통과. 린트는 이번 수정과 무관한 `CaseResultPage.tsx`·`ClosedCaseResultPage.tsx`의 기존 `react-hooks/purity` 에러 2건이 남아 있다. 기존 500kB 청크 경고는 유지된다.

## 최근 본 사건 1건일 때 첫째 자리 고정 (2026-09-21)

- 최근 본 사건이 한 건뿐이면 카드가 섹션 가운데로 갔다. `.recent-section`이 `align-items: center`라 카드 묶음이 내용 폭만큼 줄어든 채 가운데 놓였기 때문이다.
- `.recent-section__grid`에 카드 두 장 폭(`171px * 2 + 12px`)을 자리로 잡았다. 한 장이면 두 장일 때의 첫째 자리(왼쪽)에 선다. 폭을 화면에 맞춰 줄이지 않았다 — 줄이면 좁은 화면에서 두 장일 때 자리가 바뀐다.
- 검증: 375px에서 한 장일 때 카드 left 10.5px, 두 장일 때 10.5px·193.5px로 수정 전 두 장 배치와 같은 것을 실측했다. `npm run typecheck`·`npm run build` 통과.

## 시연 시계 기준점이 하루 넘게 남아 시각이 밀리던 문제 (2026-09-22)

- 후일담 상세의 댓글 네 건이 `방금 전`~`2분 전`으로 적혀 있는데 화면에는 모두 `1일 전`으로 나왔다. 후일담을 방금 썼는지와는 무관했다.
- 원인은 `demoClock.ts`의 첫 방문 기준점이다. 시연 시각은 `지금 - (첫 방문 시각 - 정의된 분)`으로 내는데, 이 기준점은 `시연 초기화`에도 일부러 남긴다(`SessionProvider`의 `clearStoredAppData`가 이 키만 제외한다). 그래서 어제 한 번 열어 본 브라우저로 오늘 들어오면 경과 시간에 하루가 그대로 얹혔다.
- 기준점에 유효 기간 12시간(`FIRST_VISIT_MAX_AGE`)을 뒀다. 넘기면 버리고 지금 시각으로 다시 잡는다. 한 번의 시연 안에서는 여전히 같은 기준점을 써서 사건·댓글의 앞뒤 순서가 유지되고, 전날 리허설한 브라우저로 발표해도 시각이 어긋나지 않는다.
- 키 이름(`walgawalbot:demo:first-visit:v1`)은 그대로 뒀다. `DEMO.storagePrefix`와 어긋나 `resetDemoSession`이 지우지 못하지만, 기준점을 남기는 것은 의도된 동작이라 이름을 맞추면 그 의도가 깨진다.
- 검증: `npm run typecheck`·`npm run build` 통과.
