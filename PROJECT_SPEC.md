# 왈가왈BOT — Figma 디자인 기반 AI 챗봇 지원 커뮤니티 웹앱 구현 기준

이 문서는 강사가 제공한 「Figma 디자인 기반 AI 챗봇 지원 커뮤니티 웹앱 구현 요청」 초안을
왈가왈BOT 프로젝트의 실제 정보로 채운 **단일 기준 문서**다.

기존에 나뉘어 있던 `PRD.md`, `design-analysis.md`, `project_rules.md`, `SKILL.md`와
`AGENTS.md`의 작업 규칙을 모두 이 문서로 흡수하고, 원본 네 개는 삭제했다.

프로젝트 문서는 네 개뿐이다.

| 파일 | 역할 |
| --- | --- |
| `PROJECT_SPEC.md` | **기준** — 잘 바뀌지 않는 것. 서비스·퍼소나·토큰·구현 규칙·검증 기준 |
| `PROJECT_CONTEXT.md` | **상태** — 계속 바뀌는 것. 완료·진행·문제·검증 기록 |
| `CLAUDE.md` · `AGENTS.md` | 위 두 문서를 불러오는 진입점과 짧은 작업 규칙 |

확정되지 않은 것은 이 문서 §9에 모아 둔다. 거기 있는 항목은 임의로 결정하지 않는다.

너는 기존 Figma 디자인과 서비스 기획을 실제 동작하는 웹앱으로 구현하는 프론트엔드 개발자다.
이 프로젝트는 디자인을 새로 만드는 작업이 아니라, 확정된 시안을 코드로 옮기는 작업이다.

---

## 0. 프로젝트 입력 정보

### 0-1. 서비스

- **서비스명**: 왈가왈BOT
- **커뮤니티 주제**: 일상 갈등(관계·금전·약속) 사례를 나누고 여러 관점을 모으는 AI 분쟁 커뮤니티
- **주요 사용자**: 관계·금전·약속 문제로 판단이 서지 않아 다른 사람의 관점을 참고하고 싶은 사용자
- **서비스의 핵심 목적**: 갈등을 안전하게 정리하고 다음 행동을 생각해 보도록 돕는다.
  판결을 내려 주는 곳이 아니라, 비공개를 우선 고려한 AI 정리 → 근거 설명 → 사례 탐색 →
  필요 시 전문가 도움으로 이어지는 보조 도구다.
- **핵심 기능**: 사례 탐색(홈·배심원 광장), 사건 상세와 AI 핵심 쟁점, AI 1심,
  배심원 2심 참여, 사건 접수, 다음 행동 안내, 이어진 이야기(왈가왈후~), MY
- **AI 챗봇의 보조 역할**: 사용 안내 + 정보 탐색 + 유사 사례 추천.
  챗봇 캐릭터명은 시안 기준 `판멍이`다. 전면 챗봇 서비스가 아니라 커뮤니티 이용을 보조한다.

핵심 사용자 흐름:

```
사례 탐색 → 사건 상세 확인 → AI가 정리한 핵심 쟁점 확인 → AI 1심
→ 필요 시 배심원 2심 참여·공개 → 결과와 다음 행동 → 이어진 이야기
```

### 0-2. 사용자 근거 (자료종합 설문)

응답 31명 기준으로 확인한 신호다. 화면 우선순위와 안전 장치의 근거로 사용한다.

| 신호 | 수치 |
| --- | --- |
| AI 결과에 관심 있음 | 84.6% |
| AI의 편향·부정확성 우려 | 57.1% |
| 개인정보·사연 노출 우려 | 50% |
| 민감한 사연을 직접 작성할 의향 | 0% |
| 우선 사례를 구경해 보고 싶음 | 71.4% |
| 개인 상황을 타인에게 말하는 일이 부담됨 | 83.3% |
| 심각한 상황은 전문가 개입이 필요함 | 78.6% |

> 개편 전 17명과 개편 후 14명 설문은 선택 방식이 달라 합산하지 않는다.
> 제품 판단에는 개편 후 결과를 우선 참고한다.

### 0-3. 디자인 및 개발 자료

- **Figma 링크**: https://www.figma.com/design/5msPuamjPpGJOUFl0OXBOX/1조-왈가왈BOT-AI분쟁-
- **구현 대상 페이지·프레임**: `개발` 페이지 (노드 `1301:8758`)에 올라온 화면.
  2026-09-09 팀 결정으로 기준이 `1차 디자인 시안 > 컨펌`(노드 `1136:16182`)에서 옮겨졌다.
  컨펌 섹션은 아직 작업 중인 프레임이 섞여 있어 **확정 여부 판단에는 쓰지 않는다.**
  다만 이미 구현한 홈은 컨펌의 `김하은/홈수정`이 기준이다. → §9-18
- **기획서·IA·유저 플로우**: Figma `자료종합`의 `전제구조ia`, `유저플로우 1`, `유저플로우 2`, 설문
- **공통 시각 기준**: Figma `확정 스타일가이드`, `자료종합 > Font / Color`
- **기존 프로젝트**: `C:\WalgawalBOT` (Vite + React 19)
- **이미지·아이콘·폰트 에셋**: `src/assets/` (`fonts/`, `home/`, `home/figma/`)
- **기존 API 또는 백엔드**: 없음 → 챗봇·인증은 mock으로 구현한다.

컨펌 섹션에서 확인한 프레임:

| 프레임 | 노드 ID | 크기 | 상태 |
| --- | --- | --- | --- |
| 김하은/홈수정 | `1136:16476` | 402 × 3466 | 디자인 확정 |
| 박효민/배심원광장_수정 | `1136:16291` | 402 × 1949 | 작업 중 |
| 박효민/배심원광장 | `1222:19804` | 402 × 1949 | 이전 버전 |
| 사건접수 (박건영) | `1136:16954` | 섹션 | 작업 중 |
| 후일담 | `1136:17535` | 섹션 | 작업 중 |
| 상세페이지 | `1136:18075` | 섹션 | 작업 중 |
| 서아 상세 | `1223:7356` | 402 × 2729 | 작업 중 |
| 마이페이지 | `1136:18580` | 섹션 | 작업 중 |
| WF01~WF05 사건 작성 | `1242:20279`~`1242:20474` | 402 × 874 | 와이어프레임 |

**디자인이 끝난 화면은 홈과 배심원 광장 둘이다.** (2026-09-09 기준)
나머지 프레임은 컨펌 섹션에 올라와 있어도 확정으로 취급하지 않는다.
확정 전 화면을 IA만으로 시각 스타일을 새로 만들지 않는다.

### 0-4. PC 목업 설정

- **기준 기기**: iPhone 17
- **기준 화면 너비**: 402
- **기준 화면 높이**: 874
- **Figma 프레임의 시스템 상태바·홈 인디케이터 포함 여부**:
  - **시스템 상태바 포함됨.** 홈 컨펌 시안의 `top_nav`(402 × 128)는
    `Component 10`(402 × 62 — 시스템 상태바) + `Header`(402 × 66 — 서비스 앱 헤더)로 구성된다.
    → 402 × 62 영역은 **목업의 상태바**로 옮기고, 앱 컴포넌트에서는 렌더링하지 않는다.
    현재 `Home.jsx`가 직접 그리고 있는 `9:41` 상태바는 제거 대상이다.
  - **홈 인디케이터 자리 포함됨.** `down_nav`(402 × 100)를 렌더링해 확인했다.
    메뉴는 `홈 / 배심원 광장 / 사건 접수(중앙 CTA) / 왈가왈후~ / MY`이고,
    하단 여백에 홈 인디케이터 자리가 비워져 있다.
    → 인디케이터 막대는 **목업의 `HomeIndicator`가 그리고**,
    앱의 `BottomNavigation`은 그리지 않는다. 중복되지 않는다.
- **PC 배경 이미지**: 미제공 → 브랜드 색상의 단색 또는 절제된 그라데이션을 사용한다.
  제공되지 않은 이미지를 실제 제공 에셋인 것처럼 사용하지 않는다.
- **PC 배경의 분위기**: 갈등을 안전하게 정리하는 차분한 분위기. 재판·승패를 강조하지 않는다.
- **배경색 및 보조 색상**: Primary blue 계열(`#374BFF`, `#EAF6FF`)을 기본,
  Secondary orange(`#FF9524`)를 절제된 포인트로 사용한다.

세로로 긴 콘텐츠 프레임 전체(홈 3466px)를 기기 화면 높이로 사용하지 않는다.
기기 내부는 402 × 874를 유지하고, 콘텐츠는 내부에서 스크롤한다.

### 0-5. 퍼소나

**A = 윤서아(신규 가입) · B = 곽지훈(기존 사용자). C는 두지 않는다.**

강사 기준 초안은 A·B·C 세 개를 전제하지만, 같은 문서의
"동일한 관심사나 데이터가 기획에 명시돼 있다면 차이를 억지로 만들지 말고 실제 기획을 따른다"에 따라
기존 기획에 있는 두 사용자만 운영한다. 세 번째 퍼소나를 만들어내지 않는다.

**퍼소나 A — 윤서아**
- 구분: 신규 사용자
- 서비스 이용 목적: 자신의 상황을 정리해 사건을 접수하고,
  다른 사람의 다양한 관점과 이후 이야기를 참고한다.
- 관심사: [입력]
- 주요 불편과 필요: [입력]
- 시연할 핵심 행동: [입력]
- 초기 상태: 가입하지 않은 신규 사용자

**퍼소나 B — 곽지훈**
- 구분: 기존 사용자
- 서비스 이용 목적: 민감한 사연을 공개하기보다 AI 1심, 챗봇, 유사 사례로
  먼저 판단 근거를 모은다.
- 관심사: [입력]
- 주요 불편과 필요: [입력]
- 기존 이용·활동 특성: [입력]
- 시연할 핵심 행동: [입력]
- 초기 상태: 가입과 초기 설정을 마친 기존 데모 사용자

**퍼소나 C** — 두지 않는다.

`[입력]`으로 남은 항목은 아직 확정되지 않았다. 값을 지어내지 않는다. → §9-1
코드에서는 `src/data/personas/index.ts`가 이 정의를 그대로 반영한다.

> A(윤서아)의 시작점인 온보딩·가입 화면 시안이 아직 없다. → §9-2

### 0-6. 제품·안전 원칙

1. **둘러보기 우선**: 로그인·작성보다 사례 탐색과 이해를 먼저 제공한다.
2. **안전한 공개**: 민감한 사연은 AI만 보는 흐름과 공개·비공개 범위를 분리해 다룬다.
   공개를 강제하지 않는다.
3. **근거 있는 AI**: AI 결과에는 판단 근거와 양쪽 맥락을 함께 보여 준다.
   AI 결과와 배심원 결과가 다를 수 있음을 숨기지 않는다.
4. **비결정적 언어**: AI 1심은 근거와 양쪽 맥락을 설명하는 보조 의견이며,
   법률·심리의 최종 판단이 아니다. 커뮤니티 의견도 전문 상담을 대체하지 않는다.
5. **도움으로 연결**: 심각한 정서·법률·금전 문제에는 챗봇 또는 전문가 도움 화면으로 안내한다.
   비공개·AI 우선·전문가 도움 진입점을 흐름에서 누락하지 않는다.
6. **관점 보존**: 사건 관점 선택은 승패가 아니다.
   한쪽을 고르게 하지 않고 여러 관점을 함께 남긴다. 항목 수와 문구는 §9-12에서 확정한다.
7. **투표로 끝내지 않기**: 해결 뒤 변화는 `이어진 이야기`로 연결한다.

### 0-7. 실제 사례 카피 기준

임의의 새 사건을 만들지 않고 IA에 이미 적힌 사례를 정적 화면과 더미데이터에 우선 사용한다.

- 친구 축의금 10만원, 적당한가?
- 반려견 개물림 사고, 견주 구속 합당한가?
- 친구에게 300만원 빌려주고 6개월째 미변제
- 전 애인을 친구 모임에 초대해도 될까요?
- 상사가 제 아이디어를 자신의 공로로 발표했어요
- 부모님이 제 결정에 너무 간섭하세요
- 친구가 제 비밀을 다른 친구에게 말했어요

---

## 1. 기존 디자인과 구현 범위

Figma를 시각적 구현의 기준으로 사용한다.
색상, 타이포그래피, 간격, 모서리, 그림자, 이미지 비율, 아이콘, 컴포넌트 상태,
화면별 정보 우선순위를 유지한다.

임의로 디자인 시스템을 바꾸거나 유행하는 스타일을 덧붙이지 않는다.
기존 화면을 일반적인 템플릿형 커뮤니티나 관리자 대시보드로 재해석하지 않는다.

화면 전체를 이미지로 붙이고 투명한 클릭 영역만 얹는 방식은 금지한다.
모든 요소를 절대 위치로 배치하지 않는다.
텍스트, 버튼, 입력창, 목록, 카드 등을 실제 React 컴포넌트로 구현한다.

PC용 앱과 모바일용 앱을 각각 복제하지 않는다.
페이지·라우팅·상태·기능은 공유하고, 외부 표현 방식만 다르게 구성한다.

구현 범위는 이 문서와 Figma를 따른다.
결제, 관리자 기능 등 기획에 없는 기능을 임의로 추가하지 않는다.

### 1-1. 시각 기준 우선순위

| 우선순위 | 자료 | 쓰임 |
| --- | --- | --- |
| 1 | Figma `개발` 페이지의 해당 화면 (없으면 `1차 디자인 시안 > 컨펌`) | 실제 화면 구성, 카피, 상태, 간격의 최종 기준 |
| 2 | Figma `자료종합 > 전제구조ia`, `유저플로우 1, 2` | 화면 간 이동과 정보 전개 기준 |
| 3 | Figma `확정 스타일가이드`, `자료종합 > Font / Color` | 폰트, 컬러, 텍스트 위계의 공통 기준 |
| 4 | 이 문서, `PROJECT_CONTEXT.md` | 제품 목적, 구현 범위, 현재 상태 |

IA와 컨펌 화면이 충돌하면 컨펌 화면을 우선하고,
충돌 내용은 임의로 결정하지 않고 §9에 기록한 뒤 팀에 확인한다.

### 1-2. 확정 스타일가이드 — 타이포그래피

폰트 역할

- 제목: **Paperlogy 7 Bold**
- 일반 본문·UI: **Pretendard Variable**
- 보조 문구: **RIDIBatang**
- 인용구: **Noto Sans KR Bold**
- **Cafe24 Ssukssuk**: 도라에몽 쉘 제목에만 쓰는 한정 장식 폰트. 공통 UI에 사용하지 않는다.

공통 텍스트 스케일

| 용도 | 크기 / 굵기 / 줄간격 |
| --- | --- |
| 큰 수치 | 40 Bold / 100%, 32 Bold / 100% |
| 화면 핵심 제목 | 28 Bold / 130%, 26 Bold / 120% |
| 섹션 제목·강조 | 20 Bold / 120%, 20 Medium / 120%, 20 Regular / 120% |
| 카드 제목·강조 문구 | 18 Bold·SemiBold·Medium·Regular / 120% |
| 본문 강조 | 16 Bold / 130% |
| 본문 | 16 Medium·Regular / 130% |
| 보조 본문 | 14 SemiBold·Medium·Regular·Light / 130% |
| 작은 라벨 | 13 SemiBold·Regular / 130%, 12 Bold·Medium / 120% |
| 메타 정보 | 11 Regular / 140% |
| 하단 내비게이션 | 14 Medium / 100% |

스타일가이드에 `미정`으로 표시된 필드는 이 표의 위계를 임시 적용하되,
별도 신규 텍스트 스타일을 만들지 않는다.

프로젝트에 실제로 들어와 있는 폰트 파일은 `Paperlogy-7Bold.ttf`, `PretendardVariable.ttf` 두 개다.
RIDIBatang과 Noto Sans KR은 아직 에셋으로 정리되지 않았다. → §9

### 1-3. 확정 스타일가이드 — 컬러

Neutral
`#252525` · `#424042` · `#59565B` · `#78757A` · `#A29FA3` · `#BFBCC1` · `#D5D2D6` · `#E0DDE2` · `#E8E6EA` · `#F0EEF1` · `#FAFAFA`

> 자료종합 `Color` 섹션의 Neutral은 12단계이고 `#78757A`와 `#A29FA3` 사이에 한 단계가 더 있다.
> 그런데 색상 칩의 실제 값(`#939094`)과 옆에 적힌 표기(`#818181`)가 서로 다르다.
> 확정 전까지 이 단계는 `tokens.css`에 넣지 않는다. → §9-19

Primary blue
`#1035C7` · `#3043EA` · `#374BFF` · `#2863FA` · `#377DFF` · `#4D8EFD` · `#659EFF` · `#81B7FF` · `#B4D7FF` · `#D8EAFF` · `#EAF6FF` · `#F9FAFD`

Secondary orange
`#F57200` · `#FD8013` · `#FF9524` · `#FFA748` · `#FFB361` · `#FFC27D` · `#FFD396` · `#FFDDA7` · `#FFEABD` · `#FFF6DC` · `#FEFBEF` · `#FFFDF8`

Semantic / accent
- Positive: `#374BFF`
- Negative: `#FF6358`
- Bright green: `#DEFF73`
- Green: `#83D400`
- Pink: `#FFBBBB`
- Dark brown: `#614D28`
- Brown: `#8D786D`

컬러는 장식이 아니라 정보의 역할에 맞춰 쓴다.
파랑은 선택·강조·긍정, 빨강은 부정·주의, 주황은 보조 포인트로 사용한다.
한 카드 안에서 포인트 색을 여러 개 경쟁시키지 않는다.
공통 컬러 스케일 밖의 새 색을 임의로 확정하지 않는다.

### 1-4. 레이아웃 원칙

- 기준 아트보드: **402 × 874px (iPhone 17)**.
  기본 콘텐츠 폭은 약 **354px**, 좌우 여백은 **24px**를 우선 기준으로 한다.
- 이 크기는 **PC 목업 내부의 기준 크기**다. 모바일에서는 고정하지 않는다(§3).
- 긴 콘텐츠는 기기 내부에서 세로 스크롤한다.
- 공통 상단바·하단바·중앙 사건 접수 CTA는 각 화면에서 같은 구조와 여백을 유지한다.
- 카드 내부에는 제목, 보조 정보, 행동 요소가 섞이지 않도록 충분한 세로 간격을 둔다.
- 텍스트가 커질 때는 고정 높이를 유지하지 말고 카드·버튼·행 높이와 줄바꿈을 함께 조정한다.
- `Corner_Radius(임시)`는 전역 확정값이 아니다.
  확정 시안에서 확인되는 반경을 우선 사용하고, 새 반경을 전역 토큰으로 단정하지 않는다.
  자료종합에 적힌 임시 기준은 참고용으로만 쓴다 —
  바깥 코너 12 · 안쪽 주황 8 · 박스 안의 박스 8 또는 12 · 카운터 박스 4 · 버튼과 칩 50(pill).
  같은 섹션의 인용 카드 배경 `#FFF6E8`은 §1-3 스케일에 없는 색이다. → §9-19
- 버튼은 `button`, 화면 이동은 링크·라우팅 요소처럼 의미에 맞는 요소를 쓴다.
- 아이콘은 `src/assets/home/figma/`의 실제 제공 에셋을 우선 사용한다.
  임의 SVG·이모지로 대체해야 하면 팀에 명시한다.

### 1-5. 구현 범위와 제외 범위

제외 범위

- 실제 계정, 데이터베이스, 실시간 투표, 알림 발송
- 실제 AI 모델 호출·자동 판정·법률/심리 전문 자문·전문가 예약
- 컨펌되지 않은 화면·외부 연동·이미지·캐릭터의 임의 추가

데모 범위에서 허용되는 것

- mock 챗봇 응답, 데모 회원가입·로그인, localStorage 기반 시연 상태 유지
- 정적 더미데이터에 기반한 투표 수치·참여 수치 표현

---

## 2. PC 화면: 가운데 기기 목업 + 서비스 관련 배경

### 외부 쇼케이스 영역

PC에서는 브라우저 화면 중앙에 기기 목업을 배치한다.
주변에는 서비스와 관련된 배경 또는 브랜드 배경을 표시한다.

배경 이미지, 배경색, 오버레이 강도, 기기 크기는 `src/config/`에서 교체할 수 있게 한다.
배경 이미지가 미제공이므로 브랜드 색상의 단색 또는 절제된 그라데이션을 사용한다.
제공되지 않은 이미지를 실제 제공 에셋인 것처럼 사용하지 않는다.

서비스명, 짧은 설명, 퍼소나 선택 도구는 기기 바깥에 둘 수 있다.
이 보조 영역 때문에 기기가 브라우저의 가로 중앙에서 밀리지 않게 한다.

### 기기 목업

목업에는 다음 시각 요소를 포함한다.

- 기기 외곽 프레임과 베젤
- 화면의 둥근 모서리
- 상단 시스템 상태바
- iPhone 17 기준 다이내믹 아일랜드
- 하단 홈 인디케이터

시스템 상태바는 서비스 내부의 앱 헤더와 다른 요소다.
홈 인디케이터는 서비스의 하단 내비게이션과 다른 요소다.

**중복 주의**: 홈 컨펌 시안의 `top_nav` 상단 62px가 시스템 상태바다(§0-4).
이 영역은 목업의 `StatusBar`가 담당하고, 앱 컴포넌트는 그 아래 `Header`(66px)부터 그린다.
현재 `Home.jsx`의 `status-bar` 마크업은 목업으로 옮기면서 제거한다.

목업 장식은 클릭, 터치, 키보드 탐색을 방해하지 않게 한다.

**구현하며 실제로 틀렸던 것들. 고칠 때 다시 밟지 않는다.**

- **바깥 치수는 화면 + 30px이다.** 화면(402 × 874) 바깥에 검은 베젤 3px × 2와
  금속 프레임 12px × 2가 있다. 베젤 여백을 빼먹으면 화면이 베젤을 밀고 나가
  한쪽 테두리만 얇아진다. `src/config/app.ts`의 `DEVICE_FRAME`이 기준값이다.
- **`transform: scale()`은 레이아웃 자리를 줄이지 않는다.** 축소해도 원래 높이만큼
  자리를 차지해 기기 아래에 빈 공간이 남고 페이지가 스크롤된다.
  바깥 래퍼에 축소된 크기만큼만 자리를 잡아 준다.
- **축소 비율은 여백을 뺀 높이로 계산한다.** 위아래 여백은 축소되지 않으므로
  `(창 높이 − 여백) / 기기 높이`로 낸다.
- **기기 안에 데스크톱 스크롤바가 보이면 안 된다.** 실제 휴대폰에는 상시 스크롤바가 없고,
  막대가 보이면 콘텐츠 폭도 그만큼 깎인다. `scrollbar-width: none`과
  `::-webkit-scrollbar { display: none }`로 막대만 감추고 스크롤은 유지한다.
- **보조 패널은 절대 위치로 붙이지 않는다.** 좌 패널 · 기기 · 우 패널을 같은 폭으로
  한 줄에 놓으면 기기는 산술적으로 중앙에 온다. 절대 위치는 창 크기에 따라 어긋난다.
- **'화면에 맞추기'에서는 페이지 스크롤을 막는다.** 세로 스크롤바가 생기면
  그 폭만큼 가로 중앙이 밀린다.

### 크기와 스크롤

기기 내부는 402 × 874를 유지한다. 콘텐츠가 길어져도 기기 자체가 세로로 늘어나지 않게 한다.

PC 브라우저 높이가 낮으면 목업 전체를 비율에 맞춰 축소하는 `화면에 맞추기` 방식을 제공한다.
이때 앱 내부의 기준 레이아웃 크기를 임의로 바꾸지 않는다.
축소하지 않은 `원본 크기 보기`도 가능하게 하고, 이 경우 바깥 영역의 스크롤을 허용한다.

일반 화면에서는 앱의 콘텐츠 영역을 스크롤한다.
앱 헤더와 하단 내비게이션의 고정 여부는 Figma 설계를 따른다.

처음 보는 사람이 스크롤 가능 여부를 알 수 있도록
기기 바깥에 `기기 안에서 스크롤해 보세요` 같은 안내를 표시하고, 스크롤 후에는 줄이거나 숨긴다.

모달, 바텀시트, 토스트, 플로팅 버튼은 브라우저 전체가 아니라 기기 내부 화면을 기준으로 표시한다.
모달의 어두운 배경이 PC 배경 전체를 덮지 않게 한다.

---

## 3. 모바일 화면: 목업 제거 + 실제 기기에 맞춘 웹앱

전환 기준은 화면 너비 **1024px**을 기본안으로 한다.
1024px 이상은 PC 쇼케이스, 미만은 목업 없는 앱 화면이다.
이 기준은 `src/config/`에서 조정할 수 있게 한다.

User-Agent 문자열에만 의존하지 않고 화면 크기를 기준으로 대응한다.

모바일에서 제거하는 요소

- PC용 외부 배경과 서비스 소개 영역
- 기기 외곽 프레임과 베젤
- 목업용 상태바와 다이내믹 아일랜드
- 목업용 홈 인디케이터
- PC용 스크롤 안내

서비스 자체의 헤더, 뒤로가기, 하단 내비게이션은 유지한다.

**모바일에서는 앱을 402 × 874로 고정하지 않는다.**
너비는 실제 화면에 맞추고, 높이도 현재 표시 가능한 영역에 대응한다.
PC 목업을 통째로 축소한 화면을 모바일에 보여주는 방식은 금지한다.

> 이 항목은 기존 `AGENTS.md`의 "기준 아트보드 402 × 874px" 규칙을 대체한다.
> 402 × 874는 **디자인 기준 크기이자 PC 목업 내부 크기**이지, 모바일의 고정 크기가 아니다.

`index.html`의 viewport를 다음으로 교체한다.

```
width=device-width, initial-scale=1, viewport-fit=cover
```

사용자의 확대를 막는 `user-scalable=no` 같은 설정은 사용하지 않는다.

화면 높이는 `100dvh`를 우선 검토하고 필요한 fallback을 둔다.
노치와 홈 인디케이터 주변은 `env(safe-area-inset-*)`를 활용한다.
기존 디자인 여백과 안전 영역이 중복 적용되지 않게 한다.

상단 헤더, 스크롤 콘텐츠, 하단 내비게이션의 영역을 명확히 나눈다.
불필요한 이중 스크롤과 가로 스크롤을 만들지 않는다.

키보드가 열렸을 때 다음을 확인한다.

- 가입 폼의 현재 입력창이 가려지지 않는다.
- 댓글 입력창과 전송 버튼을 사용할 수 있다.
- 챗봇 입력창과 최근 메시지를 확인할 수 있다.

`100dvh`와 안전 영역 설정만으로 키보드 대응이 완료됐다고 판단하지 않는다.
실제 브라우저 동작을 확인하고 필요한 경우 추가 보정한다.

화면 크기가 바뀌어도 현재 페이지, 입력 내용, 로그인 상태가 초기화되지 않게 한다.

---

## 4. 퍼소나별 시연 진입 구조

A·B는 별도의 앱이나 관리자 권한 등급이 아니다.
같은 서비스에서 서로 다른 사용자 여정을 체험하기 위한 시연 시나리오다.

### 시연용 진입 도구

실제 서비스 UI와 구분되는 퍼소나 선택 도구를 제공한다.

- PC: 기기 바깥의 패널
- 모바일: 별도의 데모 진입 화면 또는 접을 수 있는 도구

서비스의 가입 화면 안에 퍼소나 선택 항목을 임의로 삽입하지 않는다.

선택 도구에 드러낼 정보

- 퍼소나 이름
- 신규 사용자 / 기존 사용자 구분
- 주요 이용 목적
- 이 퍼소나로 체험하기
- 해당 퍼소나의 시연 데이터 초기화

### A(윤서아): 신규 가입 시나리오

윤서아는 **비로그인 상태로 시작해 서비스를 둘러보다가, 투표하려는 순간 가입한다.**
제품 원칙 1번 "둘러보기 우선"과 같은 흐름이다. (§0-6)

```
첫 진입(비로그인) → 홈 → 오늘 사건의 `투표하러 가기`
→ 사건 상세 투표 화면 → `로그인하고 나도 투표하기`
→ 회원가입 → 원래 보던 사건 상세로 복귀
```

**회원가입 화면(`/signup`)은 Figma 컨펌 시안이 없다.**
발표 시연을 위해 팀 결정으로 추가한 화면이며, 확정 스타일가이드 토큰만 사용한다.
시안이 나오면 그 기준으로 교체한다. → §9-2

구현 규칙

- 입력 항목은 이메일 · 비밀번호 · 닉네임 세 가지다.
- 윤서아의 더미 정보가 흐리게 미리 채워져 있고 **직접 수정할 수 없다**(readOnly).
- `더미텍스트 입력` 버튼을 누르면 값이 확정되어 진해지고, 그때부터 가입 버튼이 활성화된다.
  발표에서 타이핑 없이 빠르게 넘어가기 위한 장치다.
- 가입 후에는 `?from=` 으로 받은 앱 내부 경로로 되돌아간다. 외부 URL은 받지 않는다.
- 실제 백엔드가 없는 데모 동작이며, **비밀번호는 어디에도 저장하지 않는다.** (§6)
- A의 개인 활동 기록은 비워 두되, 공개 피드(홈·광장의 사건 목록)는 비로그인도 볼 수 있다.

온보딩 화면과 별도 로그인 화면은 만들지 않는다. 시안도 없고 시연 흐름에도 없다.

### B(곽지훈): 기존 사용자 시나리오

곽지훈은 **가입 흐름을 거치지 않는다.**
발표에서 윤서아 순서 다음에 이어지므로, **계정 전환 버튼으로 서아 → 지훈으로 바꾸는 장면**만 보여준다.

- 전환 즉시 가입과 초기 설정을 마친 로그인 상태로 진입한다.
- 전환 도구는 기기 바깥 패널의 `PersonaSwitcher`다. 서비스 화면 안에 넣지 않는다.
- 곽지훈의 관심사·활동 특성에 맞는 프로필·피드·개인 기록을 연결한다.
  (세부 데이터는 아직 확정 전 → §9-1)

> 마이페이지 섹션의 `ProfilePopup`(프로필을 전환했습니다) 시안이 이 동작과 같은 맥락이지만
> 아직 확정 전이라 그대로 구현하지 않았다. → §9-11

### C: 두지 않는다

세 번째 퍼소나는 만들지 않는다.
B의 이름과 프로필만 바꾼 복제 데이터를 만드는 것보다,
기획에 실제로 있는 두 사용자를 정확히 표현하는 편이 낫다고 판단했다.

### 진입과 상태 유지

퍼소나를 선택한 최초 진입에서는 해당 시작 경로로 이동한다.
그 뒤 화면을 이동하거나 새로고침할 때마다 시작 화면으로 보내지 않는다.

새로고침하면 선택한 퍼소나와 진행 상태를 복원한다.
처음부터 다시 체험하는 동작은 `시연 초기화`로 구분한다.

발표용으로 퍼소나별 직접 진입 URL을 제공한다.
URL의 퍼소나 값은 시연 상태 선택에만 사용한다.
퍼소나 선택이나 URL 파라미터를 실제 인증·권한 부여 수단으로 사용하지 않는다.

---

## 5. 퍼소나별 더미데이터와 상태 관리

더미데이터는 컴포넌트 TSX에 흩어 넣지 않고 `src/data/`에서 별도 파일로 관리한다.
공통 커뮤니티 데이터(`data/common/`)와 퍼소나별 개인 데이터(`data/personas/`)를 구분한다.

기획에 존재하는 항목에 한해 다음을 구성한다.

- 프로필과 관심사
- 관심 카테고리 또는 팔로우한 주제
- 홈 피드의 구성과 노출 순서
- 작성한 사건과 댓글
- 좋아요와 저장한 사건
- 알림과 읽음 상태
- 챗봇 이용 기록 또는 추천 질문

기획에 없는 기능을 더미데이터를 위해 새로 만들지 않는다.

사건·작성자·댓글·저장 기록 등의 ID 관계를 일관되게 유지한다.
프로필의 작성 글 수와 실제 목록처럼 서로 연결된 수치도 맞춘다.

사례 문구는 §0-7의 IA 사례를 우선 사용한다.
`게시글 1`, `내용입니다`, Lorem ipsum으로 주요 화면을 채우지 않는다.
실제 학생의 개인정보나 실제 계정 정보는 사용하지 않는다.

같은 퍼소나로 다시 시연했을 때 결과를 재현할 수 있도록 초기 데이터를 무작위로 바꾸지 않는다.
시연 중 작성·좋아요·저장 등의 변경 사항은 상태에 반영하고 새로고침 이후에도 유지한다.

퍼소나별 저장 공간을 분리한다. 예: `walgawalbot:demo:v1:A` / `:B` / `:C`

공통 초기 데이터는 공유할 수 있지만, 시연 중 변경되는 데이터는 퍼소나별로 격리한다.
퍼소나를 전환할 때 이전 사용자의 입력 중인 폼, 개인 기록,
진행 중인 챗봇 응답이 다른 퍼소나에 섞이지 않게 한다.

초기화는 해당 서비스와 해당 퍼소나 데이터에만 적용한다.
`localStorage` 전체를 지우는 방식은 사용하지 않는다.

브라우저 저장소를 사용할 수 없거나 저장 데이터가 손상된 경우에도
앱이 흰 화면으로 멈추지 않도록 안전한 초기 상태로 복구한다.

---

## 6. 커뮤니티 인터랙션과 AI 챗봇

### 커뮤니티

Figma에 표현된 주요 버튼과 사용자 흐름이 실제로 연결되게 한다.
목록 → 상세 → 댓글, 좋아요, 저장, 검색, 탭 전환, 프로필 이동 등이 동작하게 한다.

버튼을 눌렀을 때 성공 알림만 띄우고 실제 상태는 변하지 않는 형식적인 구현은 하지 않는다.
로딩, 데이터 없음, 검색 결과 없음, 입력 오류, 처리 실패 상태도 필요한 화면에 구현한다.

사건 상세의 관점 선택은 승패 투표가 아니다.
여러 관점을 그대로 남기는 것이 목적이며, **항목 수와 문구는 아직 확정되지 않았다. → §9-12**
확정 전까지는 `OpinionChoice`가 항목 배열을 prop으로 받도록 만들고,
컴포넌트 안에 문구나 개수를 고정하지 않는다.

밸런스 게임의 선택 전·선택 후 시각 상태는 화면 로컬 상태로 표현한다.

### AI 챗봇

이 서비스의 중심은 커뮤니티다. AI 챗봇은 사용자의 커뮤니티 이용을 보조한다.
전면 챗봇 서비스로 바꾸지 않는다. 챗봇의 위치·진입 방법·화면 구조는 Figma를 따른다.

현재 퍼소나의 관심사와 이용 목적을 추천 질문이나 응답 맥락에 반영할 수 있게 한다.
같은 질문이라도 맥락이 실제로 다를 때만 차이를 둔다.

메시지 입력·전송, 응답 대기, 오류 안내, 재시도가 동작해야 한다.

AI 호출 로직은 UI 컴포넌트와 분리해 `src/services/`에 둔다.
실제 API와 mock 응답을 교체할 수 있는 구조로 만든다.

**이 프로젝트는 백엔드가 없으므로 mock 모드로 구현한다.**
정해진 응답을 실제 생성형 AI 응답이라고 설명하지 않는다.
시연 도구와 결과 설명에서 mock 여부를 확인할 수 있게 한다.

AI 제공자의 비밀 API 키를 브라우저 코드에 넣지 않는다.
`VITE_` 환경변수에 비밀키를 넣는 방식도 금지한다.
API 오류 발생 시 mock 응답으로 조용히 바꾸고 실제 AI가 응답한 것처럼 표시하지 않는다.

회원가입과 로그인도 실제 백엔드가 없으므로 데모 동작임을 구분한다.
실제 비밀번호를 `localStorage` 등에 저장하지 않는다.

AI 판정 결과는 §0-6의 안전 원칙을 따른다.
결론만 보여주지 말고 핵심 쟁점·양쪽 맥락·근거·한계를 함께 보여 준다.

---

## 7. 라우팅·폴더 구조·컴포넌트·상태 관리 구현 규칙

학생들이 코드를 읽고 담당 기능을 수정할 수 있도록 구현한다.
한 파일에 모든 화면과 로직을 넣지 않는다.
반대로 작은 기능을 지나치게 많은 파일과 추상화로 분리하지 않는다.

### 7-1. 기술 구성과 기존 프로젝트 확인 결과

**기술 구성 (2026-09-08 전환 완료)**

| 항목 | 전환 전 | 현재 |
| --- | --- | --- |
| 빌드 도구 | Vite `^8.2.2` | 그대로 (`vite.config.ts`) |
| 프레임워크 | React `^19.2.8` | 그대로 |
| 언어 | JavaScript | **TypeScript** (`tsconfig.json` 프로젝트 참조 방식) |
| 라우터 | 없음 (`App.jsx`가 `Home` 직접 렌더) | **React Router Declarative** (`BrowserRouter`) |
| 스타일 | 일반 CSS | 그대로 + `src/styles/tokens.css` 신설 |
| 상태 관리 | `useState`만 | `useState` + `SessionProvider`(Context) |
| API | 없음 | 없음 (챗봇·인증 모두 mock 예정) |
| 스크립트 | `dev` `build` `lint` `preview` | **`typecheck` 추가**, `build`가 타입 검사 선행 |
| 린트 | ESLint `^10.9.0` | + `typescript-eslint`, 대상 `**/*.{ts,tsx}` |

> `tsconfig.json`은 프로젝트 참조(solution) 방식이라 `tsc --noEmit`은 아무것도 검사하지 않는다.
> 반드시 빌드 모드 `tsc -b`를 쓴다. (`npm run typecheck`)

**결정 사항**

- **언어: TypeScript로 전면 전환한다.** (팀 결정, 강사 기준 준수)
  - `typescript`, `@typescript-eslint/*`를 설치하고 `tsconfig.json`, `tsconfig.node.json`을 추가한다.
  - `main.jsx` → `main.tsx`, `App.jsx` → `App.tsx`, `Home.jsx` → `HomePage.tsx`로 마이그레이션한다.
  - `index.html`의 스크립트 경로도 `/src/main.tsx`로 바꾼다.
  - `package.json`에 `"typecheck": "tsc -b --force"` 스크립트를 추가한다.
    (초안은 `tsc --noEmit`이었으나 프로젝트 참조 방식에서는 아무것도 검사하지 않는다. 위 주의 참고)
  - ESLint 설정에 TypeScript 파서를 연결한다.
- **라우터: React Router의 Declarative Mode를 도입한다.** (현재 라우터가 없으므로 기본안 채택)
  - `BrowserRouter`: 브라우저 URL과 앱 연결
  - `Routes` / `Route`: 경로와 페이지 연결
  - `Outlet`: 공통 레이아웃 안의 하위 페이지 표시
  - `Link` / `NavLink`: 사용자가 누르는 페이지 이동 링크
  - `useNavigate`: 가입 완료 등의 처리 이후 이동
  - `useParams`: 사건 ID 등의 경로 값 확인
  - `useSearchParams`: 검색어·필터 등의 쿼리 값 관리
  - `BrowserRouter`와 `RouterProvider`를 중복으로 감싸지 않는다.
  - 설치된 버전과 맞지 않는 import나 다른 모드 전용 API를 섞지 않는다.
- **스타일: 기존 방식(파일 단위 일반 CSS)을 유지한다.**
  강사 기준의 "스타일은 기존 프로젝트 방식을 유지한다"를 따른다.
  전역 토큰은 `src/styles/tokens.css`를 신설해 §1-2, §1-3의 값을 CSS 변수로 정의하고,
  페이지·컴포넌트 CSS는 해당 폴더에 가깝게 둔다.
- **상태 관리: `useState` + Context를 기본으로 한다.**
  상태 변경 규칙이 복잡해지면 `useReducer`를 검토한다.
  명확한 이유 없이 Redux·Zustand 등을 추가하지 않는다.

Tailwind, UI 라이브러리는 추가하지 않는다.
라우터·TypeScript 추가를 이유로 프로젝트를 재생성하거나
Next.js, SSR, React Router Framework Mode로 전환하지 않는다.

### 7-2. 폴더 구조와 파일별 책임

필요한 파일만 생성하고 빈 폴더와 불필요한 파일을 미리 양산하지 않는다.
페이지 폴더명은 이미 있는 `Home/`, `Plaza/`, `Case/`, `Submit/`, `My/`의 대문자 표기를 따른다.
비어 있는 `src/common/`은 쓰임이 확인될 때 채운다.

아래는 **목표 구조**다. 2026-09-09 기준으로 아직 만들지 않은 것은
`routes/guards/`, `layouts/DetailLayout.tsx`, `pages/AfterStory/`, `components/case/`,
`services/`, `utils/`이고, `pages/Case/` · `pages/Submit/` · `pages/My/`는 빈 폴더다.
해당 화면을 맡을 때 그 폴더만 만든다.

```
src/
├─ main.tsx
├─ App.tsx
│
├─ routes/
│  ├─ AppRoutes.tsx
│  ├─ paths.ts
│  └─ guards/
│     ├─ RequireAuth.tsx
│     └─ GuestOnly.tsx
│
├─ layouts/
│  ├─ ShowcaseLayout.tsx
│  ├─ AuthLayout.tsx
│  ├─ MainLayout.tsx
│  └─ DetailLayout.tsx
│
├─ pages/            # 기존 프로젝트의 대문자 폴더명을 유지한다
│  ├─ Home/           # HomePage.tsx, Home.css
│  ├─ Plaza/          # 배심원 광장
│  ├─ Case/           # 사건 상세 · AI 1심
│  ├─ Submit/         # 사건 접수
│  ├─ AfterStory/     # 왈가왈후~ (이어진 이야기)
│  ├─ My/
│  ├─ Auth/           # SignupPage.tsx (시안 없이 추가한 데모 화면)
│  └─ Error/          # NotFoundPage.tsx
│
├─ components/
│  ├─ common/         # BottomNavigation, Button, Chip, Tabs, SectionHeader, Card
│  ├─ device/         # DeviceFrame, StatusBar, HomeIndicator, AppViewport
│  ├─ demo/           # PersonaSwitcher — 실제 서비스 UI와 구분되는 시연 도구
│  └─ case/           # CaseCard, CaseMeta, AiIssueSummary, OpinionChoice
│
├─ hooks/             # useDemo, useChatbot 등
├─ state/             # 퍼소나·세션·공유 데이터 Provider
├─ services/          # 사건 조회·변경, 챗봇 요청 (실제 API ↔ mock 교체 지점)
├─ data/
│  ├─ common/         # 공통 커뮤니티 seed
│  └─ personas/       # A(윤서아)·B(곽지훈) 개인 seed
├─ types/             # User, Persona, Case, Comment, ChatMessage
├─ utils/
├─ config/            # 서비스명, 기기 크기, 배경, 브레이크포인트, 데모 모드
├─ styles/            # tokens.css, 전역 초기화
└─ assets/            # fonts/, home/, home/figma/
```

각 영역의 책임

- **main.tsx** — 앱 시작점, 전역 스타일 로드, Router를 한 번만 연결
- **App.tsx** — 전역 Provider와 라우트 연결. 개별 화면 UI나 더미데이터를 넣지 않음
- **routes/** — URL과 페이지 연결, 경로 생성 함수, 접근 제어
- **layouts/** — 여러 페이지가 공유하는 화면 틀. 개별 사건 데이터나 챗봇 요청을 직접 관리하지 않음
- **pages/** — URL에 대응하는 실제 화면. 한 페이지에서만 쓰는 조각은 해당 폴더에 둠
- **components/** — 공통 UI, 목업 요소, 재사용되는 커뮤니티 UI
- **hooks/** — 여러 곳에서 쓰거나 페이지에서 분리할 필요가 있는 React 로직
- **state/** — 선택한 퍼소나, 데모 세션, 여러 화면이 공유하는 변경 데이터
- **services/** — 데이터 처리 경계. 실제 API와 mock 구현 교체 위치
- **data/** — 화면에서 직접 수정하지 않는 원본 seed
- **types/** — 공통 타입
- **utils/** — React 상태가 필요 없는 일반 함수
- **config/** — 서비스명, 기기 크기, 배경, 브레이크포인트, 데모 모드 설정
- **styles/** — 전역 초기화 스타일과 공통 디자인 토큰
- **assets/** — 이미지, 아이콘, 폰트

컴포넌트 분리 원칙

- 화면은 한 개의 큰 파일로 만들지 않는다. 페이지는 섹션을 조립하는 역할만 맡는다.
- 공통 여부가 확실하지 않은 첫 사용 컴포넌트는 페이지 전용 폴더에 두고,
  두 번째 사용이 확인되면 공통 폴더로 올린다.

디자인 단위와 컴포넌트 단위 대응

| 디자인 단위 | React 단위 | 재사용 위치 |
| --- | --- | --- |
| 상단바·하단바 | TopBar, BottomNavigation, MainLayout | 전 화면 |
| 섹션 제목과 더보기 | SectionHeader | 홈·광장·왈가왈후~·MY |
| 사건 요약 카드 | CaseCard, CaseMeta | 홈·광장·최근 본 사건·MY |
| 태그·탭·상태 | Chip, Tabs, StatusBadge | 탐색·필터·내 사건 |
| AI 안내와 핵심 쟁점 | AiInsight, AiIssueSummary | 홈·상세·다음 행동 |
| 관점 선택 | OpinionChoice | 사건 상세·밸런스 게임 |
| CTA | Button | 접수·투표·다음 행동 |
| 기기 목업 | DeviceFrame, StatusBar, HomeIndicator, AppViewport | ShowcaseLayout |

### 7-3. 화면·URL 대응표

**현재 디자인이 확정된 화면은 홈과 배심원 광장이다.** 나머지 행은 시안 확정 후 채운다.
확정 여부는 Figma `개발` 페이지에 올라온 화면인지로 판단한다. (§0-3)
Figma 프레임 하나를 무조건 페이지 하나로 만들지 않는다.
독립적인 방문·공유·뒤로가기가 필요한 화면만 라우트로 구분하고,
버튼 상태·오류 표시·단순 모달은 컴포넌트 상태로 처리한다.

| Figma 프레임 | 화면명 | URL | 페이지 컴포넌트 | 레이아웃 | 로그인 | 상태 |
| --- | --- | --- | --- | --- | --- | --- |
| — | 최초 진입 분기 | `/` | EntryRedirect | — | 불필요 | 구현 예정 |
| — | 시연용 퍼소나 선택 | `/demo` | DemoPage | ShowcaseLayout | 불필요 | 보류 — 가입 시안 부재(§9-2) |
| 김하은/홈수정 | 홈 | `/home` | HomePage | MainLayout | 불필요 | **디자인 확정, 구현됨** (에셋 교체 남음) |
| 개발 > 광장 > JuryPlazaScreen | 배심원 광장 | `/plaza` | PlazaPage | MainLayout | 불필요 | **디자인 확정, 구현됨** (비트맵 에셋 3종 남음) |
| 상세페이지 · 서아 상세 | 사건 상세 · AI 1심 | `/cases/:caseId` | CaseDetailPage | DetailLayout | 확인 필요 | 디자인 미확정 |
| 사건접수 · WF01~WF05 | 사건 접수 | `/cases/new` | CaseSubmitPage | DetailLayout | 확인 필요 | 디자인 미확정 |
| 후일담 | 왈가왈후~ | `/afterstory` | AfterStoryPage | MainLayout | 불필요 | 디자인 미확정 |
| 후일담 상세 | 이어진 이야기 상세 | `/afterstory/:storyId` | AfterStoryDetailPage | DetailLayout | 확인 필요 | 디자인 미확정 |
| 마이페이지 | MY | `/my` | MyPage | MainLayout | 필요 | 디자인 미확정 |
| 미확정 | 챗봇 | 확인 필요 | ChatbotPage 또는 바텀시트 | 확인 필요 | 확인 필요 | 디자인 미확정 |
| **시안 없음** | 회원가입 (데모) | `/signup` | SignupPage | AuthLayout | 비로그인 전용 | **구현됨** — 팀 결정으로 추가 (§9-2) |
| — | 존재하지 않는 경로 | `*` | NotFoundPage | ShowcaseLayout | 불필요 | **구현됨** |

챗봇이 별도 화면이 아니라 바텀시트로 설계됐다면 `/chatbot` 페이지를 억지로 추가하지 않는다.
사건마다 별도 페이지 파일을 만들지 않는다. `CaseDetailPage`에서 `caseId`로 데이터를 조회한다.

오류는 구분해서 처리하고, 모든 오류를 무조건 홈으로 보내서 숨기지 않는다.

- 잘못된 URL: 페이지를 찾을 수 없음
- 없는 사건 ID: 삭제됐거나 존재하지 않는 사건
- 권한이 없는 사건(비공개 사건): 접근할 수 없음

### 7-4. 공통 레이아웃과 기기 목업 연결

기기 목업은 페이지마다 복사하지 않는다. 공통 레이아웃에서 한 번만 구성한다.

```
Router
└─ 앱 상태 Provider
   └─ AppRoutes
      ├─ 시연용 진입 화면 (/demo)
      └─ ShowcaseLayout
         └─ DeviceFrame
            └─ AppViewport
               └─ Outlet
                  ├─ AuthLayout   → 온보딩·가입·로그인 계열
                  ├─ MainLayout   → 홈·광장·왈가왈후~·MY
                  └─ DetailLayout → 사건 상세·접수·챗봇
```

- **ShowcaseLayout** — PC에서는 배경과 중앙 기기 목업, 퍼소나 선택 패널 표시.
  모바일에서는 외부 배경과 목업 장식 제거.
- **AuthLayout** — 온보딩·가입·로그인 계열 틀. 하단 내비게이션은 기본적으로 표시하지 않음.
- **MainLayout** — 앱 헤더와 하단 내비게이션, 내부 페이지 Outlet.
- **DetailLayout** — 뒤로가기, 제목, 완료 버튼 등. 하단바 표시 여부는 실제 디자인을 따름.

레이아웃의 모양과 로그인 접근 권한은 별개로 관리한다.
`AuthLayout`을 사용한다고 모두 비로그인 전용 페이지인 것은 아니다.

PC·모바일 전환 때문에 앱의 주요 컴포넌트를 교체하거나 라우터와 Provider를 다시 생성하지 않는다.
가능하면 동일한 컴포넌트 트리에서 CSS로 표현을 바꾼다.
PC용 `HomePage`와 모바일용 `HomePage`를 따로 만들지 않는다.

`AppViewport`가 앱의 화면 경계를 담당한다.
모달·바텀시트용 portal 대상은 기기 내부이면서 스크롤 콘텐츠 바깥에 마련한다.

### 7-5. 퍼소나와 로그인 상태 분리

퍼소나 선택과 로그인 여부를 같은 값으로 취급하지 않는다.

- `personaId`: A / B / C 중 어떤 시나리오인지
- `sessionStatus`: 초기 복원 중 / 비로그인 / 로그인
- `currentUser`: 현재 사용자 정보 또는 `null`
- `profileCompleted`: 초기 설정 완료 여부

A는 처음에는 비로그인이지만 가입 후에는 로그인 사용자가 된다.
B(곽지훈)도 로그아웃할 수 있으므로 "B이면 항상 로그인" 같은 조건을 적용하지 않는다.
퍼소나는 초기 시나리오와 데이터 선택 기준이지 인증·권한의 대체 수단이 아니다.

페이지 접근 규칙

1. **공개 페이지** — 비로그인도 접근 가능.
   `둘러보기 우선` 원칙(§0-6)에 따라 홈·광장·사건 열람은 비로그인도 가능하게 한다.
2. **로그인 필요 페이지** — `RequireAuth`로 공통 처리. 비로그인이면 기획된 가입 경로로 안내.
3. **비로그인 전용 페이지** — 이미 로그인한 사용자를 가입 화면으로 다시 보내지 않는다.
4. **초기 설정 페이지** — 가입은 했지만 초기 설정을 마치지 않은 사용자가 접근.
   단순 비로그인 전용 가드 아래에 넣지 않는다.

저장된 상태를 복원하는 중에는 접근 여부를 성급하게 판단하지 않는다.
복원이 끝나기 전에 로그인 화면이 잠깐 나타나거나 기존 데이터를 빈 값으로 덮어쓰지 않게 한다.

보호된 페이지에 접근하다가 로그인했다면 원래 목적지로 돌아갈 수 있게 한다.
복귀 경로는 검증된 앱 내부 경로만 허용한다.

비로그인 사용자가 투표·댓글·저장 등 제한된 동작을 누르는 경우도 페이지 접근과 별도로 처리한다.
프론트엔드에서 버튼과 페이지를 숨긴 것만으로 보안을 구현했다고 하지 않는다.

### 7-6. 퍼소나별 최초 진입과 직접 URL 접근

```
퍼소나 선택 기록 없음        → /demo
A 최초 시작 또는 A 초기화     → 가입 시작 화면 (디자인 확정 후)
A 가입 및 초기 설정 완료      → /home
B 최초 시작                 → 곽지훈 데모 데이터 준비 → /home
```

퍼소나별 직접 진입 주소: `/demo?persona=A` · `/demo?persona=B`

직접 진입 파라미터는 해당 시나리오를 선택하는 데만 사용한다.
기존 진행 데이터가 있으면 이어서 체험한다.
데이터 초기화는 명시적인 초기화 동작으로만 수행한다.
파라미터 처리 후에는 실제 시작 화면으로 `replace` 이동해
뒤로가기 때 같은 처리가 반복되지 않게 한다.

퍼소나 선택 때문에 서비스 URL 전체를 복제하지 않는다.
금지 예: `/a/home`, `/b/home`, `/desktop/home`, `/mobile/home`

A·B와 PC·모바일은 같은 서비스 URL을 사용한다.
보여주는 데이터와 외부 표현 방식만 달라진다.

최초 진입 분기는 `/`와 명시적인 데모 진입에서만 처리한다.
사용자가 `/cases/c001`을 직접 열었는데 전역 효과가 무조건 `/home`으로 보내면 안 된다.
직접 연 URL은 필요한 상태 복원과 접근 확인 후 유지한다.

### 7-7. 이동·뒤로가기·검색·스크롤 규칙

앱 내부의 일반 페이지 이동에는 `Link`를 사용한다.
하단 내비게이션처럼 활성 상태가 필요한 링크는 `NavLink`를 사용한다.
가입 완료, 사건 접수 완료 등의 처리 이후 이동은 `useNavigate`를 사용한다.
앱 내부 이동에 `window.location.href`를 반복 사용하지 않는다.

하단 메뉴의 활성 상태는 현재 URL에서 판단한다.
별도의 `selectedMenu` 상태와 URL이 어긋나지 않게 한다.

검색어·카테고리·정렬처럼 새로고침하거나 공유해도 유지돼야 하는 조건은 URL 쿼리로 관리한다.
예: `/plaza?category=money&sort=latest`, `/plaza?q=축의금`

입력 중인 검색어와 확정된 검색 조건은 구분한다.
키를 입력할 때마다 불필요한 방문 기록을 쌓지 않는다.

단순 모달 열림 여부는 화면 상태로 관리한다.
공유 주소나 브라우저 뒤로가기가 필요한 모달만 라우트와 연결한다.

뒤로가기 버튼은 앱 내부에서 이동해 온 기록이 있을 때 돌아간다.
외부에서 상세 URL로 바로 들어온 경우에는 `/plaza` 등 정해진 fallback으로 이동한다.
`history.length`만 보고 안전한 앱 내부 이전 화면이 있다고 가정하지 않는다.

스크롤은 실제 앱 콘텐츠 컨테이너를 기준으로 관리한다.

- 새로운 상세 화면 진입: 상단에서 시작
- 목록 → 상세 → 뒤로가기: 목록의 검색 조건과 스크롤 위치 복원
- 주요 탭 재방문: 기획에서 정한 스크롤 정책 적용
- 챗봇: 일반 페이지와 구분되는 대화 스크롤 정책 적용

기기 내부가 스크롤되는데 `window.scrollTo`만 호출하는 구현은 하지 않는다.

### 7-8. 상태와 더미데이터의 저장 위치

- **화면 내부 상태** — 모달 열림, 입력 중인 값, 펼침 여부.
  해당 페이지 또는 가장 가까운 공통 부모에서 관리.
- **URL 상태** — 현재 페이지, 사건 ID, 검색어, 필터.
  라우터에서 읽고 변경하며, 같은 값을 전역 상태에 중복 저장하지 않음.
- **앱 공유 상태** — 선택한 퍼소나, 현재 사용자,
  여러 화면에서 함께 쓰는 사건·저장·좋아요 변경 사항.
- **지속 저장할 데모 상태** — 퍼소나별 진행 상황과 활동 기록.
  저장·복원 로직을 한곳에 모아 관리.

컴포넌트마다 `localStorage`를 직접 읽고 쓰지 않는다.
변경은 상태 관리 함수를 통해 수행하고 저장 로직은 분리한다.

원본 seed 데이터와 사용자가 변경하는 상태를 분리한다. seed 객체를 직접 수정하지 않는다.

변경은 관련 화면에 일관되게 반영한다.

- 목록에서 좋아요 → 상세에서도 같은 상태
- 상세에서 저장 → MY 저장 목록에도 반영
- 작성한 사건 삭제 → 목록·상세·내 활동 기록에서 일관되게 처리

퍼소나별 데이터 저장 공간과 요청 결과를 분리한다.
전환 전 사용자의 늦게 도착한 챗봇 응답이 전환 후 사용자의 화면에 반영되지 않도록
취소하거나 무시한다.

페이지가 바뀔 때마다 전체 Provider를 초기화하지 않는다.
다만 퍼소나가 바뀔 때는 이전 사용자의 임시 입력이 남지 않도록 필요한 화면 상태만 정리한다.

### 7-9. 컴포넌트·타입·스타일 작성 기준

페이지 컴포넌트는 화면 구성을 담당한다.
복잡한 데이터 처리, 저장소 처리, API 호출을 한 파일에 몰아넣지 않는다.

재사용 컴포넌트는 props로 필요한 데이터를 받는다.
`CaseCard` 내부에서 특정 퍼소나의 seed 파일을 직접 import하지 않는다.
공통 컴포넌트에 화면별 문구·수치를 고정하지 않는다.
선택·읽음·비공개·마감 같은 상태는 prop 또는 명확한 로컬 state로 표현한다.
CSS 클래스만으로 상태를 숨기지 않는다.

서비스 처리는 `services/`, 공유 상태 변경은 `state/`,
재사용되는 React 로직은 `hooks/`에 둔다.
실제 API와 mock 구현은 가능한 한 같은 입출력 타입을 사용한다.

TypeScript에서는 다음을 지킨다.

- 컴포넌트 props와 주요 데이터 타입 명시
- `any`나 `@ts-ignore`로 오류를 일괄 숨기지 않음
- URL 파라미터가 없거나 잘못된 경우 처리
- 저장소에서 읽은 데이터의 누락·손상 여부 확인
- 타입 단언만으로 외부 데이터가 검증됐다고 간주하지 않음

명명 규칙

- 컴포넌트 파일명은 `HomePage.tsx`, `CaseCard.tsx`처럼 의미를 드러낸다.
- `HomeA.tsx`, `HomeB.tsx`처럼 퍼소나별 화면을 복제하지 않는다.
- 이벤트 함수는 `handle`로 시작한다.
- 불리언 상태는 `is`·`has`·`can`·`should`로 시작한다.
- 임시 `console.log`를 남기지 않는다.

전역 CSS에는 초기화·폰트·디자인 토큰 등 공통 규칙을 두고,
페이지·컴포넌트의 스타일은 해당 영역에 가깝게 둔다.

학생이 수정할 수 있도록 필요한 주석을 작성한다.
문법을 그대로 반복하는 주석보다 상태 분리 이유, 라우트 접근 조건,
PC·모바일 차이를 설명한다.

### 7-10. 구현 순서

1. **기존 코드와 Figma 확인** — 화면·URL·레이아웃 대응표 작성(§7-3), 패키지·라우터 방식 확인 ✅
2. **TypeScript 전환 및 공통 구조 연결** — tsconfig 추가, 기존 파일 마이그레이션,
   Router 연결, PC 목업·모바일 공통 화면 틀 구현, NotFound 연결 ✅ (`npm install` 후 검증 필요)
3. **홈 화면 완성** — 상태바를 목업으로 분리 ✅ / 실제 에셋 적용·폰트 등록은 남음
4. **대표 사용자 흐름 완성** — B(곽지훈)의 기존 사용자 진입, 목록 → 상세 → 뒤로가기 연결
   (A(윤서아)의 가입 흐름은 디자인 확정 후)
5. **기능 연결** — 커뮤니티 상태 변경, 챗봇 mock 연결, 오류·빈 상태·로딩 처리
6. **검증과 인계** — 타입 검사·빌드, 배포 URL 직접 접근과 새로고침, 수정 위치 정리

Vite 개발 서버에서 화면이 보인다는 이유만으로
타입 검사와 프로덕션 빌드까지 통과했다고 판단하지 않는다.

`BrowserRouter`를 사용하는 SPA는 배포 환경에서도 하위 URL이 앱 진입 파일로 연결되도록 설정한다.
배포를 피하려고 임의로 `HashRouter`로 바꾸지 않는다.

### 7-11. 변경 시 지킬 것

- 사용자가 요청한 화면과 담당 폴더만 수정한다.
- 공통 코드(`App.tsx`, `main.tsx`, `src/components/`, `src/layouts/`)는
  팀 담당자와 조율 없이 대규모로 바꾸지 않는다.
- 새 라이브러리 설치, 폴더 이동, 파일명 변경, 전면 리팩터링은 요청이 있을 때만 한다.
- 여러 파일을 바꾸기 전에 변경 범위와 공통 파일 영향 여부를 짧게 정리한다.
- 실제 에셋, 기존 CSS, 기존 컴포넌트와 확정된 토큰을 우선 재사용한다.
- 컨펌 시안과 IA가 충돌하거나 시안에 없는 결정을 해야 하면
  구현 전 이유·영향·확인 필요 사항을 §9와 `PROJECT_CONTEXT.md`에 남긴다.
- 작업이 끝나면 `PROJECT_CONTEXT.md`에 완료 내용과 검증 결과를 갱신한다.

### 7-12. 설정을 바꾸는 위치

강사 기준이 요구하는 "배경·기기·퍼소나 데이터를 수정하는 위치"다.
값을 여기저기 흩어 두지 않고 아래 파일에만 둔다. 화면 코드에 숫자를 직접 적지 않는다.

| 바꾸고 싶은 것 | 파일 | 이름 |
| --- | --- | --- |
| 서비스명·한 줄 소개 | `src/config/app.ts` | `SERVICE` |
| 기준 기기·화면 크기(402 × 874) | `src/config/app.ts` | `DEVICE` |
| 기기 테두리·베젤 두께 | `src/config/app.ts` + `DeviceFrame.css` | `FRAME_PADDING`, `BEZEL_PADDING` |
| PC 배경 이미지·그라데이션 | `src/config/app.ts` | `SHOWCASE_BACKGROUND` |
| PC ↔ 모바일 전환 너비 | `src/config/app.ts` | `DESKTOP_BREAKPOINT` |
| 데모 모드 on/off, 저장소 키 | `src/config/app.ts` | `DEMO` |
| 컬러·폰트·텍스트 스케일 | `src/styles/tokens.css` | CSS 변수 |
| 퍼소나 정의 | `src/data/personas/index.ts` | `PERSONAS` |
| 시연 계정(이메일·닉네임) | `src/data/personas/index.ts` | `DEMO_ACCOUNTS` |
| 홈 화면 더미데이터 | `src/data/common/homeContent.ts` | — |
| URL 경로 | `src/routes/paths.ts` | `PATHS` |
| 하단바 메뉴·활성 여부 | `src/components/common/BottomNavigation.tsx` | `NAV_ITEMS` |
| 가입 폼 입력 항목 | `src/pages/Auth/SignupPage.tsx` | `FIELDS` |

화면이 새로 생기면 `NAV_ITEMS`의 `enabled`를 `true`로 바꾸고
`AppRoutes.tsx`에 `Route`를 한 줄 추가하면 하단바까지 함께 연결된다.

---

## 8. 검증 및 결과 보고

### 확인할 화면 조건

- PC: 1440 × 900, 1366 × 768
- 모바일: 너비 360, 390, 402, 430
- 모바일 가로 방향
- 모바일 키보드가 열린 가입·댓글·챗봇 화면

### 라우팅·상태 체크리스트

- [ ] 주요 화면을 각각 URL로 직접 열 수 있다.
- [ ] 상세 페이지에서 새로고침해도 정상 표시된다.
- [ ] 브라우저 뒤로가기·앞으로가기가 의도대로 작동한다.
- [ ] 외부에서 직접 연 화면의 뒤로가기 fallback이 있다.
- [ ] 없는 URL과 없는 사건을 구분해 처리한다.
- [ ] 로그인 상태 복원 중 잘못된 리다이렉트가 발생하지 않는다.
- [ ] A는 가입 후 정상적인 로그인 사용자 흐름으로 이어진다.
- [ ] B가 로그아웃해도 자동으로 다시 로그인되지 않는다.
- [ ] 퍼소나 선택 때문에 서비스 페이지가 복제되지 않는다.
- [ ] PC·모바일 전환으로 현재 화면과 입력값이 초기화되지 않는다.
- [ ] 목록의 검색 조건과 필요한 스크롤 위치가 복원된다.
- [ ] 개인 상태와 비동기 응답이 퍼소나 사이에 섞이지 않는다.
- [ ] 시연 초기화와 단순 새로고침이 구분된다.
- [ ] 타입 검사와 빌드를 실제 실행한 결과가 기록된다.

### 화면·시연 체크리스트

- [ ] PC에서 기기가 화면 중앙에 놓인다.
- [ ] 낮은 PC 화면에서도 목업의 아래쪽이 접근 불가능하게 잘리지 않는다.
- [ ] PC 배경과 기기 설정을 쉽게 교체할 수 있다.
- [ ] 모바일에서 기기 테두리와 가짜 시스템 UI가 사라진다.
- [ ] 모바일에서 실제 화면 너비에 맞고 가로 스크롤이 발생하지 않는다.
- [ ] 시스템 상태바가 목업과 앱에서 중복 표시되지 않는다.
- [ ] 모달·바텀시트·토스트가 의도한 앱 영역 안에서 표시된다.
- [ ] A의 개인 빈 상태와 공개 커뮤니티 데이터가 구분된다.
- [ ] B는 기존 사용자 상태로 진입하고, 데이터가 곽지훈의 기획과 연결된다.
- [ ] 퍼소나를 전환해도 다른 사용자의 개인 상태가 섞이지 않는다.
- [ ] 퍼소나별 초기화 후 동일한 시나리오를 다시 체험할 수 있다.
- [ ] mock 기능과 실제 API 기능이 명확히 구분된다.

### 제품 완료 조건

- [ ] 각 담당 화면이 해당 Figma 컨펌 시안과 IA 흐름을 함께 만족한다.
- [ ] AI 판정이 근거·양쪽 맥락·한계를 읽을 수 있는 정보 구조로 표현된다.
- [ ] 민감한 사연에서 공개/비공개와 전문가 도움의 안전한 진입점이 누락되지 않는다.
- [ ] 기기 내부 402 × 874에서 텍스트 잘림, 의도하지 않은 가로 스크롤, CTA·하단바 겹침이 없다.
- [ ] `npm run lint`, `npm run typecheck`, `npm run build`가 통과한다.
- [ ] 서버 없이 표현한 데이터·상태가 `PROJECT_CONTEXT.md`에 기록됐다.

### 결과 보고에 남길 내용

- 구현한 화면과 연결된 사용자 흐름
- PC·모바일의 표시 방식
- A·B별 시작 경로와 데이터 차이
- 배경·기기·퍼소나 데이터를 수정하는 위치
- 실제 구현 / mock 구현 / 미구현 구분
- 실행한 검증과 결과
- 확인하지 못한 자료와 남은 문제

사용 가능한 검사 스크립트를 확인한 뒤 실제로 실행한다.
실행하지 못한 검사나 실기기 테스트를 통과했다고 말하지 않는다.

---

## 9. 확인 필요 항목

확정 전까지 임의로 결정하지 않는다. 결정되면 이 문서에 반영하고 이 목록에서 지운다.

### 아직 확정되지 않은 것

| # | 항목 | 내용 | 확인 대상 |
| --- | --- | --- | --- |
| 1 | 퍼소나 세부 특성 | A=윤서아(신규 가입), B=곽지훈(기존 사용자), C 없음으로 확정. §0-5의 `[입력]` 5칸이 아직 비어 있다. **채울 근거는 자료종합에 있다** — 윤서아 `24세 · 대학생 · 사연 제출 중심`, 곽지훈 `31세 · 프리랜서 · 법률 도움 진입 장벽형`(메모: 신중히 정보를 수집한 후 사건을 접수하는 타입, 민감한 사건이라 AI 1심만 받고 챗봇과 유사 사건의 도움으로 해결). 이 값으로 §0-5를 채울지 팀 확인 필요 | 팀·기획 |
| 3 | 홈·광장 외 화면 디자인 | 사건 접수·상세·후일담·마이페이지가 작업 중. 특히 **사건 상세**가 없어 가입 진입점(`로그인하고 나도 투표하기`)을 제자리에 붙이지 못하고 홈 버튼에 임시로 걸어 두었다 | 각 담당자 |
| 12 | 관점 선택 항목 수와 문구 | 타입은 3택(`writer` / `other` / `both`)으로 잡았으나 자료종합에서는 **4택**이 일관되게 나온다 — 전제구조ia의 사건 상세, 서아 플로우, 지훈 플로우 모두 `글쓴이 입장이 더 타당해요` / `상대(친구·팀원·의뢰인) 입장이 더 타당해요` / `양쪽 모두 일리가 있어요` / `양쪽 모두 타당하지 않아요`다. 같은 파일의 챗봇 FAQ에는 또 다른 표기(`내 잘못 / 상대 잘못 / 둘 다 잘못 / 판단 보류·정보 부족`)가 있다. 개수(3 vs 4)와 문구(`~이해돼요` vs `~더 타당해요` vs `~잘못`)를 함께 확정해야 한다 | 팀·기획 |
| 13 | 광장 랭킹 탭 전환 화면 | 탭(주간·월간 등)을 눌렀을 때의 화면이 시안에 없다. 선택 상태만 로컬로 표현하고 목록은 바꾸지 않았다 | 디자인 담당 |
| 14 | 광장 정렬·검색·페이지네이션 | 컨트롤은 시안에 있으나 동작 후 화면이 없다. 현재 비활성(`disabled`)으로 두고 카테고리 칩만 실제로 거른다 | 디자인 담당 |
| 15 | 광장 `랭킹 보러가기` 목적지 | 버튼은 있으나 이동할 랭킹 전체 화면 시안이 없다. 라우트를 만들지 않았다 | 디자인 담당 |
| 6 | 챗봇 화면 형태 | 컨펌 섹션에 챗봇 대화 전용 화면이 없다. 판멍이는 홈 AI 추천 카드와 사건 상세의 `판멍이 1심 판결`로만 등장한다. 별도 화면인지 바텀시트인지 미정이라 라우트를 만들지 않았다 | 디자인 담당 |
| 7 | 폰트 에셋 | RIDIBatang, Noto Sans KR이 프로젝트 에셋에 없다. Paperlogy·Pretendard도 `@font-face` 등록이 아직 없다 | 디자인 담당 |
| 8 | PC 배경 이미지 | 미제공. `SHOWCASE_BACKGROUND.gradient`로 임시 처리 중 (`src/config/app.ts`) | 디자인 담당 |
| 9 | 로그인 필요 범위 | 사건 상세·접수·MY 중 어디까지 비로그인 열람이 가능한지 미정. 홈 시안의 `로그인 하고 사건 투표하기`만 확인됨 | 팀·기획 |
| 10 | 사건 상세 프레임 3종 | `서아 상세 투표`(1136:18076), `서아 상세`(1136:18280), `서아 상세`(1223:7356)가 각각 존재한다. 어느 쪽이 기준인지 미정 | 디자인 담당 |
| 11 | 프로필 전환 팝업 | `ProfilePopup/persona1`(곽지훈·윤서아) 팝업이 마이페이지 섹션에 있으나 **아직 확정되지 않은 시안**이다. 계정 전환 UI로 쓸지 미정. 자료종합 IA에는 경로가 `내정보 → 프로필 → 계정 전환`으로 적혀 있다 | 디자인 담당 |
| 18 | 기준 페이지 이관 범위 | 2026-09-09 결정으로 확정 판단 기준이 `1차 디자인 시안 > 컨펌`에서 `개발` 페이지로 옮겨졌다. 이미 구현한 홈은 컨펌의 `김하은/홈수정`이 기준이라 두 페이지가 섞여 있다. 홈 시안도 `개발`로 옮길지, 컨펌을 그대로 둘지 미정 | 팀·디자인 담당 |
| 19 | 자료종합 토큰 불일치 | 자료종합 `Color`·`Corner_Radius(임시)`에서 확인된 어긋남 4건. (1) Neutral이 12단계이고 `#78757A`와 `#A29FA3` 사이 단계의 칩 값(`#939094`)과 표기(`#818181`)가 다르다. (2) Blue 500이 칩 `#649EFF` vs 표기 `#659EFF`. (3) Orange 100이 칩 `#FEFAEF` vs 표기 `#FEFBEF`. (4) 인용 카드 배경 `#FFF6E8`이 컬러 스케일에 없다. 현재 `tokens.css`는 표기 쪽 값을 쓰고 있고 12번째 Neutral 단계는 빠져 있다 | 디자인 담당 |
| 20 | 반경 토큰화 여부 | 자료종합에 임시 반경 기준(12 · 8 · 4 · pill 50)이 있으나 `tokens.css`에는 반경 변수가 없다. 화면마다 값을 직접 적을지, 확정 후 토큰으로 올릴지 미정 | 팀·디자인 담당 |

### 정리된 것 (기록)

| # | 항목 | 결론 |
| --- | --- | --- |
| 2 | 회원가입 화면 시안 부재 | 팀 결정으로 `/signup`을 시안 없이 추가했다. 확정 스타일가이드 토큰만 사용했고, 시안이 나오면 교체한다. 온보딩·별도 로그인 화면은 만들지 않는다 |
| 4 | 하단바 메뉴 표기 | 컨펌 시안 기준으로 확정 — `홈 / 배심원 광장 / 사건 접수(중앙 CTA) / 왈가왈후~ / MY`. 자료 IA의 `알림`은 컨펌 시안에서 상단 헤더 아이콘이므로 충돌이 아니라 IA가 구버전이다. 팀 공유만 필요 |
| 5 | `down_nav` 홈 인디케이터 | 인디케이터 자리가 포함돼 있다. 막대는 목업의 `HomeIndicator`가 그리고 앱 하단바는 그리지 않는다 (§0-4) |
| 16 | 광장 비트맵 에셋 | 해결됨 (2026-09-09). Figma에서 직접 내보내 `src/assets/plaza/`에 넣었다 — `hero-bg.webp` · `hero-mascot.png` · `hero-trophy.png` · `rank-1.png` · `rank-2.png` · `rank-3.png`. 컴포넌트가 직접 import 하며, 초안에 있던 `optionalImages.ts` 자동 감지 방식은 쓰지 않는다 |
| 17 | 아이콘 확정 범위 | 디자이너 메모상 아이콘 전체는 아직 확정이 아니다. 다만 `수정금지` 프레임의 **기타 아이콘 · nav · 화살표 · 꼬리화살표**는 확정이므로 이 네 묶음만 코드에 쓴다. 나머지는 모아 둔 참고용이라 쓰지 않는다 |

### 밸런스 게임 추가 승인 (2026-09-09)

사용자 요청으로 깻잎 외 3문항을 데모 카피로 추가한다. 새우 껍질·패딩 지퍼·차량 블루투스 주제를 사용하며, 확정 Figma 시안으로 취급하지 않는다. 추가 문항은 기존 토큰의 질문 카드로 표현한다. 4문항 순환·문항별 로컬 선택 유지·퍼소나 전환 초기화를 지원하며 집계 수치나 별도 결과 화면은 추가하지 않는다.

### 밸런스 게임 범위 정정 (사용자 요청)
깻잎 1문항만 우선 구현한다. 첨부된 상태 시안에 따라 선택 접시에 고정 → 확대 → 제공된 깻잎 더미 이미지로 전환한다. 다른 문항은 노출하지 않으며 새로고침 아이콘은 다시하기로 사용한다. 1/4 표시는 참조 시안을 유지한 것이다.

밸런스 게임 동작 정정: 드래그 중 접시 위에서 접시가 점진 확대되며, 손을 놓는 순간 확정하여 해당 접시에서 깻잎 더미 결과로 전환한다. 드래그 유지 중 자동 확정하지 않는다.

### 막상막하 상호 교환 동작 (사용자 요청, 2026-09-10)

바꿔보기는 아래 작은 사건을 위 게이지 사건과 교환한다. 교환 시 위의 제목·양쪽 비율·게이지 바늘·차이와 아래의 제목·퍼센트·라벨·그래프를 같은 데이터로 함께 갱신한다.
